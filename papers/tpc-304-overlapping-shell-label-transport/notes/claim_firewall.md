# TPC-304 claim firewall

## PROVED_EXACT_FINITE

- absolute normalized overlap inner product is invariant under independent
  global sign gauges;
- aligned disagreement equals `(1-rho)/2` for binary labels;
- the finite argmin/argmax comparison is logically valid once the certificate
  rows are fixed.

## NUMERICALLY_CERTIFIED_FINITE

- six label-transport rows and their exact overlap/mismatch data;
- mean correlations `1/2,1/11,1/2`;
- the unique fracture at `Q=60->70` under the declared `rho<=1/3` coarse
  threshold;
- budget descent counts `3,15,3` and same-prefix counts `0,9,0`;
- all nine same-prefix descents localize at `Q=60->70`.

## MODELING_CHOICE

The fixed source spine, the source-first parent labels, the global-sign
alignment convention, and the coarse fracture threshold `rho<=1/3`.

## OPEN / NONE

No causal separation of target-label switching from physical shell/operator
change, no asymptotic budget theorem, no arithmetic `L2` credit, no fixed-power
credit, no full Gate B, and no twin-prime conclusion are claimed.
