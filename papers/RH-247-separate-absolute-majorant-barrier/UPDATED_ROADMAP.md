# Roadmap after RH-247

## Current coordinate

```text
cancellation_blind_absolute_majorant_ruled_out_open_grouped_signed_envelope_and_anchor
```

The absolute-value-before-cancellation route is now a strict scoped dead end:
the Hardy-scaled Perron counterloop has root rate `r_H^{-1}>1`.  Any viable
long-loop estimate must group physical and atomic sectors first, as in the
orthogonal quotient of RH-245, and then control a signed/complex quotient
kernel.

The next finite question is whether the anchored reachability obstruction
survives relaxing prefixes to all shell subsets and their convex zonotope.
Gates A--E remain open.
