# RH-352 theorem ledger

| Item | Status | Exact scope |
|---|---|---|
| Growing lower-even window | FROZEN | `m_(k,j)=k-j`, `2<=j<=J_k`, `J_k->infinity`, `J_k=o(k)`. |
| Direct physical coefficient type | SOURCE LOCKED | `p=tau-a=Y+P-S`, with `tau=Tr(C_sigma^n)`. |
| Noisy modulus cap | SOURCE LOCKED | RH-282 gives `|tau_(sigma,n)|<=sigma^(-1)q^(n-2)`, `q=1/2`. |
| Deterministic target cap | SOURCE LOCKED | RH-267 gives `|a_n|<48q_*^n`, `q_*=(r_H lambda)^(-1)`. |
| Uniform local natural-scale cap | PROVED | `limsup U_k^(1/k)<=max(rho_N,rho_T)<1`. |
| Noisy normalized rate | PROVED | `rho_N=r_H^2 lambda^3/4<1419857/1600000<1`. |
| Target normalized rate | PROVED | `rho_T=1/lambda<17/28<1`. |
| Actual normalized direct budget | PROVED | `L_k^act` has the same root ceiling and tends to zero exponentially. |
| Actual signed-remainder tracking | PROVED | Uniformly, normalized `C_M Y` tends to `1-a_k lambda^(2-j)`. |
| Actual aggregate Y law | PROVED | `Yagg_k^act=F_(J_k-2)(a_k)/C_M+o(1)`. |
| RH-350 small-Y hypothesis | DISPROVED | The actual aggregate has liminf at least `A_infinity/C_M>0`. |
| Unnormalized selected prefix closure | OPEN | The separate noisy cap has superunit root `lambda^2(qR)^2>9604/7225`. |
| Full direct prefix and E_off | OPEN | Critical, first-lower, odd, upper-alias, head, and remaining orders are not controlled. |
| RH-241 moving noisy all-order envelope | OPEN | A selected lower-even theorem is not an all-order envelope or coefficient bridge. |
| RH-288 determinant gluing | OPEN | The complete physical prefix leaf is absent. |
| Gates A--E | OPEN | All five values remain false. |

Finite rows reproduce exact bound formulas at a rational in-range fixture.
They are not observations of noisy coefficients and are not asymptotic
evidence.
