# Paper plan

## Question

Does the TPC-406 complete-shell local-entry boundary persist across a larger
prime-shell scale ladder?

## Exact theorem

At fixed `H=66,N=264`, every even complete shell with `Q>N` obeys the same
exact adjacent-entry bound `z<=4/(a_min H)<=4/H`.

## Certificate

Audit all primes at `Q=4096,8192,16384,32768`, with exact CRT and rational
energies, then replay both masked rows independently.

## Boundary

This is a finite Q-scale audit of one synthetic proxy entry.  It does not
bound the full normalized operator, arbitrary origins, physical `h_0`, or
arithmetic source quantities.
