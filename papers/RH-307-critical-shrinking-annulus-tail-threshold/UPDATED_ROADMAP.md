# Roadmap after RH-307

Shrinking the annular gap can preserve the minimal-clock tail only below the
explicit logarithmic threshold.  The next step determines the optimal blowup
of the coefficient-conversion constant as that gap closes.
