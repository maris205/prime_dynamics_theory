[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "rh298-parity-majorant"
version = "0.1.0"
[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
