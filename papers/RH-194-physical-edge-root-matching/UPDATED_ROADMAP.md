# Roadmap after RH-194

```text
full Frobenius rank-4 shell                            [rejected type]
source-cyclic state                                    [proved]
  -> temporal roots match one physical quartet/side    [finite positive]
  -> define source-observation Riesz channels           [next theorem]
  -> normalize a canonical biorthogonal spectral packet[next theorem]
  -> explain temporal-to-spectral convergence           [open]
  -> cross-scale quartet transport and Gate A            [open]
```

The complement-resolvent problem is now reformulated.  The full ambient
complement necessarily contains the repeated column copies.  In the
source-relative spectral channel, the matched mode itself is exactly
invariant; the new challenge is canonicity and all-level transport, not
forcing a false complement-free count in `I_m tensor A`.
