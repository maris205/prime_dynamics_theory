# Roadmap after RH-253

The deterministic target is now tabulated through order 28.  The new
coefficients continue a finite geometric-looking pattern, but no extrapolation
is promoted to a theorem.  The next route decision is to obtain a genuinely
new spectral input: either an expanded resolved candidate window or a
structurally defined signed selector.

Route coordinate:

```text
analytic_target_tail_order28_atlas_open_expanded_resolved_window_or_signed_selector
```

The RH-252 analytic tail remains conditional on its boundary constant `M_S`.
Gates A--E remain false/open.
