# TPC-313 local route evaluation

The Session-named `propose.md` and `skills/route-a-evaluator.md`/
`route-b-evaluator.md` are absent from this checkout.  This is a fail-closed
local assessment using the proof package, exact certificate, independent
replay, stress suite, and Bridge-B checker.

```text
ROUTE_A = NOT_EVALUATED_OFFICIALLY
ROUTE_B = SCOPED_ADVANCE_ONLY
ROUTE_B_FINITE_INTERFACE = YES
ROUTE_B_ASYMPTOTIC_ARITHMETIC = NO
ROUTE_B_EXTERNAL_INDEPENDENCE = NO
ROUTE_B_FIXED_POWER_CREDIT = 0
FULL_GATE_B = OPEN
```

Strongest positive: a formal outward-rounded rational budget interface on a
new source-shell panel, with strict finite thresholds on a common prefix.

Strongest obstruction: the weighted label is still generated from the same
physical Gram, and no canonical/external weighting law follows.

Open theorem: a weighting law justified independently of the target Gram that
survives a genuinely fresh physical holdout and admits a growing estimate.

Reusable structure: `source profile -> exact image/Gram -> first feasible
prefix -> rational primal/dual pair -> outward interval -> threshold audit`.

`ROUND2_CLUE = AUDIT_EXTERNALLY_JUSTIFIED_WEIGHTING_ON_A_FRESH_PHYSICAL_HOLDOUT_AFTER_FORMAL_BUDGET_CERTIFICATION`.
