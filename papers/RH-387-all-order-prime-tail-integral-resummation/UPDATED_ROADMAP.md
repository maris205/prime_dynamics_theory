# RH-387 updated roadmap

## Route A: GO

The executable route closed by RH-387 is

    explicit VK theta envelope
      -> strict absolute Stieltjes error
      -> all-r Tonelli/logarithmic resummation
      -> exact-to-power logarithmic integral
      -> real seven-coordinate cube
      -> endpoint l_infinity/l_1 Lipschitz ledger
      -> 3528/588 gap bounds.

This route is reusable when a later argument consumes the complete RH-383
endpoint map at one prime cutoff and needs a first-order integral
replacement. The strict endpoint, seven real channels, and fixed
prime-index order of limits are part of the interface.

## Route B: STOP_SCOPED

Stop rather than extrapolate when a consumer asks for:

- P_2-scale, second-order, or cubic coefficient precision;
- a complex channel c or any channel outside the seven frozen integers;
- a clock growing with a prefix length or a joint prefix/prime limit;
- active phasewise c11 cancellation;
- replacement of the analytic source theorem by a finite fit;
- an operator, trace, zero-divisor, or RH conclusion.

The obstruction is quantitative: epsilon_x x^2 tends to infinity, so the
source term dominates the x^-3/L power-kernel scale.

## Source route

The external analytic input is only Johnston--Yang Theorem 1.4,
equation (1.8), printed page 2. Corollary 1.2, its table fallback, and the
two recorded source typos are provenance-only and not theorem inputs. The
remote author PDF and source tar remain nonredistributed and are never
publication members.

## Artifact route

Release completion requires simultaneous agreement of:

1. 42 exact oracle rows and 24 genuine mathematical mutations;
2. fresh result and recursively closed official Draft 2020-12 schema;
3. 68 immutable Git blobs plus one canonical remote logical lock;
4. default-offline and explicit opt-in source verification;
5. byte-identical main and semantic PDFs;
6. publication manifest plus external-input replay;
7. theorem, integrity, review, format, remote-source, visual, and replay
   audits.

Finite rows always retain the role reproduction_not_analytic_proof.
