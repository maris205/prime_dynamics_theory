# TPC-392 proof package

## Proved finite facts

1. The selected affine intervals are pairwise disjoint from the declared
   prior panels and from one another.
2. The rational anchor has positive row geometry and symmetric law matrices.
3. The predeclared Cartesian panel has exactly 256 rows and 32 phase cells.
4. The four normalization definitions, calibration/holdout roles, and
   forecast functional are fixed in the canonical payload.
5. The producer certificate is canonical JSON with a payload hash and fixed
   parent provenance.
6. An independent descending-shell implementation reconstructs the rows and
   phase aggregates; its 25-mutation stress suite rejects altered contracts.

## Numerically certified finite facts

The independent replay certifies the 7/8 versus 8/8 forecast census, the
maximum errors, terminal ordering, stability counts, and row-level envelope
flags reported in the README and paper.  These are scoped to the declared
finite panel.

## Open facts

The certificate does not prove that any normalization is source-valid for a
growing analytic operator.  Origin/count uniformity, a growing operator
bound, source-uniform arithmetic $L^2$, Route-A closure, Route-B reassembly,
and the twin-prime endpoint remain open.  No fixed-power credit is assigned.

## Route status

The official Session evaluator files are absent from this checkout.  The
available local Route-B evidence is fail-closed artifact consistency only;
it cannot declare an official Route-A or Route-B pass.
