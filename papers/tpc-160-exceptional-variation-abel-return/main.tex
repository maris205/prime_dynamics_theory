\documentclass[11pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{microtype}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[colorlinks=true,linkcolor=blue!55!black,citecolor=blue!55!black,urlcolor=blue!55!black]{hyperref}
\usepackage[nameinlink,noabbrev]{cleveref}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}
\newcommand{\E}{\mathcal E}
\newcommand{\Bbad}{\mathcal B}
\newcommand{\Sshadow}{\mathcal S}
\newcommand{\Treg}{\mathcal T}
\newcommand{\norm}[1]{\lVert#1\rVert}

\title{\textbf{Exceptional-Variation Abel Return:\\
Literal Weights, Almost-Prefixes, and the Atomic All-Prefix Barrier}}
\author{Liang Wang\\
\small School of Artificial Intelligence and Automation,\\[-2pt]
\small Huazhong University of Science and Technology, Wuhan 430074, P.R. China\\
\small \texttt{wangliang.f@gmail.com}}
\date{July 2026}

\begin{document}
\maketitle

\begin{abstract}
TPC-159 controls cumulative determinant-two M\"obius sums at every
endpoint outside a low range and an explicit dyadic exceptional
shadow.  We combine that result with exact Abel summation.  A literal
weight is charged by its variation on good and bad endpoints:
good variation receives the logarithmic correlation saving, while
bad variation receives only the trivial bound.  This gives a sharp
conditional route for physical weights.  For a prefix cutoff the
Abel derivative is one atom, so uniform all-prefix control requires
the actual endpoint registry to avoid the shadow or a new pointwise
theorem.  Exceptional-set density alone cannot provide this.
\end{abstract}

\noindent\textbf{Keywords.}
Abel summation; M\"obius correlation; literal weight; exceptional
variation; atomic prefix.

\medskip
\noindent\fcolorbox{red!70!black}{red!3}{\parbox{0.94\linewidth}{
\textbf{Claim boundary.}
The Abel inequality is proved on the actual periodic two-M\"obius
core.  A decaying physical-weight estimate remains conditional on a
source-locked literal weight and quantitative variation bounds.
The all-prefix obstruction stops only this exceptional-shadow/Abel
route.  No generic phase, four-sign H3 return, positive fixed-\(X\)
power, \(1/400\), prime-pair lower bound or twin-prime theorem is
proved.}}

\section{Ordered fiber and the bad set}

Retain
\[
 q=as,\qquad t(z)=ad+qz,\qquad
 c_z=\mu(d+sz)\mu(u+az),
\]
and a bounded period-\(R\) function \(\rho\).
Let
\[
 0<t_1<t_2<\cdots<t_m\le T
\]
be the points \(t(z_i)\), and put
\[
 \sigma_i=c_{z_i}\rho(z_i),\qquad
 A_i=\sum_{j=1}^i\sigma_j.
\]
TPC-159 fixes
\[
 J=\lceil A\log_2\log X\rceil,\qquad
 \Sshadow_{X,J}=\bigcup_{j=1}^J2^j\E_X^\star
\]
and proves a prefix estimate outside that shadow
\citep{WangTPC159}.  Define
\begin{equation}
 \Bbad_{X,J}=[1,2^J\sqrt X)\cup\Sshadow_{X,J}.
\label{eq:bad}
\end{equation}

\section{Exact Abel summation}

For arbitrary literal weights \(w_1,\ldots,w_m\in\mathbb C\), set
\begin{equation}
 d_i=w_i-w_{i+1}\quad(1\le i<m),\qquad d_m=w_m.
\label{eq:difference}
\end{equation}

\begin{lemma}[Finite Abel identity]\label{lem:abel}
\begin{equation}
 \sum_{i=1}^m\sigma_iw_i=\sum_{i=1}^mA_id_i.
\label{eq:abel}
\end{equation}
\end{lemma}

\begin{proof}
The coefficient of \(\sigma_j\) on the right is
\[
 \sum_{i=j}^{m-1}(w_i-w_{i+1})+w_m=w_j.
\]
\end{proof}

\begin{definition}[Exceptional variation]\label{def:variation}
Set
\[
 V_{\rm good}(w)=
 \sum_{\substack{1\le i\le m\\t_i\notin\Bbad_{X,J}}}|d_i|,
 \qquad
 V_{\rm bad}(w)=
 \sum_{\substack{1\le i\le m\\t_i\in\Bbad_{X,J}}}|d_i|.
\]
\end{definition}

\section{The weighted return}

Put
\begin{equation}
 \varepsilon_X=(\log X)^{-\kappa_0}+2^{-J}+\frac qT.
\label{eq:epsilon}
\end{equation}

\begin{theorem}[Exceptional-variation return]\label{thm:main}
Assume the determinant-two and period envelope of TPC-159 and
\(T\le X\).  Then
\begin{equation}
 \boxed{\;
 \frac qT\left|\sum_{i=1}^m\sigma_iw_i\right|
 \ll\norm{\rho}_\infty
 \left\{
 \varepsilon_XV_{\rm good}(w)
 +\left(1+\frac qT\right)V_{\rm bad}(w)
 \right\}.\;}
\label{eq:main}
\end{equation}
\end{theorem}

\begin{proof}
If \(t_i\notin\Bbad_{X,J}\), then it lies in the endpoint range of
TPC-159 and avoids the shadow.  That theorem gives
\[
 \frac q{t_i}|A_i|
 \ll\norm{\rho}_\infty
 \left\{(\log X)^{-\kappa_0}+2^{-J}+\frac q{t_i}\right\}.
\]
Since \(t_i\le T\), this implies
\[
 \frac qT|A_i|\ll\norm{\rho}_\infty\varepsilon_X.
\]
At a bad endpoint, use only
\[
 |A_i|\le\norm{\rho}_\infty\left(\frac Tq+1\right).
\]
Insert these two estimates into \eqref{eq:abel} and separate the
variation according to \cref{def:variation}.
\end{proof}

\begin{corollary}[Conditional logarithmic promotion]\label{cor:promotion}
Suppose
\[
 2^J\sqrt X\le T\le X
\]
and an actual source-locked registry proves, for some
\(\beta,\gamma>0\),
\[
 V_{\rm good}(w)\le(\log X)^{\beta+o(1)},\qquad
 V_{\rm bad}(w)\le(\log X)^{-\gamma+o(1)}
\]
and
\[
 \beta<\min\{\kappa_0,A\}.
\]
Then the literal weighted periodic core has logarithmic exponent
\[
 \min\{\min(\kappa_0,A)-\beta,\gamma\}+o(1).
\]
\end{corollary}

Indeed, \(q\le(\log X)^{\eta_0}\) and the terminal-scale lower bound
make \(q/T\) smaller than every fixed negative power of \(\log X\).
The conclusion then follows by inserting the two variation estimates
in \eqref{eq:main}.  Without a terminal-scale lower bound, the factor
\(1+q/T\) in the bad-variation term cannot simply be discarded.

The corollary is a promotion rule.  The current occurrence archive
does not supply the literal \(w\), its variation, or the required
support theorem.  Its fixed-\(X\)-power exponent remains zero, even
when the displayed logarithmic exponent is positive.

\section{The atomic all-prefix barrier}

For \(1\le k\le m\), consider the literal prefix cutoff
\[
 w_i^{(k)}=\mathbf1_{\{i\le k\}}.
\]

\begin{proposition}[One-atom derivative]\label{prop:atom}
The differences in \eqref{eq:difference} satisfy
\[
 d_i^{(k)}=\mathbf1_{\{i=k\}},
\]
and hence
\begin{equation}
 V_{\rm bad}(w^{(k)})
 =\mathbf1_{\{t_k\in\Bbad_{X,J}\}}.
\label{eq:badatom}
\end{equation}
\end{proposition}

\begin{proof}
The step is constant except for its single drop after index \(k\);
when \(k=m\), the terminal term \(d_m=w_m\) is the same atom.
\end{proof}

Let \(\Treg_X\) be the actual registry of requested prefix endpoints.
To obtain a uniform saving for all its step cutoffs from
\cref{thm:main}, one must prove
\begin{equation}
 \boxed{\quad
 \Treg_X\cap\Bbad_{X,J}=\varnothing,
 \quad}
\label{eq:avoid}
\end{equation}
or add a pointwise theorem covering the intersection.
The logarithmic measure of the shadow cannot imply
\eqref{eq:avoid}.  More precisely, enlarging the source exceptional
set by
\[
 \left\{2^{-J}T:
 T\in\Treg_X\cap[2^J\sqrt X,X]\right\}
\]
does not change its continuous logarithmic measure, does not
invalidate the source theorem outside the enlarged set, and places
the displayed registry endpoints in the \(j=J\) shadow copy.
This is the atomic firewall of TPC-150 \citep{WangTPC150}, now tied
to the exact dyadic shadow forced by the prefix proof.

\section{Phase and ledger interactions}

A rational or sufficiently major-arc phase may enter the periodic
factor using TPC-158 \citep{WangTPC158}; a generic phase remains
outside this theorem.  Alternatively, TPC-157 may charge a literal
multiplier through periodic approximation \citep{WangTPC157}.
These costs must be counted once.  None converts a power of
\(\log X\) into a positive fixed-\(X\) exponent.

The accompanying exact-rational audit verifies Abel summation,
the good/bad variation partition, and the one-atom prefix derivative.
It records the production weight and endpoint registry as
\(\mathrm{NOT\mbox{-}TESTABLE}\).

\section{Conclusion}

The almost-endpoint theorem can now consume a literal weight through
one precise quantity: its variation on the dyadic bad set.  Smooth or
well-distributed weights may pass this interface; atomic prefixes
expose the unresolved endpoint directly.  The next advance must
therefore come from actual provenance plus a variation certificate,
proved shadow avoidance, or a genuinely pointwise fixed-two theorem.

\bibliographystyle{plainnat}
\bibliography{references}
\end{document}
