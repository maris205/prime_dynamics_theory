[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "conditional-prime-dynamics-hilbert-polya-roadmap"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["matplotlib"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
