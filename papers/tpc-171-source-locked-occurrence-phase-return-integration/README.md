# TPC-171: Source-locked occurrence--phase return integration

TPC-171 integrates TPC-163--170 without promoting their quantifiers.

The structural crosswalk gap is now the three-root antichain

```text
H1.source_backed_local_occurrence_edge_family
H1.actual_active_support_certificate
H1.canonical_minimal_representation_certificate
```

TPC-170 supplies a genuine actual-core, all-prefix packet theorem for
Lebesgue-almost every fixed phase. It does **not** supply a named
physical phase, a production phase registry, active support, or
program-positive L2.

The selected-synthesis-root minimal not-testable antichain also
contains the four independent H9 registries. This seven-root list is
not the whole H1 DAG: the complete map descendant still contains the
nine literal defects and the independent occurrence registry, while
the scalar-plus-ETO clause remains a separate alternative. Those
descendants are suppressed in the minimal antichain by their earlier
missing roots.

The local occurrence-edge root carries only
`SOURCE_BACKED_LOCAL_SUPPORT`; it does not carry active-support
semantics. The actual lift reaches `ACTUAL_ACTIVE_SUPPORT` only after
also consuming the independent active-support and
canonical-minimality certificates.

The current result is

```text
Verdict = NOT_TESTABLE
FirstMissing = H1.source_backed_local_occurrence_edge_family
```

The integration uses:

- a six-axis quantifier signature;
- disjoint architecture-route and arithmetic-subroute registries;
- an H9 arithmetic-ancestor firewall;
- Endpoint Ledger V4, where metric fixed powers cannot be charged as
  named-fixed-atom powers.

The four H9 leaves are data registries. Their `decay_axis` is `NONE`:
the weight and normalization leaves carry literal-data axes, the phase
leaf carries named-phase data, and the endpoint leaf carries
deterministic-endpoint data. A registry never manufactures a decay
exponent.

Reproduce after TPC-163--170 have been generated:

```powershell
python experiments/tpc171_source_locked_integration.py
python experiments/tpc171_source_locked_integration.py --check
pdflatex -interaction=nonstopmode -halt-on-error main.tex
bibtex main
pdflatex -interaction=nonstopmode -halt-on-error main.tex
pdflatex -interaction=nonstopmode -halt-on-error main.tex
```

Hashes are integrity locks only. No positive program-L2, strict
`1/400`, prime-pair lower bound, or twin-prime theorem is asserted.
