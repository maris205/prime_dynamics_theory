# TPC-332 route evaluation

## Local fallback

The producer, independent reverse replay, stress suite, exact anchor, and
Bridge-B wrapper jointly certify the declared finite object.  The wrapper is
fail-closed and requires normal/optimized byte equality and empty stderr.

## Official-route boundary

The Session files `propose.md`, `skills/route-a-evaluator.md`, and
`skills/route-b-evaluator.md` are absent from this checkout.  Accordingly:

```text
Route-A = NOT_EVALUATED_OFFICIAL
Route-B = LOCAL_FAIL_CLOSED_FALLBACK
ARITHMETIC_ADVANCE = NO
FIXED_POWER_CREDIT = 0
FULL_GATE_B = OPEN
```

The finite positive census is a structural diagnostic and does not pay an
arithmetic gate.
