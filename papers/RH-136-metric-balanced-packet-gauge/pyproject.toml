[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "metric-balanced-packet-gauge"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["matplotlib", "mpmath", "numpy"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
