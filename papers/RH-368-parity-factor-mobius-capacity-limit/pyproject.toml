[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rh368-parity-factor-capacity"
version = "0.1.0"
description = "Exact parity-factor Mobius capacity checks"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
