# Roadmap after RH-235

The viable object is now a cloud-extracted trace jet.  RH-236 computes orders
one through twelve at every endpoint; RH-237 compares those jets between the
fine and Haar-coarse channels.
