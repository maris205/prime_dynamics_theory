# RH-109 theorem ledger

## Exact results

- Finite-memory spectral exterior certificate
  `q4 >= prod_j<=4 (shat_j-delta)_+ / (shat_1+delta)^4`.
- Trace exterior certificate with the exact
  `binom(r,4)^(-1/2)` dimension factor.
- Reduced-moment distinction:
  `lambda_max(wedge^4(K*K))` gives spectral four-volume squared, while
  `e4(K*K)=tr(wedge^4(K*K))` gives Frobenius four-volume squared.
- Rank-four specialization to `det(K*K)`.
- Sharp scalar-volume interval `nu4 <= q4 <= nu4^(1/3)`.
- Exact trace-one source-seeded normalized-memory families attaining both
  interval endpoints at the same scalar volume and fixed diagonal blocks.

## Finite validation

- Five RH-94 scales, two channels, three thresholds.
- 360 threshold-update records and 234 fine threshold-update records.
- Fine spectral counts: `78/78`, `72/78`, `55/78` at
  `1e-8`, `1e-6`, `1e-4`.
- Fine trace-`e4` counts: `78/78`, `65/78`, `42/78`.
- Minimum fine spectral lower bound `5.1091096e-8`.
- Minimum fine trace lower bound `1.3191664e-8`.
- Minimum fine actual normalized volume `5.1119586e-8`.
- Minimum fine volume-loss factor `5.2335553e-5`.
- Zero certificate-implication, exterior-ordering, selector-equivalence, or
  observed tail-bound failures.

## Open boundaries

- An all-level physical lower bound for the spectral exterior volume.
- A physical upper law for `(s2/s1)(s3/s1)` or a direct fourth-mode law.
- Exterior-trace concentration beyond the generic binomial factor.
- Unconditional eventual fine-support separation and Stage A closure.
- Hilbert--Polya, zero identification, and RH.
