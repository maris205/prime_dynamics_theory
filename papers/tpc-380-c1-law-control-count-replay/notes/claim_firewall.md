# TPC-380 claim firewall

```text
PROVED_EXACT_FINITE = grid/origin protocol, endpoint disjointness,
                      common geometry, four fixed laws, eight-block c=1 mask,
                      and finite Rayleigh decomposition
NUMERICALLY_CERTIFIED = complete 36-row count-2048 replay and its profiles
NUMERICAL_OBSERVATION = all-plus high-Q persistence versus signed controls
MODELING_CHOICE_OPEN = square-energy normalization as an arithmetic source
OPEN = law/origin/scale uniformity, causality, growing bound, source L2,
       Route-B reassembly
NO = arithmetic advance and fixed-power credit
NONE = twin-prime conclusion
```

The signed controls are diagnostic finite laws.  Their sub-cap values cannot
be interpreted as a source-valid arithmetic estimate.  The official
Session-named evaluators are absent; the local Bridge-B is fail-closed
repository evidence.
