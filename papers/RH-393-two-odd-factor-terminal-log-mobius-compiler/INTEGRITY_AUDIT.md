# RH-393 research-integrity audit

## Source integrity

- RH-392 is the direct frozen predecessor. Its equation (19) supplies the
  fixed-periodic one-form cancellation and its Theorem 2.2 supplies the fixed
  nonparallel two-form terminal cancellation.
- Tao's Theorem 2, equation (3), is inherited analytic provenance through
  RH-392. Johnston--Yang and Maynard are closure-only in RH-393.
- Davenport is inherited one-form provenance; Mirsky is historical context
  only. The multishift phase formula and landscape are proved locally.
- TPC-137 is a determinant-two blueprint and is not promoted to an
  arbitrary-determinant theorem.
- No remote PDF or source archive is vendored. Five external payload hashes
  are excluded from publication members and the complete RH-393 tree.

## Mathematical integrity

- All dimensions, periods, shifts, coefficients, masks, and clocks are fixed
  before `X->infinity`.
- The local density first deduplicates shift residues modulo `p^2`; the
  modulo-`p` multiplicity is then counted without collapse.
- Odd-support zero uses finite CRT and terminal Abel. Odd-support one and two
  use separate frozen RH-392 inputs after a fixed finite Boolean mask.
- The square-mask tail is uniform in `X,P` for fixed shifts; the order is
  fixed `P`, terminal limit, then `P->infinity`.
- Pairwise-distinct shifts pay the nonzero determinant for every two-odd
  monomial. Three-odd monomials remain outside the theorem.
- Landscape lower equality, zero iff prime-square cover, nonattained upper
  supremum, and phase/global distinction are separately proved.
- The 192-table result requires every phase table to satisfy `c11=0`; the
  other 320 tables receive no convergence or nonzero-limit claim.

## Artifact integrity

- The certificate has 576 exact rows and explicitly declares finite
  reproduction, not analytic proof.
- False verification is builder-independent; 32 named semantic mutations are
  rejected.
- Strict JSON rejects duplicates and nonfinite values and preserves exact
  Boolean/integer types.
- The Draft 2020-12 schema is recursively closed; the official validator
  accepts the stored result with zero errors.
- Result, schema, manifest, and report rebuild exactly under normal and
  optimized Python.

## Closure and archive integrity

The closure is exactly 117 release-bound Git objects in groups `106+8+3`,
plus three ordered remote locks, for 120 logical inputs. The fixed publication
has 38 members and the release-stage set has 40 files. Frozen hashes, source
identity, rights, zero-request replay, semantic-PDF identity, payload
exclusion, regular-file/path/type checks, and tree/EOF hygiene are executable
gates.

## Declarations and verdict

The manuscript supplies data, ethics, contributions, conflict, funding, and
AI-use declarations. Independent theorem and source reviews returned zero
blockers and zero minors. Verdict: accept within the stated fixed-data scope.
