# TPC-337 route evaluation

The repository fallback consists of the parent-locked producer, independent
reverse-shell replay, exact finite proof package, mutation stress, PDF audit,
and local fail-closed Bridge-B wrapper.

The Session-named files `propose.md`, `skills/route-a-evaluator.md`, and
`skills/route-b-evaluator.md` are absent from this checkout:

```text
Route-A = NOT_EVALUATED_OFFICIAL
Route-B = LOCAL_FAIL_CLOSED_FALLBACK
ARITHMETIC_ADVANCE = NO
FIXED_POWER_CREDIT = 0
FULL_GATE_B = OPEN
```

The strongest positive result is the exact covariance-Gram structure and its
six-row replay.  The strongest obstruction is that centered orbit variation,
rather than the coherent average, carries most of the full response.
