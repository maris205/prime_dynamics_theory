# Source Lock

TPC-211 uses no external asymptotic theorem.  Its source-locked local inputs
are:

* `research/tpc-big-road/bridge_b_transition_native_euler_bdh_compiler.md`
  (V46, sections 1--4): the literal (F_p), (G_{p,z}), (P_d), (B_{d,z}),
  and (Delta_{d,z}) definitions, together with the proper-factor congruence;
* `research/tpc-big-road/bridge_b_proper_factor_poisson_transference.md`
  (V43, sections 1--4): the product-frozen coefficient
  (-mu(d)log d/log u) in the transition branch;
* `papers/tpc-210-poisson-profile-realizability/`: the preceding scoped
  profile-class obstruction and its physical-coupling firewall;
* `TPC_HANDOFF.md`: current route position and claim ceiling.

The new theorem is finite algebra on these definitions.  The CRT lift is only
a common bookkeeping space for profiles modulo divisors of one squarefree
integer; it is not asserted to be the complete physical AP residual.  The
shared endpoint constructed by Gram duality is a finite surrogate, not the
actual sequence (Lambda(u+2)-b_x^{(z)}(u)).

No external citation, prime-distribution estimate, asymptotic variance law,
Kloosterman theorem, or twin-prime implication is used as evidence for this
paper's positive claims.
