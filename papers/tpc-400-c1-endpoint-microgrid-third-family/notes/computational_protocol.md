# TPC-400 computational protocol

The producer accumulates the prime shell in ascending order. The independent
checker accumulates it in descending order and independently forms the same
four finite matrix probes. Both use float64 matrices and one
BLAS/OpenMP thread per process. Before any current response is summarized,
the TPC-399 producer and canonical certificate are checked against their
normalized-LF SHA-256 locks.

The current family is `a_j=7600001+401j`, with selected indices
`(0,8,16,24,32,40)`, three calibration origins, and three holdout origins.
The complete panel is 6 origins × 4 laws × 4 normalizations = 96 rows. The
parent comparison uses the direct same-law TPC-399 all-origin mean; it is
frozen before current readout and no segment fit is performed. The current
disjointness check explicitly includes all prior declared endpoint panels.
Reverse-order numeric replay uses the declared `8e-8` relative tolerance.

The rational anchor checks the interpolation identity over exact fractions.
Normal and optimized producer, checker, stress, and Bridge-B executions must
agree at their declared output interfaces.
