# Roadmap after RH-124

Normalization exponents are now exact.  The combined directional theorem
should retain common information instead of independently degrading every
factor.  Then develop a direct-margin recurrence as a fallback and an
outward transport theorem for independently rounded data.

