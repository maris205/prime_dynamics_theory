# Route evaluation

Strongest positive result: the literal V59 adjoint Haar scalar is exactly one
`B_Q`-weighted beta midpoint plus explicit input-unit, hard-window, and
child-jump corrections; the complete-lattice centered alias is zero.

Strongest obstruction: complete-lattice Poisson removes only the centered
alias.  Deleting the physical diagonal returns at shell scale `B_Q`, and no
TPC-255 input estimates that beta midpoint collectively with the two boundary
lanes.

Open theorem: estimate the signed sum of all surviving lanes on the same V59
clock without triangulating the prime shell or either unit correction.

Reusable structure:

```text
literal adjoint test
-> complete unit-centered lattice
-> Poisson zero
-> deleted-diagonal return
-> outer hard-window leakage
-> inner child-jump leakage
-> one literal beta linear form
```

`ROUND2_CLUE`: attack the `B_Q`-weighted literal beta rank midpoint together
with the two boundary corrections; do not count the complete-lattice zero as
a Gate payment.
