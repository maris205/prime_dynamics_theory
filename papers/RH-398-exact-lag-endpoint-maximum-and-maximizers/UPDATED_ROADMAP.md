# RH-398 updated roadmap

## Closed route

RH-398 closes the fixed-lag extremum route opened by RH-396:

1. RH-396 supplies the exact fixed-lag endpoint and strict finite-clock
   nonattainment.
2. The run second difference telescopes to the finite alternating `A_m`
   formula cut off at `p0^2`.
3. A path-residue deletion has the exact four-branch loss `Lambda_T(L)`.
4. Prime-square collision levels inherit a weak local order with named strict
   even-run branches.
5. A common finite-prime support compares every lag with `h=1`; a common
   cofinal passage transfers the comparison to the analytic endpoints.
6. Positive-density exact-run cylinders pay every strict local loss.
7. The exact equality set is squarefree and coprime to `210`.
8. Prime squares at primes at least `11` approach the maximum from the
   complement without attaining it.
9. The `p0>=5` branch has a uniform quantitative gap.
10. Combining the lag maximum with RH-396 gives the exact joint finite-clock
    supremum and nonattainment.

## New theorem edge

```text
RH-396 fixed-lag Euler-run endpoint
                 |
                 v
finite alternating A_m telescope
                 |
                 v
path deletion Lambda_T(L)
                 |
                 v
one-prime collision-level transfer
                 |
                 v
exact maximizer set and strict cylinders
                 |
                 v
complement, quantitative gap, and joint endpoint.
```

The route changes the scalar endpoint landscape, not the underlying terminal
clock theorem.  RH-397 remains a different overlap-geometry result and is not
used analytically here.

## Admissible next work

These are questions, not RH-398 conclusions:

- audit the post-RH-398 breadth of the fixed-data terminal-log program before
  selecting any successor;
- seek other exact scalar endpoint landscapes only where a frozen analytic
  interface and a complete local-transfer proof both exist;
- investigate effective rates only if one source supplies a common uniform
  theorem for all moving parameters;
- revisit causal rules under their own information constraint;
- revisit larger windows only when every required correlation channel has an
  analytic source.

No next paper number follows from this list.  A successor requires a fresh
source lock, an exact theorem contract, and an independent proof audit.

## Permanent stops

Do not infer growing or adaptive `h`, `q`, or tables; an `h(X)` or `q(X)`
theorem; a uniform rate; ordinary Cesaro convergence; a prelimit maximum;
causality; monotonicity in `h`; an analytic theorem from finite certificate
rows; operator or trace identities; zeta-zero identification; RH; or any
upgrade of Gates A--E.
