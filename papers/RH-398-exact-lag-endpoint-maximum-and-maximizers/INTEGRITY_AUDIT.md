# RH-398 research-integrity audit

## Source integrity

- RH-396 commit `cd57086fa90939d56656c3f952a08ffad9aabefe` is the sole
  load-bearing theorem and analytic endpoint source.
- Its exact locator is definitions (18)--(21), Theorem 1.3 equation (22), and
  Corollary 1.4 equation (23), PDF page 3.
- RH-397 commit `dd63a109dcfa72365c749e0b183820d2611af733` is the direct
  release and provenance predecessor only; it supplies no analytic theorem
  to RH-398.
- RH-394, RH-392, RH-395, and RH-375 are transitive provenance or comparison
  objects only.
- The two bibliography entries resolve to immutable repository papers and
  are both used.  No fabricated or unbound citation key enters the claim
  chain.
- Four exact local remote-lock records are included, while all six external
  payload identities are excluded from publication members and the complete
  paper tree.

## Mathematical integrity

- `h`, `q`, every phase table, and the admissible terminal clock are fixed
  before the terminal limit; all scalar endpoint comparisons come afterward.
- The score retains the center multiplier `mu(n)`, the centered shifts
  `(-h,0,+h)`, and the boundary extension `mu_0`.
- Safety is at distance `d=2h`; no separation-`h` overlap theorem from
  RH-397 is substituted.
- `t_p(d)` uses `gcd(d,p^2)`, and `A_m` uses `min(m,t_p)`.  The second
  difference has signs `+,-2,+` and right index `ell+2`.
- The alternating telescope starts at `m=1` with positive sign and ends at
  `p0^2-1`.
- Every prime comparison is made on a common finite support before one common
  cofinal passage.  Separate limits are not spliced.
- The four parity branches of `Lambda_T(L)` retain their `max`, `min`, and
  zero edge cases.
- Odd run lengths are equality branches.  Strictness is asserted only at
  named even lengths and only after a positive-density exact-run cylinder is
  proved.
- The prime-`2` branch is `2|h` iff `4|d`; it is not mislabeled as an
  ordinary square divisor of `h`.
- The maximizer condition is the conjunction
  `mu^2(h)=1 and gcd(h,210)=1`.
- The complement uses a sequence of separately fixed lags `h=p^2`; no
  growing-lag terminal law is claimed.
- The quantitative gap keeps the full factors `1/2`, `1/25`, `1/49`, tail
  greater than `3/5`, and local loss `1/9`.

## Artifact integrity

- The 72-row certificate explicitly declares finite reproduction, not
  analytic proof.
- Builder-independent checks reject 66/66 core, 44/44 result, and 32/32
  schema mutations under normal and optimized Python.
- Strict JSON rejects duplicate keys, nonfinite values, open nested shapes,
  tuple substitutions, and Python booleans passed as integers.
- The recursively closed Draft 2020-12 schema has exact typed-node counts
  `800,209,574,1000,2185,0`, totaling 4,768.
- Result and schema builders preserve canonical, ordered pretty-byte, and
  semantic equality seals.
- The source closure has exactly 184 Git rows and four remote rows; source
  roles, rights, and zero-request offline behavior are executable contracts.

## AI-research failure-mode boundary

This is a theoretical paper with no empirical experiment claim.  Numerical
fixtures are exact integer or rational reproductions of finite identities,
not reported experiments.  Implementation bugs, fabricated results, and
method-description drift are countered by independent builders, saved exact
artifacts, normal and optimized replay, exhaustive finite grids, and named
mutation attacks.  A surprising finite fixture is never reframed as analytic
evidence.  The manuscript's contribution is bounded by the frozen source
closure rather than presented as a global novelty claim.

The release declares data/code availability, author contributions, AI-tool
use, competing interests, funding, ethics, limitations, and scope.  The
executable release can reproduce deterministic objects, but no deterministic
replay guarantee is made for AI-assisted prose generation.

An independent adversarial proof review and an independent
source/citation/PDF review of the identical frozen quartet each reported zero
blockers and zero minor findings.  The final archive reproduces the stored
and fresh objects, enforces 41 publication members and 43 release-stage
files, and reports `failure_count=0`.

Integrity verdict for the theorem and final release: pass within the
fixed-lag, fixed-clock, centered, noncausal scope; Gates A--E remain false.
