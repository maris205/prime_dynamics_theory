# TPC-305 claim firewall

## PROVED_EXACT_FINITE

- the overlap alignment and full-shell transported-target protocol;
- binary/sign and common-prefix feasibility statements;
- within a fixed operator row, native versus transported targets are separate
  constrained quadratic programs with the same operator and source Gram;
- the four-way orientation rule once the ratio statuses are fixed.

## NUMERICALLY_CERTIFIED_FINITE

- 18 target-swap cases and 36 operator budget tables;
- three-normalizer ordered ratio enclosures and invariant classifications;
- at `Q=60->70`, right-label-cheaper in 5/6 cases and home-operator-favored in
  1/6;
- all 3/3 inherited same-prefix cases are right-label-cheaper;
- outer-pair orientation counts `(left, cross)=(4,2)`, middle `(right,home)=(5,1)`,
  and final `(left,cross,home)=(3,1,2)`.

## MODELING_CHOICE

The fixed source spine, native off-overlap extension, global alignment, common
prefix `max` rule, three normalizers, and descriptive orientation labels.

## OPEN / NONE

The two physical operators are not held equal across shells, so the result is
only a partial counterfactual control.  The operator interaction term, a
uniform asymptotic budget theorem, arithmetic `L2`, fixed-power credit, full
Gate B, and a twin-prime conclusion remain open or none.
