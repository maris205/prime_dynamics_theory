# Roadmap after RH-317

Exact finite spectral realizability has a sharp logarithmic degree clock:
`N=(log M)/log(q_star/q)+O(1)`.  RH-318 combines that clock with the endpoint
logarithm to compute the optimal spectral `H2` approximation constant.
