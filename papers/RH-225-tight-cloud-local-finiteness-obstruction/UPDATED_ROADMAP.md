# Roadmap after RH-225

The route must distinguish operator resonances from determinant zeros. RH-226
restores the finite identity `det(I-zA)=product(1-z lambda)` and reconnects
the cloud to RH-7's fixed-noise Hilbert--Schmidt determinant.

```text
direct resonance zeros [ruled out]
reciprocal Fredholm zeros [next]
small-noise local finiteness [open]
```
