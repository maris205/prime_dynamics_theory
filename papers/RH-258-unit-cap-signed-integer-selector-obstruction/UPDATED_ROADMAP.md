# Roadmap after RH-258

The smallest monodromy-legal signed lattice does not reach the deterministic
anchor.  Larger caps rapidly become expensive and, even if they fit, would
still require an operator realization.  The higher-value next step is the
orthogonal quotient block: audit contractive powers on every tractable
expanded endpoint and isolate whether the finite block constant improves or
deteriorates.

Route coordinate:

```text
unit_cap_integer_selector_obstructed_open_quotient_block_uniformity
```

Larger integer caps remain a secondary route.  Gates A--E remain false/open.
