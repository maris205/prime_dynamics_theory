# TPC-385 route evaluation

The repository-local Bridge-B checker is intended to lock the producer,
independent reverse-shell replay, adversarial mutation suite, exact anchor,
documentation, and PDF identity in normal and optimized Python modes.

The Session-named Route-A and Route-B evaluator files are not present in this
checkout. No official evaluator pass is therefore claimed. The route state is:

```text
ROUTE_A = OPEN
ROUTE_B = OPEN
ARITHMETIC_ADVANCE = NO
FIXED_POWER_CREDIT = 0
L2 = NONE
STRICT_1_OVER_400 = UNPAID
TWIN_PRIME_RESULT = NONE
```

The only new route information is finite, calibration-blind transfer of the
all-plus high-bandwidth phase to two coordinate-disjoint holdout origins. The
signed-law obstruction and every arithmetic gate remain open.
