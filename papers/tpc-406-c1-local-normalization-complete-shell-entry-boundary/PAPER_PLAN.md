# Paper plan

## Question

Does the TPC-405 one-entry scale bound survive when the entire declared prime
shell, rather than a small selected prefix, is present?

## Exact theorem

For the complete shell `Q<p<=2Q` under the explicit alternating CRT profile,
the same adjacent normalized proxy entry satisfies
`z<=t_1/(a_min sqrt(S_0S_1))<=4/(a_min H)<=4/H`.

## Certificate

Use exact rational arithmetic at `Q=8192`, all 872 shell primes, and
`H=16,32,66,128,256` with `N=4H`.  Reconstruct the complete CRT origin and
replay both masked row energies independently.

## Boundary

The result remains one synthetic adjacent entry.  It does not establish a
complete normalized matrix/operator estimate, arbitrary origins, physical
source signs, arithmetic `L2`, a growing theorem, or any twin-prime result.
