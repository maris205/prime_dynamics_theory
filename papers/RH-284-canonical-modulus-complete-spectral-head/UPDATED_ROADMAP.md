# Roadmap after RH-284

The noisy spectral head is now canonical once a modulus threshold is fixed.
The next analytic step is to upgrade the high-order trace estimate to uniform
control of the regularized determinant and its derivatives.  A separate
head/block tradeoff still determines which threshold is efficient.
