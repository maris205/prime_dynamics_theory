# RH-397 research-integrity audit

## Source integrity

- RH-394 commit `6b3d616851cd2d7cba66371d0aa9f25b8e8bf2f7` is the sole
  analytic terminal-law source, inherited through RH-396.
- RH-396 commit `cd57086fa90939d56656c3f952a08ffad9aabefe` is the direct
  collision-aware density, projection, reflection, and finite-optimizer
  predecessor.
- RH-392, RH-395, and RH-375 are transitive comparison objects only; none is
  promoted to a new analytic input.
- Tao 2016 and Tao--Teravainen 2019 remain inherited provenance through
  RH-394. Johnston--Yang and Maynard remain closure-only objects.
- Both bibliography entries are cited, and every citation key resolves.
- Four exact local lock records are included, while all six external payload
  identities are excluded from the publication members and entire paper tree.

## Mathematical integrity

- `h`, `q`, every phase table, and the terminal clock are fixed before the
  limit; the fixed-table limit precedes the finite safe maximum.
- The score retains the center multiplier `mu(n)` and the boundary extension
  `mu_0(n-h)`.
- Universal safety has separation `h` and shares both center/right symbols;
  neither a separation-`2h` rule nor a one-symbol overlap is substituted.
- The fourth finite safety symbol is not treated as a fourth analytic shift.
- `Theta`, `Pi`, `kappa2`, and `kappa3` deduplicate collisions modulo prime
  squares before evaluating local counts.
- Positive projection and reflection have the correct score signs.
- Exact relation enumeration gives 512 relations, 262144 ordered pairs,
  61440 safe pairs, class counts `16,48,48,400`, and rectangle sizes
  `4,6,6,9`.
- `M,U,V,W` retain their factors `1,1/2,1/2,1/4`; translation is by `h`.
- Edge filling uses the proved nonnegative gain.  The final optimizer is
  weighted, includes self-loop behavior, and is surjective.
- The odd-lag theorem classifies declared-clock parity.  It neither requires
  a minimal period nor asserts an even-lag analogue.

## Artifact integrity

- The 72-row certificate explicitly declares finite reproduction, not
  analytic proof.
- Builder-independent checks reject 60/60 core, 78/78 result, and 32/32
  schema mutations under normal and optimized Python.
- Strict JSON rejects duplicate keys, nonfinite values, and nested exact-type
  substitutions; Python booleans cannot pass as integers.
- The recursively closed Draft 2020-12 schema has 568 object nodes, 140 array
  nodes, and 3484 total nodes.  The official `jsonschema` validator accepts
  the stored result with zero errors.
- Result and schema builders preserve canonical, ordered pretty-byte, and
  semantic equality seals.

## Release integrity

The source closure contains exactly 172 Git inputs in groups `160+8+4` and
four ordered remote locks, for 176 logical inputs.  The archive binds the
group and logical digests, five source commits, frozen Stage1 and manuscript
hashes, offline rights, semantic-PDF equality, exact result/schema replay,
official schema validation, payload exclusion, and whole-tree hygiene.

The publication set has 41 members and the release-stage set 43 files.  Path
traversal, invalid hash types, symlinks, special files, caches, bytecode,
editor temporaries, carriage returns, EOF defects, unlisted files, sentinels,
and payload insertion fail closed.  Release tests contain no bare `assert`.

## Declarations and verdict

The manuscript states data/code availability, author contributions, AI-tool
use, competing interests, funding, ethics, limitations, and scope.  The
independent theorem and source/citation/PDF audits of the same frozen quartet
each reported zero blockers and zero minor findings.  Verdict: accept within
the fixed-lag, fixed-clock, centered, noncausal scope; Gates A--E remain
false.
