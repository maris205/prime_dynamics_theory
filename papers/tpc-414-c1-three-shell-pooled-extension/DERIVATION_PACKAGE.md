# TPC-414 derivation package

The third complete shell is disjoint from the first two and all its primes
remain larger than `N=264`.  Pooling therefore preserves the masked local
identities.  The total count `36848` is even, restoring equal parity classes;
the shell-local amplitude normalization still satisfies `a_i>1`, so the same
Cauchy--Schwarz proof yields `z<=4/H`.
