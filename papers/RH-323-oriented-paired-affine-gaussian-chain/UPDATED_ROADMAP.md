# Roadmap after RH-323

RH-323 identifies the exact oriented tangent probability chain seeded by the
RH-322 endpoint row.  Retaining the source coordinate makes the Markov lift
an exact `L1` isometry; marginalization gives contraction only.  The output
law is an extended-skew-normal mixture rather than one Gaussian, and the
clearance phase remains visible in all moments.

RH-324 should prove a finite-noise remainder theorem for one physical
critical leg.  It must control, on a common weighted domain:

1. the quadratic Taylor displacement;
2. wrong-fold and state-boundary Gaussian tails;
3. the exact row-normalization error;
4. propagation of the RH-322 clearance phase;
5. a quantitative physical-kernel versus affine-kernel norm bound.

RH-324 must not import the RH-18 peak-normalized packet coefficient as a
probability mass factor.  RH-325 remains responsible for moving-order
Duhamel composition.  Parity, sibling/shell coupling, and their same-order
cancellation remain assigned to RH-326--RH-328.  No joint first-alias
reopening theorem has been supplied, and Gates A--E remain false/open.
