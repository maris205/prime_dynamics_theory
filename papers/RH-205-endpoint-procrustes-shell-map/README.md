# RH-205: endpoint Procrustes shell map

The polar factor of the packet overlap gives the unique optimal endpoint
alignment and an exact partial isometry between equal-rank packet spaces.
Physical costs remain `0.44075`--`0.70099`, and the map uses the fine
endpoint, so it is retrospective rather than predictive.
