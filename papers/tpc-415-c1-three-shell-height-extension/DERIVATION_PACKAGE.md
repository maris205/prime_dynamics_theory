# TPC-415 derivation package

The three complete shells are disjoint and every retained prime is larger than
the largest window `N=512`.  Thus pooling preserves the local mask identities
at all four heights.  The even pooled count gives equal parity classes, while
shell-local positivity gives `a_i>1`; the same Cauchy--Schwarz cancellation
therefore yields the uniform finite bound `z<=4/H`.
