# TPC-415 theorem ledger

- Shells: `Q=65536` (5709), `131072` (10749), `262144` (20390); total 36848.
- Heights `H=16,32,66,128`, with `N=4H`; parity counts 18424/18424.
- Shell-local `a_i=p_i^3/[Q_i^2(p_i-1)]` is used for every prime.
- Exact finite conclusion at every height: `0<=z<=t1/(a_min sqrt(S0 S1))<=4/H`.
- Full operator, physical, arithmetic, fixed-power, Route-B, and twin-prime
  claims remain unpaid.
