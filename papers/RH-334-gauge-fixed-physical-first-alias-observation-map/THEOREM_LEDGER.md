# Theorem ledger

| Claim | Status |
|---|---|
| Signed domain `I=[-1,1]`, folded domain `[0,1]`, `T=abs(f)` | Frozen physical data type |
| Backward-observable convention; `M_J` marks the source/basepoint | Frozen |
| `K_signed` and `K_fold` Hilbert--Schmidt; powers `n>=2` trace class | Imported and checked from RH-10/RH-19 |
| Localized noisy folding identity for measurable `J`, `n>=2` | Proved exactly |
| `Fix(f^n) -> Fix(T^n)` by absolute value for `n>=1` | Proved bijective |
| Signed/folded periodic multiplier and flat-trace weight preservation | Proved |
| Critical cusp or `0,1` creates a periodic multiplicity | False; all are preperiodic, while `r` is smooth and simple |
| Correct deterministic localization `P_n^abs(J)` | Proved |
| RH-327 positive-`x` localized sum | False at `n=2`; explicit missed negative point |
| Exact missed `n=2` weight `1/(4*u_c-3)` | Proved |
| Fixture memberships and fixed-point-free gauge interval | Proved from the rational bracket `3/2<u_c<25/16`, not decimal sampling |
| RH-327 boundary-owned `J_minus=[...,b)`, `J_plus=[b,...]`, and complement frozen before evaluation | Required and implemented |
| Corrected raw packet `T=B+S+R` | Proved exactly for `n>=2` |
| First-alias type `coefficient_type=hardy_full_trace_constituent` | Locked |
| `q_FT=e=B+S+R+P-A` at `n=2k` | Proved exactly |
| `q_FT` equals the modulus complement without a head discrepancy theorem | False; `tau-a=q_FT-d`, `d=h-s` |
| Localization of Perron/parity projectors or Floquet sectors | Not performed |
| Strict window-partition dependence under a positive-measure reassignment | Proved |
| Window-partition dependence equals RH-330 exchange/observation gauge | False; the two transformations act on different objects |
| 32-point Nyström rows | Finite reproduction only; not continuum certificates |
| Slot sign, magnitude, or moving-order asymptotic | Open |
| Far remainder `o(H_k)` | Open |
| Forward probability law identified with cyclic trace | False/open |
| Physical exchange parameter or actual/model replacement | Open |
| Duhamel, off-alias, head, or determinant closure | Open |

The exact correction is finite and local to the physical observation map.
Gates A--E remain false/open.
