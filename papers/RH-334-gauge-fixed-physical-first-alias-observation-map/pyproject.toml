[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rh334-physical-observation"
version = "0.1.0"
dependencies = ["mpmath>=1.3,<2"]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]
