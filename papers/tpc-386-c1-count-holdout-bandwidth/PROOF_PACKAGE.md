# TPC-386 proof package

## PROVED_EXACT_FINITE

- The candidate grid, selected indices, count split, band modes, laws, and
  normalization menu are fixed before response readout.
- The five current intervals are pairwise disjoint and disjoint from the
  listed prior panels.
- The 13-point rational anchor has positive geometry and symmetric matrices
  for every declared law.
- The JSON certificate is canonical and its payload and parent files are
  hash-locked.

## NUMERICALLY_CERTIFIED_FINITE

- 160 rows and 32 complete cells are reproduced in both interpreter modes.
- All 16 spectral-cap failures are independently localized to all-plus
  `N=1024` rows; no Schur-cap failure occurs.
- The four all-plus `Q=8192` forecast ratios and all cell summaries agree with
  the reverse-order replay.
- Twenty-five structural mutations are rejected.

## REFUTED_FINITE_SCOPED

The inherited finite `0.64` spectral diagnostic is not count-uniform across
this panel: it fails for every all-plus holdout row under both masks and both
normalizations.

## OPEN

Any assertion uniform in `N`, any source-native `L2` estimate, and any
arithmetic twin-prime conclusion remain open. The broad 20% ratio cap is an
audit convention, not a theorem.
