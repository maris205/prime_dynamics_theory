# RH-145: Delayed Start and Superunit Birth Isolation

Finite prefixes do not affect tail limsups, base liminfs, or eventual
directional support.  A delayed start is therefore legitimate once a reset
enters a future invariant tube.  In contrast, if the zero-state control floor
is at least one infinitely often, every policy has limsup at least one.

The finite audit finds exactly two superunit birth coefficients, both at the
same `sigma=0.08` left anchor.  Both have empty RH-144 kernels.  Starting at
`sigma=0.04` leaves a clean suffix of 18/18 outward-positive chains, with
minimum terminal floor above `1.5e-10`.  This isolates a finite obstruction;
it does not prove that future untested levels never contain another one.

