# TPC-293 route evaluation

The Session-named Route-A and Route-B evaluator files are absent from this
checkout, so no official Route-A/Route-B pass is claimed. The local
fail-closed fallback is the direct proof package, canonical exact-rational
certificate, independent physical replay, exhaustive signed-graph stress
test, and Bridge-B checker.

The result advances the structural Bridge-B map from triangle compatibility to
a shell-level signed graph. It finds no arithmetic advance and pays no
fixed-power credit. The one finite `+3` sign-only exception is retained as a
clue, while the magnitude-weighted objective, source image, Gate B, and the
twin-prime endpoint remain open.
