\documentclass[11pt]{article}

\usepackage[a4paper,margin=0.88in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{enumitem}
\usepackage[numbers,sort&compress]{natbib}
\usepackage{xcolor}
\usepackage{xurl}
\usepackage[colorlinks=true,allcolors=blue!55!black]{hyperref}
\usepackage[nameinlink,noabbrev]{cleveref}

\allowdisplaybreaks
\setlength{\parindent}{1.25em}
\setlength{\parskip}{0.15em}
\setlist{nosep}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{definition}[theorem]{Definition}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\DeclareMathOperator{\tr}{tr}
\DeclareMathOperator{\spec}{spec}
\newcommand{\R}{\mathbb R}
\newcommand{\cK}{\mathcal K}
\newcommand{\cP}{\mathcal P}
\newcommand{\cS}{\mathcal S}
\newcommand{\cU}{\mathcal U}
\newcommand{\cN}{\mathcal N}
\newcommand{\bu}{\boldsymbol u}
\newcommand{\bx}{\boldsymbol x}
\newcommand{\eps}{\varepsilon}
\newcommand{\one}{\mathbf 1}

\title{Fixed-Length Small-Noise Localization for Gaussian Quadratic Markov Cocycles\\
\large Directed Periodic-Orbit Traces and Delayed Six-Step Asymptotics}
\author{Liang Wang\\
\small School of Artificial Intelligence and Automation,\\[-0.2em]
\small Huazhong University of Science and Technology, Wuhan 430074, China}
\date{July 2026}

\begin{document}
\maketitle

\begin{abstract}
Let $f_u(x)=1-u x^2$ on $[-1,1]$ and let $\cK_{u,\sigma}$ be its
row-normalized truncated-Gaussian Markov smoothing.  Fixed positive noise
makes every finite product compact, whereas the zero-noise limit is a
singular deterministic composition.  We prove a fixed-length bridge between
the two.  For a parameter word $\bu=(u_1,\ldots,u_m)$, assume that every fixed
point of $F_{\bu}=f_{u_m}\circ\cdots\circ f_{u_1}$ is simple and that its
closed orbit stays in the interior.  Then, for $m\ge2$,
\[
 \tr(\cK_{u_1,\sigma}\cdots\cK_{u_m,\sigma})
 =\sum_{F_{\bu}(x)=x}\frac1{|1-F_{\bu}'(x)|}+O(\sigma^2).
\]
The proof is a parameter-uniform Laplace localization of the exact closed-path
integral.  Its residual Jacobian has determinant
$\pm(1-F_{\bu}')$, so Gaussian covariance factors cancel and leave the
periodic-orbit weight.  Parameter derivatives obey the same expansion, with
an explicit exponentially small but polynomially amplified remainder governed
by the action gap and boundary clearance.

Consequently, the directed three-step trace and the parity-compatible
six-step trace converge to ordered differences of deterministic periodic-orbit
sums.  At the quadratic band-merging parameter both limits are nonzero.  Dense
midpoint matrices through full dimension $15360$ recover quadratic convergence
for the three-step quantities.  Six-step traces enter the asymptotic regime
much later, reverse sign preasymptotically, and then return toward the predicted
nonzero orbit target.  The result is a fixed-cycle theorem; no uniform
long-cycle determinant limit is asserted.
\end{abstract}

\noindent\textbf{Keywords:}
small-noise asymptotics; Gaussian Markov operator; periodic-orbit trace;
nonautonomous cocycle; Laplace method; directed cycle curvature.

\noindent\textbf{MSC 2020:}
37C30; 37H10; 45C05; 47B10; 60J05.

\section{Introduction}

Noise regularizes a deterministic map and simultaneously hides the singular
nature of the zero-noise limit.  At every fixed $\sigma>0$, a smooth Gaussian
transition kernel defines a compact integral operator.  At $\sigma=0$, it
formally becomes the composition operator $g\mapsto g\circ f$, which is not a
compact perturbation of the noisy family in any useful operator norm.  Thus a
spectral statement proved at fixed noise cannot simply be evaluated at
$\sigma=0$.

Cycle traces are better behaved.  A product trace has an exact closed-path
integral whose phase is the squared defect from a deterministic orbit.  In the
weak-noise literature this observation leads to periodic-orbit expansions for
stochastic evolution operators
\citep{CvitanovicEtAl1998,PallaEtAl2001}; it is also consonant with the general
large-deviation geometry of random perturbations
\citep{Kifer1988,FreidlinWentzell2012,DemboZeitouni1998}.  The present problem
adds three features that require a separate treatment:
\begin{enumerate}[label=(\roman*),leftmargin=2.2em]
 \item the Gaussian is normalized after truncation to a compact interval;
 \item the parameter changes inside the cycle, so the relevant orbit is a
 nonautonomous composition; and
 \item the observable of interest is a difference between two parameter
 orderings, including a parity-compatible six-step word.
\end{enumerate}

Earlier fixed-noise work established continuum approximation, centered cycle
determinants, and the first directed scalar traces for this quadratic Markov
family \citep{WangContinuum2026,WangCycle2026,WangTimeOrdered2026}.  In
particular, reversing two factors cannot change their nonzero spectrum, while
three factors can carry a directed trace.  The missing bridge was whether that
trace is merely a smoothing artifact or has a deterministic periodic-orbit
limit.

\subsection*{Main results}

This paper supplies that bridge at every fixed cycle length.
\begin{enumerate}[label=\textbf{R\arabic*.},leftmargin=3.1em]
 \item The exact $m$-factor trace is a Laplace integral with action
 \[
  \cS_{\bu}(\bx)=\frac12\sum_{j=1}^{m}
  \bigl(x_j-f_{u_j}(x_{j-1})\bigr)^2,
  \qquad x_m=x_0.
 \]

 \item At a zero-action cycle, the residual Jacobian $J$ satisfies
 \[
  |\det J|=|1-F_{\bu}'(x_0)|.
 \]
 Hence the Gaussian prefactor cancels exactly and leaves the standard
 periodic-orbit weight.

 \item For a uniformly interior, nondegenerate family of words, the trace and
 every fixed finite number of parameter derivatives converge with a local
 $O(\sigma^2)$ expansion.  Away from the zero-action tubes, the explicit
 remainder has the form
 $\sigma^{-N_r}\exp(-\Gamma/\sigma^2)$.

 \item The directed three-step and parity-compatible six-step traces converge
 to deterministic order differences.  Their Vandermonde quotients therefore
 have well-defined deterministic diagonal curvatures whenever the admissible
 parameter neighborhood persists.

 \item Boundary contact and the degeneracy $F_{\bu}'=1$ are exact failure
 conditions for the stated formula.  A vanishing action gap as the cycle
 length grows also prevents this theorem from being promoted automatically to
 a full determinant limit.

 \item At the tested band-merging parameter, all three- and six-step cycles are
 interior and nondegenerate.  The deterministic directed targets are nonzero,
 and the finite-noise matrices approach them.  The six-step approach is highly
 nonmonotone because its boundary-action scale is much smaller.
\end{enumerate}

The scope is deliberately fixed-length.  The result identifies the exact
deterministic shadow of the previously constructed directed traces.  It does
not exchange the limits $m\to\infty$ and $\sigma\to0$, and it does not claim a
zero-noise limit for an entire Fredholm determinant.

\section{Normalized Gaussian words and their traces}
\label{sec:operators}

Let $I=[-1,1]$, let $0<u<2$, and put
\begin{align}
 f_u(x)&=1-u x^2,\label{eq:map}\\
 w_{u,\sigma}(x,y)&=
 \exp\left[-\frac{(y-f_u(x))^2}{2\sigma^2}\right],\\
 Z_{u,\sigma}(x)&=\int_Iw_{u,\sigma}(x,z)\,dz,\\
 q_{u,\sigma}(x,y)&=\frac{w_{u,\sigma}(x,y)}{Z_{u,\sigma}(x)}.
 \label{eq:kernel}
\end{align}
Because $f_u(I)=[1-u,1]\subset I$, truncation never excludes the Gaussian
mean, although the mean reaches the upper boundary at the critical point
$x=0$.  The observable Markov operator is
\begin{equation}
 (\cK_{u,\sigma}g)(x)=\int_Iq_{u,\sigma}(x,y)g(y)\,dy.
 \label{eq:operator}
\end{equation}
It is Hilbert--Schmidt on $H=L^2(I)$ and satisfies
$\cK_{u,\sigma}\one=\one$.  Products of at least two factors are trace class
\citep{Simon2005,Kress2014}.

Fix a word $\bu=(u_1,\ldots,u_m)$ and define its chronological composition
\begin{equation}
 F_{\bu}=f_{u_m}\circ\cdots\circ f_{u_1}.
 \label{eq:composition}
\end{equation}
The corresponding noisy cycle trace is
\begin{equation}
 T_\sigma(\bu)=
 \tr(\cK_{u_1,\sigma}\cdots\cK_{u_m,\sigma}),
 \qquad m\ge2.
 \label{eq:noisy-trace}
\end{equation}

\begin{lemma}[Exact closed-path integral]
\label{lem:path-integral}
With $x_m=x_0$,
\begin{equation}
 T_\sigma(\bu)=
 \int_{I^m}
 \frac{\exp[-\cS_{\bu}(\bx)/\sigma^2]}
 {\prod_{j=1}^m Z_{u_j,\sigma}(x_{j-1})}
 \,dx_0\cdots dx_{m-1},
 \label{eq:path-integral}
\end{equation}
where
\begin{equation}
 \cS_{\bu}(\bx)=\frac12\sum_{j=1}^{m}
 \left(x_j-f_{u_j}(x_{j-1})\right)^2.
 \label{eq:action}
\end{equation}
\end{lemma}

\begin{proof}
The product kernel is obtained by integrating over the $m-1$ intermediate
states.  Integrating its diagonal gives \eqref{eq:path-integral}; this diagonal
formula is valid because the product of two Hilbert--Schmidt factors is trace
class and its iterated kernel is continuous.  Multiplying the $m$ Gaussian
weights yields \eqref{eq:action}.
\end{proof}

The row normalizer has two different asymptotic regimes.  Uniformly on $I$ it
is of order $\sigma$.  Near a path whose next state stays away from the
boundary, it is the full Gaussian mass up to an exponentially small error.

\begin{lemma}[Normalizer bounds]
\label{lem:normalizer}
Fix a compact $U\Subset(0,2)$.  There are constants $c,C>0$ such that, for
$u\in U$, $x\in I$, and $0<\sigma\le1$,
\begin{equation}
 c\sigma\le Z_{u,\sigma}(x)\le C\sigma.
 \label{eq:normalizer-bounds}
\end{equation}
If $\operatorname{dist}(f_u(x),\partial I)\ge\delta>0$, then
\begin{equation}
 Z_{u,\sigma}(x)=\sqrt{2\pi}\,\sigma
 \left[1+O\left(e^{-\delta^2/(2\sigma^2)}\right)\right].
 \label{eq:normalizer-interior}
\end{equation}
The same assertion holds after any fixed finite number of $u$- and
$x$-derivatives, at the cost of a polynomial factor in $\sigma^{-1}$.
\end{lemma}

\begin{proof}
The Gaussian mean lies in $I$.  At a boundary mean, exactly one half of the
full Gaussian is retained up to the remote-tail correction; all interior means
retain more.  This proves \eqref{eq:normalizer-bounds}.  The standard Gaussian
tail estimate proves \eqref{eq:normalizer-interior}, and differentiation only
introduces Gaussian moments and powers of $\sigma^{-1}$.
\end{proof}

\section{Zero-action cycles and the residual determinant}
\label{sec:geometry}

Write
\begin{equation}
 R_j(\bx)=x_j-f_{u_j}(x_{j-1}),
 \qquad j=1,\ldots,m,\qquad x_m=x_0.
 \label{eq:residual}
\end{equation}
Then $\cS_{\bu}=\frac12|R|^2$.  Its zero set is elementary but decisive.

\begin{proposition}[Closed deterministic paths]
\label{prop:zero-set}
The following are equivalent:
\begin{enumerate}[label=(\roman*),leftmargin=2.2em]
 \item $\cS_{\bu}(\bx)=0$;
 \item $x_j=f_{u_j}(x_{j-1})$ for every $j$;
 \item $x_0$ is a fixed point of $F_{\bu}$ and all other coordinates are its
 ordered orbit.
\end{enumerate}
\end{proposition}

Let $J_{\bu}(\bx)=D_{\bx}R(\bx)$.  At a deterministic cycle, its nonzero
entries are
\begin{equation}
 (J_{\bu})_{j,j-1}=-f_{u_j}'(x_{j-1}),
 \qquad (J_{\bu})_{j,j}=1,
 \label{eq:jacobian-entries}
\end{equation}
with cyclic indices.  Relabeling rows and columns gives the equivalent
diagonal-plus-cyclic-shift matrix used in the numerical audit.

\begin{proposition}[Residual determinant identity]
\label{prop:determinant}
At a zero-action cycle based at $x_0$,
\begin{equation}
 \det J_{\bu}=(-1)^{m-1}\bigl(1-F_{\bu}'(x_0)\bigr),
 \qquad
 \det D^2\cS_{\bu}=\bigl(1-F_{\bu}'(x_0)\bigr)^2.
 \label{eq:determinant-identity}
\end{equation}
In particular, the minimum is nondegenerate exactly when
$F_{\bu}'(x_0)\ne1$.
\end{proposition}

\begin{proof}
In the determinant expansion of \eqref{eq:jacobian-entries}, only the identity
permutation and the full cyclic permutation contribute.  Their difference is
$1-\prod_{j=1}^m f_{u_j}'(x_{j-1})=1-F_{\bu}'(x_0)$, with the displayed
orientation sign.  Since $R=0$ at the cycle,
\begin{equation*}
 D^2\cS_{\bu}=J_{\bu}^{\mathsf T}J_{\bu},
\end{equation*}
which proves the second identity.
\end{proof}

\begin{definition}[Admissible word]
\label{def:admissible}
A parameter word $\bu$ is \emph{small-noise admissible} if
\begin{enumerate}[label=(\roman*),leftmargin=2.2em]
 \item every fixed point of $F_{\bu}$ in $I$ is simple in the trace sense,
 $F_{\bu}'(x)\ne1$; and
 \item every point of every associated closed orbit belongs to $(-1,1)$.
\end{enumerate}
It is \emph{uniformly admissible} on a compact parameter set if the boundary
clearance and $|1-F_{\bu}'|$ have positive uniform lower bounds.
\end{definition}

The second condition is not cosmetic.  If a zero-action cycle touches
$\partial I$, both the integration tangent cone and the truncated normalizer
enter the leading coefficient.  The simple full-space weight in
\cref{prop:determinant} then need not be the answer.

\section{The fixed-length localization theorem}
\label{sec:localization}

For an admissible word define the deterministic orbit sum
\begin{equation}
 \cP(\bu)=\sum_{F_{\bu}(x)=x}\frac1{|1-F_{\bu}'(x)|}.
 \label{eq:orbit-sum}
\end{equation}
This is a finite sum because $F_{\bu}(x)-x$ is a nonzero polynomial and all
its roots under consideration are simple.

\begin{theorem}[Fixed-length small-noise trace]
\label{thm:localization}
Let $m\ge2$ be fixed and let $\bu$ be admissible.  Then
\begin{equation}
 T_\sigma(\bu)=\cP(\bu)+O(\sigma^2),
 \qquad \sigma\downarrow0.
 \label{eq:main-localization}
\end{equation}
More precisely, the contribution of each closed deterministic orbit based at
$x$ is
\begin{equation}
 \frac1{|1-F_{\bu}'(x)|}\left[1+O(\sigma^2)\right],
 \label{eq:local-contribution}
\end{equation}
and the complement of fixed disjoint orbit neighborhoods is exponentially
small.
\end{theorem}

\begin{proof}
Choose disjoint neighborhoods of the finitely many zero-action cycles.  Their
closures remain in the interior by admissibility.  In one such neighborhood,
\cref{lem:normalizer} gives
\begin{equation*}
 \prod_{j=1}^m Z_{u_j,\sigma}(x_{j-1})
 =(\sqrt{2\pi}\,\sigma)^m[1+O(e^{-c/\sigma^2})].
\end{equation*}
The phase has a nondegenerate minimum there, with Hessian
$J^{\mathsf T}J$.  The multidimensional Laplace expansion
\citep{BleisteinHandelsman1986,Wong2001} therefore gives
\begin{align*}
 \int_{\cN_x}e^{-\cS_{\bu}(\bx)/\sigma^2}\,d\bx
 &=(2\pi\sigma^2)^{m/2}
 \frac{1+O(\sigma^2)}{\sqrt{\det(J^{\mathsf T}J)}}\\
 &=(\sqrt{2\pi}\,\sigma)^m
 \frac{1+O(\sigma^2)}{|1-F_{\bu}'(x)|},
\end{align*}
where the last equality is \cref{prop:determinant}.  The normalizer cancels
the Gaussian volume exactly at leading order.

On the complement of the orbit neighborhoods, compactness and
\cref{prop:zero-set} give $\cS_{\bu}\ge c>0$.  The uniform lower bound in
\eqref{eq:normalizer-bounds} makes that contribution at most a polynomial in
$\sigma^{-1}$ times $e^{-c/\sigma^2}$, hence smaller than every algebraic
power.  Summing the local contributions proves the theorem.
\end{proof}

The parameter-uniform form records why a formally asymptotic result can have a
late numerical onset.  Fix orbit tubes of radius $\rho$ and define the action
gap
\begin{equation}
 \Delta_\rho(\bu)=
 \inf\left\{\cS_{\bu}(\bx):
 \bx\in I^m\setminus\bigcup_x\cN_\rho(x)\right\}.
 \label{eq:action-gap}
\end{equation}
For an admissible word this number is positive when the tubes contain all
zero-action cycles.

\begin{theorem}[Uniform derivatives and delayed onset]
\label{thm:uniform}
Let $\cU$ be a compact uniformly admissible family of words of fixed length
$m$.  For every nonnegative integer $r$, there are constants
$C_r,\Gamma>0$ and a finite integer $N_r$ such that
\begin{equation}
 \|T_\sigma-\cP\|_{C^r(\cU)}
 \le C_r\sigma^2+C_r\sigma^{-N_r}e^{-\Gamma/\sigma^2}.
 \label{eq:uniform-bound}
\end{equation}
The exponent $N_r$ may be chosen to grow at most linearly with $r$ for the
quadratic family.
\end{theorem}

\begin{proof}
Uniform admissibility gives a finite parameter cover on which the fixed-point
branches, orbit tubes, Hessian inverses, and boundary clearances are uniformly
controlled.  The parameter-dependent Laplace expansion may therefore be
differentiated $r$ times.  Its local remainder is $O(\sigma^2)$ uniformly.
Outside the tubes, take $\Gamma$ smaller than both the uniform action gap and
the Gaussian boundary-tail exponent.  Every parameter derivative acts either
on a smooth coefficient or on an exponential divided by $\sigma^2$, producing
only finitely many additional powers of $\sigma^{-1}$.  This gives
\eqref{eq:uniform-bound}.
\end{proof}

\begin{remark}
For each fixed $r$, the exponential term in \eqref{eq:uniform-bound} is
eventually smaller than $\sigma^2$.  At a practical noise width, however, a
small $\Gamma$ and the factor $\sigma^{-N_r}$ can dominate.  Third-order
parameter responses are especially sensitive because three differentiations
can produce a factor of order $\sigma^{-6}$ before the exponential decay is
felt.
\end{remark}

\section{Directed three-block limits}
\label{sec:directed}

For a block length $\ell\ge1$, set
\begin{equation}
 A_{u,\sigma}^{(\ell)}=\cK_{u,\sigma}^{\ell}
\end{equation}
and define
\begin{equation}
 \Omega_{\ell,\sigma}(a,b,c)=
 \tr\left(A_{a,\sigma}^{(\ell)}A_{b,\sigma}^{(\ell)}A_{c,\sigma}^{(\ell)}\right)
 -\tr\left(A_{a,\sigma}^{(\ell)}A_{c,\sigma}^{(\ell)}A_{b,\sigma}^{(\ell)}\right).
 \label{eq:directed-noisy}
\end{equation}
The case $\ell=1$ is a three-step trace.  The case $\ell=2$ is the minimal
three-block trace that preserves a frozen two-step parity block, hence has six
one-step factors.

Let
\begin{align}
 \bu_{\ell}^{+}(a,b,c)
 &=(\underbrace{a,\ldots,a}_{\ell},
   \underbrace{b,\ldots,b}_{\ell},
   \underbrace{c,\ldots,c}_{\ell}),\\
 \bu_{\ell}^{-}(a,b,c)
 &=(\underbrace{a,\ldots,a}_{\ell},
   \underbrace{c,\ldots,c}_{\ell},
   \underbrace{b,\ldots,b}_{\ell})
\end{align}
and
\begin{equation}
 \Omega_{\ell,0}(a,b,c)=
 \cP(\bu_{\ell}^{+})-\cP(\bu_{\ell}^{-}).
 \label{eq:directed-deterministic}
\end{equation}

\begin{corollary}[Deterministic persistence of orientation]
\label{cor:directed-limit}
If both words in \eqref{eq:directed-deterministic} are admissible, then
\begin{equation}
 \Omega_{\ell,\sigma}(a,b,c)\longrightarrow
 \Omega_{\ell,0}(a,b,c)
 \qquad(\sigma\downarrow0).
 \label{eq:directed-limit}
\end{equation}
For a compact uniformly admissible parameter family, the error satisfies the
uniform bounds of \cref{thm:uniform}.
\end{corollary}

\begin{proof}
Apply \cref{thm:localization} to the two words and subtract.
\end{proof}

Cyclic rotations of three blocks leave both traces unchanged.  A transposition
reverses the sign.  Therefore $\Omega_{\ell,\sigma}$ and
$\Omega_{\ell,0}$ are alternating functions of $(a,b,c)$.  On a uniformly
admissible neighborhood they factor as
\begin{equation}
 \Omega_{\ell,\sigma}(a,b,c)
 =V(a,b,c)G_{\ell,\sigma}(a,b,c),
 \qquad
 V(a,b,c)=(a-b)(b-c)(c-a),
 \label{eq:vandermonde}
\end{equation}
with a smooth symmetric quotient $G$.  The diagonal values
\begin{equation}
 \chi_{\ell,\sigma}(u)=G_{\ell,\sigma}(u,u,u),
 \qquad
 \chi_{\ell,0}(u)=G_{\ell,0}(u,u,u)
 \label{eq:curvatures}
\end{equation}
are the noisy and deterministic orientation curvatures.

\begin{corollary}[Curvature limit]
\label{cor:curvature}
If a neighborhood of the diagonal word at $u$ is uniformly admissible, then
\begin{equation}
 \chi_{\ell,\sigma}(u)=\chi_{\ell,0}(u)+O(\sigma^2)
 +O\left(\sigma^{-N_3}e^{-\Gamma/\sigma^2}\right).
 \label{eq:curvature-limit}
\end{equation}
\end{corollary}

\begin{proof}
Alternating smooth functions are divisible by the Vandermonde polynomial.
The diagonal quotient is a fixed linear combination of third parameter
derivatives.  Apply \cref{thm:uniform} with $r=3$.
\end{proof}

This result separates two questions that are easily conflated.  A fixed
separated directed trace may already be close to its orbit target while its
diagonal curvature remains preasymptotic, because the latter probes three
parameter derivatives and amplifies exponentially small path sectors.

\section{Exact failure conditions and the long-cycle obstruction}
\label{sec:obstructions}

The proof identifies three distinct boundaries of the theorem.

\subsection{Trace degeneracy}

If $F_{\bu}'(x)=1$, then $\det J=0$ and the quadratic Laplace approximation
fails.  The contribution is no longer of order one with weight
$|1-F_{\bu}'|^{-1}$; its scaling depends on the first nonvanishing higher jet.
This is a genuine dynamical bifurcation, not a numerical conditioning issue.

\subsection{Boundary contact}

If a deterministic cycle contains $x_j=\pm1$, the local integration region is
a cone rather than $\R^m$, and the relevant row normalizer may contain only a
half Gaussian at leading order.  One must then recompute the tangent-cone
integral.  Merely inserting the boundary orbit into \eqref{eq:orbit-sum} is not
justified.

Even without exact contact, a small clearance $\delta$ delays the replacement
of the truncated normalizer by $\sqrt{2\pi}\sigma$.  The correction contains
a scale comparable to $\exp[-\delta^2/(C\sigma^2)]$ and can be large until
$\sigma\ll\delta$.

\subsection{No automatic determinant limit}

For every fixed $m$, compactness supplies a positive action gap outside the
orbit tubes.  Nothing in \cref{thm:localization} prevents that gap, the minimum
$|1-F_{\bu}'|$, or the boundary clearance from collapsing as $m\to\infty$.
The constants in \eqref{eq:uniform-bound} can therefore deteriorate with
$m$.  A Fredholm determinant is assembled from traces of all lengths
\citep{Ruelle1976,Baladi2018,Bornemann2010}; interchanging its trace series
with $\sigma\downarrow0$ requires a new uniform estimate.  The present theorem
does not supply one.

This is the next precise obstruction: fixed cycles have a deterministic
shadow, but a long-cycle spectral object needs uniform orbit growth,
nondegeneracy, and action-gap control.

\section{Numerical audit at the band-merging parameter}
\label{sec:numerics}

The computations use
\begin{equation}
 u_c=1.543689012692,
 \qquad
 (a,b,c)=(u_c-0.12,u_c,u_c+0.12).
 \label{eq:numerical-parameters}
\end{equation}
All fixed points are obtained independently from sign-changing brackets and
the roots of the explicit composition polynomial, then refined against
$F_{\bu}(x)-x$.  The residual determinant identity is checked orbit by orbit.
Positive-action constrained minima are found by multistart optimization; they
are an onset audit, not a proof of global minimality.

The noisy operators use the exact even-state folding.  If $n$ is the folded
dimension, the full midpoint dimension is $2n$.  Except in the resolution
audit, $n\sigma$ is held near $30.72$.  The implementation uses
NumPy, SciPy, and Matplotlib \citep{HarrisEtAl2020,VirtanenEtAl2020,Hunter2007}.

\subsection{Deterministic admissibility}

\Cref{tab:orbits} records every quantity needed by the theorem.  The
autonomous three-step word has one fixed point, while the six-step word has
fifteen.  All six tested words are interior and strongly nondegenerate.

\begin{table}[t]
\centering
\small
\caption{Deterministic orbit audit.  The final column is the least positive
boundary-constrained action found numerically.  It is diagnostic rather than
certified.}
\label{tab:orbits}
\begin{tabular}{lrrrrr}
\toprule
word & cycles & $\cP(\bu)$ & min clearance & min $|1-F'|$ & action audit\\
\midrule
$3$ autonomous & 1  & 0.17453335 & 0.456311 & 5.72956 & $1.4155\times10^{-2}$\\
$6$ autonomous & 15 & 1.62214109 & 0.017030 & 8.00615 & $2.8309\times10^{-2}$\\
$3$ forward    & 1  & 0.17536312 & 0.440355 & 5.70245 & $4.9074\times10^{-3}$\\
$3$ reverse    & 1  & 0.17538024 & 0.427628 & 5.70190 & $2.8307\times10^{-3}$\\
$6$ forward    & 11 & 1.03996685 & 0.016242 & 6.98681 & $7.7347\times10^{-4}$\\
$6$ reverse    & 11 & 1.03567599 & 0.015579 & 8.13820 & $6.1547\times10^{-4}$\\
\bottomrule
\end{tabular}
\end{table}

The resulting directed targets are
\begin{equation}
 \Omega_{1,0}=-1.7114657240\times10^{-5},
 \qquad
 \Omega_{2,0}=0.004290861044.
 \label{eq:directed-targets}
\end{equation}
Both are nonzero.  Symmetric-separation Vandermonde quotients extrapolated
quadratically in $\eps^2$ give
\begin{equation}
 \chi_{1,0}(u_c)=-0.00487349098,
 \qquad
 \chi_{2,0}(u_c)=-3.11415342.
 \label{eq:curvature-targets}
\end{equation}

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/orbit_action_geometry.pdf}
\caption{Deterministic geometry.  Left: closure residuals for the autonomous
three- and six-step compositions.  Middle: all fifteen six-step weights; color
encodes boundary clearance.  Right: least positive constrained action found
for the autonomous and directed words.  The directed six-step action scale is
roughly four to six times smaller than its three-step counterpart.}
\label{fig:geometry}
\end{figure}

\subsection{Small-noise localization}

\Cref{tab:noise} gives a representative subset of the fourteen noise levels.
The three-step trace approaches its target monotonically after the first
crossover.  The six-step trace first moves away, peaks near $\sigma=0.015$,
and only then returns.  Its directed difference changes sign several times
before recovering the positive target direction.

\begin{table}[t]
\centering
\small
\caption{Finite-noise traces.  The deterministic targets are
$0.17453335$, $1.62214109$, $-1.71147\times10^{-5}$, and $0.00429086$,
respectively.}
\label{tab:noise}
\begin{tabular}{rrrrrr}
\toprule
$\sigma$ & full $d$ & $\tr K_\sigma^3$ & $\tr K_\sigma^6$
& $\Omega_{1,\sigma}$ & $\Omega_{2,\sigma}$\\
\midrule
0.080 &   768 & 0.19311344 & 1.65903236 & $-1.90682\times10^{-2}$ &  0.0490761\\
0.040 &  1536 & 0.17534062 & 1.74737843 & $-8.09025\times10^{-3}$ &  0.0392209\\
0.020 &  3072 & 0.17473087 & 2.14680162 & $-4.33771\times10^{-5}$ & $-0.0062862$\\
0.010 &  6144 & 0.17458261 & 2.04666127 & $-1.71593\times10^{-5}$ & $-0.0048432$\\
0.006 & 10240 & 0.17455108 & 1.71644394 & $-1.71307\times10^{-5}$ & $-0.0017904$\\
0.004 & 15360 & 0.17454123 & 1.64649700 & $-1.71218\times10^{-5}$ &  0.0032098\\
\bottomrule
\end{tabular}
\end{table}

At the five smallest widths, the fitted powers of the absolute errors are
\begin{equation}
 |\tr K_\sigma^3-\cP_3|\sim\sigma^{2.00071},
 \qquad
 |\Omega_{1,\sigma}-\Omega_{1,0}|\sim\sigma^{2.00222},
 \label{eq:fitted-powers}
\end{equation}
matching \cref{thm:localization}.  At $\sigma=0.004$, their absolute errors are
$7.88\times10^{-6}$ and $7.12\times10^{-9}$.

The six-step quantities are later but visibly turn toward their targets.  At
the smallest width,
\begin{equation}
 \tr K_\sigma^6=1.64649700,
 \qquad
 \Omega_{2,\sigma}=0.00320980,
 \label{eq:six-smallest}
\end{equation}
compared with $1.62214109$ and $0.00429086$.  The sign recovery in the second
quantity is essential: the intermediate negative values are preasymptotic,
not the deterministic orientation.

\begin{figure}[t]
\centering
\includegraphics[width=0.94\textwidth]{figures/small_noise_localization.pdf}
\caption{Small-noise localization.  Top: autonomous traces and their orbit
targets.  Bottom: directed traces normalized by their nonzero deterministic
targets.  The three-step ratio reaches one rapidly.  The six-step ratio is
nonmonotone and sign-changing before returning toward one.  The horizontal
axis is logarithmic and decreases to the right.}
\label{fig:localization}
\end{figure}

\subsection{Resolution and diagonal curvature}

At fixed $\sigma=0.004$, the folded dimension was varied from $3072$ to
$7680$, so $n\sigma$ ranged from $12.288$ to $30.72$.  Across this range the
spreads were
\begin{equation}
 \begin{array}{c|cccc}
 &\tr K^3&\tr K^6&\Omega_1&\Omega_2\\ \hline
 \text{spread}
 &6.11\times10^{-16}&1.18\times10^{-6}&3.05\times10^{-16}&1.76\times10^{-7}.
 \end{array}
 \label{eq:resolution-spread}
\end{equation}
Thus the remaining six-step target error is a noise effect, not a midpoint
resolution artifact.  Fixed-$\sigma$ midpoint convergence follows the
collectively compact theory developed for the same kernel family
\citep{Anselone1971,Atkinson1997,WangContinuum2026}.

\begin{figure}[t]
\centering
\includegraphics[width=0.91\textwidth]{figures/deterministic_curvature.pdf}
\caption{Deterministic Vandermonde quotients against squared symmetric
separation.  The star at zero is the extrapolated diagonal limit.  Both the
three-step and parity-compatible six-step limits are nonzero.}
\label{fig:curvature}
\end{figure}

The contrast between \cref{eq:fitted-powers} and the six-step crossover is
consistent with the geometry in \cref{tab:orbits,fig:geometry}.  The directed
six-step words have positive boundary-action scales near
$6\times10^{-4}$ and orbit clearances near $1.6\times10^{-2}$.  Both are much
less favorable than for three steps.  The uniform theorem predicts eventual
localization but does not promise monotonic approach.

\section{Consequences and next mathematical gate}
\label{sec:consequences}

The fixed-noise directed trace has passed a precise deterministic test.  It is
not created by Gaussian smoothing: at fixed length it converges to the
difference between two ordered periodic-orbit sums.  The parity-compatible
six-step scalar also survives; its late onset explains why moderate-noise
experiments can report the wrong sign.

Three distinctions remain important.
\begin{enumerate}[label=(\roman*),leftmargin=2.2em]
 \item A finite-noise resonance list and a deterministic orbit sum are not the
 same object.  This paper relates fixed traces, not individual eigenvalues.
 \item A fixed-length result is not a determinant result.  Uniform estimates
 in the cycle length are still missing.
 \item A nonzero directed orbit sum records temporal ordering.  It does not by
 itself provide a canonical self-adjoint realization or a universal counting
 law.
\end{enumerate}

The next mathematical gate is therefore unambiguous: control the orbit count,
multipliers, boundary clearance, and action gap uniformly enough in $m$ to
sum the centered trace series on a nontrivial domain.  A positive result would
produce a deterministic directed cycle determinant.  A collapse of any one of
those controls would be a rigorous obstruction rather than a failed numerical
fit.

\section{Conclusion}

The normalized Gaussian quadratic cocycle has a rigorous fixed-length
zero-noise trace formula.  Closed-path localization reduces the trace to
deterministic cycles, and the residual determinant identity produces the
weight $|1-F'|^{-1}$ without an undetermined covariance factor.  The theorem
is uniform under interiority and nondegeneracy and exposes the exponentially
delayed sectors that parameter derivatives can amplify.

Both minimal directed constructions persist.  The three-step trace converges
quadratically and rapidly to a nonzero ordered orbit difference.  The
parity-compatible six-step trace is nonmonotone over a broad range but
eventually recovers the predicted sign and moves toward a second nonzero
target.  Boundary contact, trace degeneracy, and loss of a uniform action gap
are the exact places where the argument can fail.

The outcome is a constructive fixed-cycle bridge and a sharply stated next
problem: determine whether the estimates can be made uniform in cycle length.

\section*{Data and code availability}

The manuscript source, tested implementation, machine-readable tables, and
complete figure script are available at
\url{https://github.com/maris205/prime_dynamics_theory/tree/main/papers/RH-9-small-noise-cycle-localization}.
Exact reproduction commands are listed in the accompanying README.

\section*{Acknowledgments}

The author acknowledges the use of an AI language model for assistance with
mathematical cross-checking, code auditing, manuscript organization, and
typesetting.  The author verified the final arguments and assumes
responsibility for the content.

{\small
\bibliographystyle{plainnat}
\bibliography{references}
}

\end{document}
