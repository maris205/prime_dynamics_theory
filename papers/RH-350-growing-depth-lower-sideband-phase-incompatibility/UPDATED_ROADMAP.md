# Roadmap after RH-350

RH-350 proves that the phase-incompatibility mechanism is not confined to two fixed sidebands.  On every triangular lower-even window

    m_(k,j)=k-j,  2<=j<=J_k,
    J_k->infinity,  J_k=o(k),

the orbit demand and parity packet admit uniform normalized laws.  The finite-depth relative minimax tends to one, while the physically weighted minimax has the positive limiting value

    A_infinity=1/(x-1)-1/(x lambda-1).

Under the named actual aggregate hypothesis

    x^(-(k-2)) sum_j |Y_(k,j)|/(2H_(m_(k,j))) -> 0,

the selected lower-even direct subprefix diverges exponentially.  This is conditional.  The repository supplies no theorem for that actual triangular-array remainder.

RH-351 should now close the RH-342--RH-351 batch with a ten-layer review.  It must distinguish:

1. exact physical coefficient decompositions and boundary-orbit extractions;
2. scalar information-class underdetermination results;
3. fixed-depth and growing-depth conditional phase obstructions;
4. the still-missing actual signed remainder, head transport, odd-order, upper-alias, and full `E_off` theorems.

The review must keep the RH-241 moving noisy all-order envelope separate from the later deterministic numerator anchor and envelope.  It must report zero Gate A--E progress, no RH-288 activation, and no Hilbert--Polya, Riemann-zero, von Mangoldt, completed-zeta, or RH conclusion.

The only genuine positive reopening input after this batch is an actual moving-order theorem that controls the signed `Y` family, or a direct aggregate annular theorem for the physical modulus complement.  Without such input, the conditional minimax laws do not become unconditional physical prefix verdicts.
