# Roadmap after RH-240

The next strict target is an all-order estimate for cloud-extracted noisy
periodic traces, uniform in the small-noise parameter.  It must be paired with
a coefficient anchor showing that the selected cloud removes the singular
part without swallowing the deterministic numerator.
