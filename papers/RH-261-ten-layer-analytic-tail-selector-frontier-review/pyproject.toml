[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rh261-frontier-review"
version = "0.1.0"
description = "RH-252--RH-261 analytic-tail and selector frontier review"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
