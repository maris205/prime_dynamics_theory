# Roadmap after RH-261

## Current coordinate

```text
legal_heads_obstructed_target_tail_exists_Ms_uncertified_quotient_finite_nonuniform_complete_certificate_zero
```

## What survives

1. The exact deterministic target-tail interface on a disk strictly larger
   than the unit disk.
2. The exact deterministic coefficient dictionary, now audited through order
   28.
3. The orthogonal quotient identity and the conditional block-power envelope
   theorem.
4. The exact finite-head/quotient-tail/target-tail gluing interface.
5. The scoped obstructions for the expanded single-use box, real
   conjugate-closed resolved-window polynomial idempotents, fractional signed
   weights, and the unit-cap integer lattice.

## Route firewall

- Do not turn the order-13--28 fitted root rate into an all-order theorem.
- Do not call a truncated boundary scan a certified value of `M_S`.
- Do not reweight the margin-32 box and present it as a new selector; the
  single-use and unit-cap classes are already excluded.
- Do not treat complex polynomial coefficients as fractional spectral
  multiplicities: idempotence forces binary generalized-root-space masks.
  The current box audit nevertheless excludes only real, conjugate-closed
  masks supported on the resolved RH-254 window.
- Do not accept arbitrary fractional signed moments as a determinant; their
  local products have nontrivial monodromy unless combined exponents are
  integral.
- Do not promote 23 finite Schur computations to a uniform small-noise
  quotient theorem.  Nine archived endpoints and the continuum remain open.
- Do not invoke the RH-240 relative-determinant route until every RH-260
  obligation is certified.

## Reopening triggers for RH-262+

1. A rigorous computable upper bound for
   `M_S = sup_{|z|=S}|log G(z/r_H)|` at some `1 < S < r_H lambda`.
2. A legal invariant anchored selector outside the audited single-use and
   unit-cap classes, together with an actual cloud-to-anchor coefficient
   bridge.  A finite least-squares fit alone is insufficient.
3. A uniform small-noise block-power theorem covering the remaining archived
   endpoints and a continuum bridge with stable selected generalized-root
   spaces.
4. A larger integer-cap audit only when accompanied by a structural operator
   realization; another unconstrained moment fit is not a reopening input.

All Gates A--E remain false/open.
