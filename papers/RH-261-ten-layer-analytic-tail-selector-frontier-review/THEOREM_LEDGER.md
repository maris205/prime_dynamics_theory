# Theorem ledger

| Layer | Decision | Boundary |
|---|---|---|
| Analytic deterministic tail | Exact Cauchy all-order interface on every `0 <= R < S < r_H lambda` | A certified numerical `M_S` is not supplied |
| Order-28 anchor atlas | Exact coefficient dictionary through order 28 | The fitted root rate `0.700998...` is finite-data only |
| Expanded spectral window | 32 finite endpoint atlases, 512 new roots | 11/32 fixed-count boundaries split a conjugate pair |
| Expanded single-use box | 0/32 passes with LP primal--dual gap below `5.83e-15` | Only the archived margin-32 box class is excluded |
| Polynomial idempotents | Exact collapse to binary generalized-root-space masks; the RH-255 consequence is real, conjugate-closed, resolved-window only | Non-conjugate complex masks, non-idempotent quotient grouping, and outside algebras remain open |
| Fractional signed fits | Exact monodromy integrality barrier | 32/32 finite fits are not legal determinant quotients |
| Unit-cap signed integers | 0/32 passes, zero reported MIP gap | Integer caps greater than one and operator realization remain open |
| Quotient block powers | 23/23 finite twelfth powers contractive | Nine archived endpoints and the small-noise continuum are open |
| Updated gluing ledger | Exact five-obligation interface; current vector has one true component | Zero complete current certificates is not global nonexistence |
| Macro route | Scoped Gate-A route stop | Gates A--E remain false/open |

## Review proposition

In the RH-260 certificate interface, a complete certificate requires all five
independent components: a legal anchored head, a coefficient bridge, a
uniform quotient tail, an analytic target tail, and a certified target
boundary constant.  The archived component vector is
`(false, false, false, true, false)`.  Therefore the current complete
certificate count is exactly zero.  This is a logical conclusion about the
archived evidence ledger, not a theorem that no other selector or analytic
construction can succeed.

The aggregate review contains 842 finite records and zero audit failures.
The implicit class sizes `62,030,604,700` and
`39,417,456,084,975,216` are reported separately and are not interpreted as
all-order information.
