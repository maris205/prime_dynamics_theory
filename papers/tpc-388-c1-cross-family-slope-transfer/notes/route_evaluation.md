# TPC-388 route evaluation

The Session files `propose.md`, `skills/route-a-evaluator.md`, and
`skills/route-b-evaluator.md` are absent from this checkout.  The available
local evidence is fail-closed:

```text
ROUTE_A = OPEN
ROUTE_B = OPEN
FINITE_CERTIFICATE = PASS (producer + independent replay + stress)
NUMERIC_RESULT = FINITE_CROSS_FAMILY_PARENT_SLOPE_TRANSFER
ORIGIN_UNIFORMITY = OPEN
ARITHMETIC_ADVANCE = NO
FIXED_POWER_CREDIT = 0
```
