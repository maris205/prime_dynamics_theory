# TPC-263 citation and provenance verification

The paper uses only repository-local source locks.  TPC-257 supplies the
four-block frame and the three adjoint coefficient asymptotics.  TPC-254
supplies the maximal-interval `m=1` extraction used on each of the four
consecutive blocks.  TPC-262 supplies the signed cross-Gram typing firewall
and the warning that a finite channel is not the whole growing-shell object.

The separately named Session Route-A / Route-B evaluator files are not present
in this checkout.  The local proof package, theorem ledger, route evaluation,
bridge checker, and `AGENTS.md` are used as the fail-closed evaluator record.
