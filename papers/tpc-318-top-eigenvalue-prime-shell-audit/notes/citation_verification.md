# TPC-318 citation verification

* Weyl's eigenvalue perturbation inequality is used only as a standard finite
  matrix fact; see `references.bib`.
* The prime-shell operator, source normalization, and predecessor trace-power
  interface are inherited from the locked local TPC-317/TPC-316 artifacts.
* No external theorem is used to convert the finite spectral observations into
  an arithmetic or asymptotic claim.
