# Route update after RH-104

## Prefix gate status

RH-104 replaces the old vague finite-prefix leaf by the exact physical target

```text
P_M^2 = sum_{r<M} ||Y A^r X||_F^2 = tr(X* G_M X).
```

The block tail is already controlled conditionally by the source block and
the square-root contraction law. The five physical anchors are green, but a
uniform theorem for `P_M` is not yet proved.

The crude product `||Y|| sqrt(S_M)` is not an adequate proxy: it loses a
factor above sixteen at the fifth anchor, and the nilpotent barrier proves
that packet normalization and block contraction cannot recover the missing
directional information.

## Decomposed absolute-scale frontier

- `P`: exact finite-prefix target identified; uniform law open.
- `Z`: reduced packet future law open.
- `Omega R`: signed observation/residual product open.
- `U`: upstream bridge power remains an inherited all-level leaf.
- `Q`: uniform gap-aware quotient law open.

## Recommended next papers

1. RH-105: prove the observation--residual cancellation theorem and audit the
   five-anchor weighted products.
2. RH-106: formulate the uniform gap-aware stopped quotient law in terms of
   cross-energy/gap ratios and endpoint slack.
3. Return to the physical prefix Gramian only when an all-level structural
   mechanism, rather than another separate-norm bound, is available.

The RH-75 full-block criterion remains the fallback route because it closes
prefix and tail together if all of its physical hypotheses can be proved.
Do not reopen moving-cloud A5, arithmetic traces, or zero identification
before the Stage-A absolute-scale and quotient gates are closed.
