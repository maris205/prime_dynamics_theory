# Roadmap after RH-135

## Metric-aware gauge

Replace the Euclidean polar gauge by an optimization balancing:

- Gram inflation, which controls `rho`;
- frame displacement, which controls frame forcing;
- boundary forcing, which is gauge-independent after the target frame is fixed.

The target objective is the optimized affine fixed floor, not angle alone.

## Finite gate

Re-audit all 216 recurrent transitions.  Improvement means increasing the
51 contractive cases without making forcing floors superunit.  A universal
impossibility theorem is also publishable if the two costs cannot be balanced.

## Later

Only after a viable relative recurrence should the route return to normalized
frame volume, capacity coupling, outward all-level composition, and the
ten-layer review.
