# Roadmap after RH-128

The directional branch is conditionally closed.  RH-129 should audit the ten
layers and isolate the smallest remaining physical packet:

- an all-level validated affine recurrence with subunit fixed point;
- a positive normalized frame/capacity liminf;
- a common assembly or RH-127 outward radii.

Finite scale fits must remain separate from this packet.

