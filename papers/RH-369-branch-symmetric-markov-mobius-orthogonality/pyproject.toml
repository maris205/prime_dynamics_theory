[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rh369-branch-symmetric-markov"
version = "0.1.0"
description = "Exact finite audits for RH-369"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
