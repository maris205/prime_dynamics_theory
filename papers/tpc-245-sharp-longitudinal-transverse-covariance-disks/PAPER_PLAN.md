# Paper Plan

## Claim contract

Maximum claim:

`PROVED_STRUCTURAL_L1_SHARP_LONGITUDINAL_TRANSVERSE_COVARIANCE_DISKS`.

The paper proves an exact abstract Hilbert-space feasible-set theorem.  It does
not identify the abstract direction or vectors with the literal V59 source.

## Narrative

1. Freeze the orientation and the missing physical attachment inherited from
   TPC-243 and TPC-244.
2. Derive the center plus transverse covariance identity.
3. Classify the feasible set in transverse dimensions zero, one, and at least
   two.
4. Extract exact zero-feasibility, minimum modulus, and a sharp phase sector.
5. Certify boundary fixtures and record the next weighted-reassembly problem.

## Invalidation conditions

- using `conjugate(b)w` instead of `conjugate(w)b`;
- claiming a disk in transverse dimension one;
- treating unrealizable positive energy in transverse dimension zero as a
  feasible singleton;
- identifying TPC-219's constant-`q` subspace with a source-defined block vector;
- promoting existential feasible-set geometry to physical cancellation;
- claiming arithmetic `L2`, strict `1/400`, full Gate B, or twin primes.
