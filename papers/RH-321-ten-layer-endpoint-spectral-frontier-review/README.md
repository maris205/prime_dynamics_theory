# RH-321: Ten-layer endpoint spectral frontier review

RH-312--RH-321 establish:

- an exact universal endpoint logarithm plus analytic remainder;
- orthogonal parity channels and the optimal `N^(-1/2)` Hardy rate;
- exact root-of-unity packets and recursive finite normal spectral prefixes;
- sharp rank and squared-mass growth `Theta((q_star/q)^N)`;
- the optimal genuine-spectral endpoint law
  `E_spec(M)^2 ~ log(q_star/q)/log M`;
- genuine spectral saturation of every strict-annulus rate;
- an escaping-packet counterexample separating strict-annulus convergence
  from endpoint energy tightness.

Synthetic spectral realizability is now sharp, but the actual noisy family
still lacks fixed-order complement transport and endpoint energy tightness.
The batch is a scoped spectral-realizability route stop and supplies no
actual endpoint or joint first-alias reopening theorem.
Both typed ledgers remain score 4/5, cross-branch glue is false, complete count
is zero, and Gates A--E remain open.

After building every result, test suite, and PDF, run the four archive tools
from this directory.
