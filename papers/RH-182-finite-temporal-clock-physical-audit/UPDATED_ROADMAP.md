# Roadmap after RH-182

The direct orthogonal temporal clock fails even after the time-domain
orientation mark:

```text
orthogonal source clock -> wrap/primal/adjoint gate   [0/126]
```

RH-183 should prove the optimal phase and arbitrary-scalar wrap lower bounds
so the negative result cannot be attributed to a sign or normalization
choice. If the projective obstruction persists, move to separate right and
left temporal spaces generated by the physical source and observation.

Validated Riesz data and Gate A remain open.
