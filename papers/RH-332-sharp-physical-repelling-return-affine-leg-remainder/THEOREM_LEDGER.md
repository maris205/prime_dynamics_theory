# Theorem ledger

| Claim | Status |
|---|---|
| Exact Jacobian-scaled physical repelling-return row | Proved |
| Exact physical-to-curved `L1` boundary identity | Proved |
| Exact curved-to-tangent Gaussian-shift identity | Proved |
| Fixed-row coefficient `sqrt(2/pi)*u_c*u^2` | Proved; zero only at `u=0` |
| Global uniform row `O(sigma)` bound | False; `x=b` gives an order-one lower obstruction |
| Uniform fourth moment of the actual RH-324 first-leg `U`-marginal | Proved on compact phase sets |
| Sectorwise weighted second-moment convergence | Proved for `U<0` and `U>0` |
| Sharp second-hybrid Duhamel row coefficient in each orientation | Proved; both transported coefficients are strictly positive |
| Closed total coefficient | Proved |
| Exponential or `o(sigma)` second-hybrid accuracy | False |
| Fully physical versus fully affine two-leg equality | Not claimed; first-leg replacement is a separate hybrid term |
| Equality after taking the `W` marginal | Not claimed; only contraction is available |
| First-alias local scale compatibility | Proved; no composition inferred |
| Full boundary-cycle `O(k*sigma)` transport | Open; RH-333 interface |
| Cyclic trace, parity/shell cancellation, full-trace replacement | Open |

`U<0` and `U>0` are repelling orientations around `r`; they are not the
RH-19 critical siblings around `b`.  Gates A--E remain false/open.
