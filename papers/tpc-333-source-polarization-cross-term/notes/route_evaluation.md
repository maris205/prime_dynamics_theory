# TPC-333 route evaluation

The local evidence stack is the producer, independent source replay, exact
rational anchor, mutation stress, and fail-closed Bridge-B wrapper.

The Session files `propose.md`, `skills/route-a-evaluator.md`, and
`skills/route-b-evaluator.md` are absent from this checkout, so the official
evaluators cannot be invoked:

```text
Route-A = NOT_EVALUATED_OFFICIAL
Route-B = LOCAL_FAIL_CLOSED_FALLBACK
ARITHMETIC_ADVANCE = NO
FIXED_POWER_CREDIT = 0
FULL_GATE_B = OPEN
```
