# TPC-238 Claim Firewall

## Allowed claims

- The triangular-window identity is exact.
- Primitive rational frequencies of height at most \(U\) are
  \(U^{-2}\)-separated.
- The Fejér off-diagonal estimate is
  \(F_L(\theta)\leq(4L\|\theta\|^2)^{-1}\).
- The circular packing row sum is at most
  \(\pi^2/(3\delta^2)\).
- The finite-window and normalized lower-frame inequalities are proved.
- The V59 defect exponent is \(-67/100\).
- Cross-reduced-frequency fixed-power saving is refuted only after
  \(q\)-collapse and only relative to the collapsed coefficient energy.

## Forbidden upgrades

The following statements are outside the proved result:

- cancellation is impossible inside a \(q\)-bucket;
- the literal coefficients \(C_h\) have no useful signed cancellation;
- the signed four-packet projection has no useful cancellation;
- the constant \(\pi^2/12\) is sharp;
- the normalized constant \(1/2\) is sharp;
- an arithmetic prime-shell estimate has advanced;
- Route A or full Route B has passed;
- the global strict \(1/400\) target has been paid.

## Machine-readable ledger

    TPC238_STATUS = PROVED_STRUCTURAL_OBSTRUCTION_L1
    ARITHMETIC_ADVANCE = NO
    C_H_SIGNED_CANCELLATION = NONE
    WITHIN_Q_BUCKET_CANCELLATION = OPEN
    SIGNED_FOUR_PACKET_PROJECTION = OPEN
    L2 = NONE
    FULL_GATE_B = OPEN
    STRICT_1_OVER_400 = UNPAID_GLOBAL
    FIXED_ATOM = 0
    SHARPNESS = NOT_CLAIMED
