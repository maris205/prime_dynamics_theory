# TPC-340 route evaluation

The local evidence is the finite Schur/Frobenius proof, parent-locked
producer, independent reverse-shell replay, mutation stress, PDF audit, and
fail-closed Bridge-B wrapper.

The Session-named `propose.md`, `skills/route-a-evaluator.md`, and
`skills/route-b-evaluator.md` files are absent:

```text
Route-A = NOT_EVALUATED_OFFICIAL
Route-B = LOCAL_FAIL_CLOSED_FALLBACK
ARITHMETIC_ADVANCE = NO
FIXED_POWER_CREDIT = 0
FULL_GATE_B = OPEN
```

The Schur branch materially improves the zero-support envelope, but the
broad-mask tightness obstruction remains.  The next step is a structural
cross-class compensation/orthogonalization test or a fresh adversarial holdout.
