# Roadmap after RH-281

The next admissible input is aggregate noisy-cloud transport into the
monodromy shell together with a variable-rank block-power certificate, or an
independent proof that the graded counterterm is canonical for the noisy
dynamics.  Fixed finite phase fits, raw `S2` zero-noise limits, and fixed-rank
limiting contractions are closed routes.
