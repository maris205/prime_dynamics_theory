# Roadmap after RH-188

The directed product survives in a small local window set, so the next step
is structural rather than another scalar residual threshold:

```text
biorthogonal packet
  -> exact packet/complement coordinates
  -> Feshbach self-energy
  -> packet contour inverse + complement contour inverse
  -> directed Schur product
  -> Riesz rank
```

RH-189 should construct the exact oblique coordinate similarity and
determinant factorization.  Only after that identity is fixed should a
physical complement-resolvent budget be attempted.
