# TPC-329 computational protocol

1. Verify the TPC-328 producer/certificate and the TPC-267 V59 source locks.
2. Freeze the two new origins, two scales, four shell anchors, two exponents,
   literal masks, source formula, and `5e-8` ratio guard.
3. Build the source midpoint vector and four coherent matrices for each of the
   `32` Cartesian rows.
4. Record `E`, `D`, `O`, the ratio, the outward display guard, and its label.
   Require positive energy and diagonal terms.
5. Apply the predeclared affine permutation `pi(i)=(5*i+17) mod M`; verify its
   bijection and exact source multiset/`L2` preservation, then replay all four
   metrics on the permuted vector.
6. Pair the `4096` and `8192` rows at fixed origin, shell, and exponent to
   produce `64` growth records; do not infer an exponent beyond this finite
   comparison.
7. Rebuild every row in the independent checker without importing the
   producer, using reverse shell accumulation and separate source arithmetic.
8. Run the exact rational anchor and mutation stress suite.
9. Run normal/optimized producer, independent, stress, PDF, and local
   Bridge-B checks; require empty stderr and byte-identical paired output.
10. Treat every readout as finite.  No row count, growth factor, or placement
    difference creates a source-uniform theorem or fixed-power credit.
