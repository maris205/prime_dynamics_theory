# TPC-360 route evaluation and proof package

## Frozen object

The object is the TPC-355 unsigned mask-energy congruence applied to the
literal deleted-diagonal two-endpoint divisibility-masked prime-shell matrix.
TPC-359's three origins are inherited as fixed data; TPC-360 adds no source
response and no arithmetic reassembly.  Counts are 256 and 512, shells are
`Q=24,54,80`, exponents are 1 and 2, and all four fixed sign laws receive a
true symmetric-matrix spectral replay.

## Evidence ledger

| item | status | evidence |
|---|---|---|
| Schur envelope | `PROVED_EXACT_FINITE` | finite induced-norm inequality and exact anchor |
| Frobenius envelope | `PROVED_EXACT_FINITE` | finite singular-value inequality and exact anchor |
| all-law spectral replay | `NUMERICALLY_CERTIFIED_FINITE_144_ROWS` | producer plus independent reverse-shell checker |
| Schur slack | `NUMERICALLY_CERTIFIED_FINITE_SCOPED` | max spectral/Schur `0.77628391453148915` |
| law-uniform finite cap | `NUMERICALLY_CERTIFIED_FINITE_SCOPED` | all 144 spectra below `0.64` |
| all-plus universal proxy | `OPEN` | mod-4 wins 6 of 36 settings |
| growing operator estimate | `OPEN` | no growing quantifier tested |
| source-uniform arithmetic `L2` | `OPEN` | source response absent |
| Route-B / twin-prime endpoint | `OPEN` / `NONE` | official evaluators absent; no arithmetic credit |

## Tightness calculation

For each normalized matrix `T`, the checker records
`rho_S=||T||_2/S(T)` and `rho_F=||T||_2/||T||_F`.  On this finite set,
`max rho_S=0.77628391453148915` and
`max rho_F=0.62110877254133434`, so the observed relative slack is at least
`0.22371608546851085` and `0.37889122745866566`, respectively.  These are
finite descriptive bounds, not uniform estimates in origin or interval length.

The setting-wise spectral winner census is all-plus `30`, mod-4 `6`, and zero
for alternating-index and half-split.  This is useful for choosing future
diagnostics, but it does not justify dropping the other laws in a theorem.

## Route decision

The official Session files `propose.md`, `skills/route-a-evaluator.md`, and
`skills/route-b-evaluator.md` are absent, so no official evaluator verdict is
issued.  Local Bridge-B is fail-closed artifact evidence only.  The maximum
claim remains finite and scoped with `ARITHMETIC_ADVANCE=NO`,
`FIXED_POWER_CREDIT=0`, and `FULL_GATE_B=OPEN`.

## ROUND2_CLUE

`TEST_INDEPENDENT_HIGH_ORIGIN_REPLICATION_WITH_TIGHTNESS_LEDGER`.
