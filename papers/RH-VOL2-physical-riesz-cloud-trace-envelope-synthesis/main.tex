\documentclass[11pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{booktabs,array,enumitem,longtable,xcolor}
\usepackage[colorlinks=true,linkcolor=blue!55!black,urlcolor=blue!55!black]{hyperref}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}
\newcommand{\Tr}{\operatorname{Tr}}

\title{\textbf{Prime Dynamics Program, Volume II}\\
\Large Physical Riesz Packets, Temporal Clouds, and the Trace-Envelope Frontier\\
\large A Provenance-Preserving Synthesis of RH-161--RH-241}
\author{Liang Wang}
\date{August 2026}

\begin{document}
\maketitle

\begin{abstract}
This volume synthesizes RH-161--RH-241, the eighty-one-paper interval that
turns the conditional Stage-A language of Volume I into a typed
packet-to-Riesz, relative-determinant, temporal-cloud program.  RH-161 proves
an abstract fixed-contour assembly theorem.  If a packet block has resolvent
bound $M$, off-packet coupling $\varepsilon$, and $M\varepsilon<1$, spectral
rank is stable; the quantitative graph margin
\[
 \delta=\frac{|\Gamma|M^2\varepsilon}
 {2\pi(1-M\varepsilon)}<1
\]
gives an invertible packet-to-Riesz map.  Complementary Schatten convergence,
pole accounting, normalization, and directed marked traces then define a
conditional enhanced datum for determinant types $p=1$ and $p=2$ without
identifying the two branches.

The following eighty papers develop physical Riesz shells, history/cycle
algebra, biorthogonal Feshbach reductions, source-channel quotients, transport
obstructions, quartet gauges, rank-growing reciprocal clouds, and
projection-free $\det_2$ factors.  The endpoint RH-240 theorem is an exact
criterion: a uniform moving all-order noisy trace envelope would yield a
normal relative-determinant family.  RH-241, however, certifies only orders
$2$--$12$ and leaves both the all-order envelope and coefficient anchor open.

We prove an executable provenance theorem, restate the typed assembly theorem
with its physical hypotheses visible, and give a finite-jet nonpromotion
theorem.  The moving noisy all-order trace envelope remains open.  Gates A--E
remain false/open.  This synthesis does not construct a Hilbert--Polya
operator, identify Riemann zeros, prove a von Mangoldt trace, prove
completed-zeta divisor equality, or prove the Riemann Hypothesis.
\end{abstract}

\noindent\textbf{Keywords:} Riesz projections; packet reductions; relative
determinants; temporal traces; reciprocal clouds; trace envelopes;
provenance audit.

\section{Scope and source hierarchy}

Volume II begins at RH-161 rather than RH-160 because RH-161 is the first
paper to state the complete typed assembly interface.  It is followed by
eight ten-paper phases ending at RH-171, 181, 191, 201, 211, 221, 231, and
241.  Review papers summarize status and finite counts; analytic claims
remain sourced to the individual theorem papers.

We distinguish analytic theorems, finite certificates, conditional
implications, scoped negatives, and open/not-testable leaves.  A packet in an
abstract finite algebra is not automatically an actual noisy spectral packet.
A local probability law is not a trace observation without an identification
theorem.  A finite list of moments is not an all-order analytic envelope.

\section{Executable provenance theorem}

\begin{theorem}[Volume II provenance theorem]\label{thm:provenance}
The executable inventory discovers exactly one complete source directory for
each integer $161\le n\le241$.  It partitions the eighty-one labels into the
singleton RH-161 assembly phase and eight consecutive ten-paper phases,
records the nine anchors, and binds selected source files by SHA-256.
\end{theorem}
\begin{proof}
Directory discovery uses the numerical prefix.  A duplicate or a source
missing its README, TeX manuscript, or PDF is rejected.  The integer list and
the union of the phase intervals are checked against the full source range.
The builder hashes available theorem ledgers, roadmaps, and result records;
the archive verifier independently replays all local and external hashes.
This proves source identity rather than re-proving eighty-one papers.
\end{proof}

The eight review artifacts contain $30{,}435$ explicitly defined finite
items in total.  Their per-review counts are stored in the machine audit.
These counts document breadth and internal consistency only.

\section{RH-161: the typed assembly theorem}

Let a finite packet block be isolated by a contour $\Gamma$.  Write $M$ for
the declared block resolvent supremum and $\varepsilon$ for the off-packet
coupling.  RH-161 proves rank stability under
\[
 M\varepsilon<1
\]
and an invertible graph bridge under the stronger bound
\begin{equation}\label{eq:delta}
 \delta=\frac{|\Gamma|M^2\varepsilon}
 {2\pi(1-M\varepsilon)}<1.
\end{equation}

The assembly then requires one of two reset seeds together with five physical
interfaces:
\begin{equation}\label{eq:assembly}
 (S_{\rm native}\vee S_{\rm lagged})\wedge R\wedge Q\wedge U\wedge Z\wedge T.
\end{equation}
Here $R$ is the physical packet-to-Riesz lift, $Q$ the cloud coefficient and
pole ledger, $U$ a common-space Schatten complement limit, $Z$ canonicity and
normalization, and $T$ directed marked-trace convergence.

\begin{theorem}[Typed assembly theorem]\label{thm:assembly}
Under one seed and all five interfaces in~\eqref{eq:assembly}, exact moving
cloud division, complementary $\mathcal S_p$ convergence, complete pole
accounting, target-independent normalization, schedule independence, and a
fixed separating family of directed marked traces produce an enhanced
relative-determinant datum.  The theorem covers $p=1$ and $p=2$ as typed
alternatives and does not identify them.
\end{theorem}
\begin{proof}
The packet homotopy and~\eqref{eq:delta} identify a same-rank Riesz space.
Exact reducing factorization divides out the finite cloud.  Common-space
$\mathcal S_p$ convergence gives locally uniform $\det_p$ convergence for the
complement.  $Q$ restores the deterministic pole divisor, $Z$ fixes the
zero-free factor and removes schedule/gauge dependence, and $T$ retains the
directed finite words.  Every step uses a named hypothesis.  None of
$R,Q,U,Z,T$ is proved for the all-level prime-dynamics family by RH-161.
\end{proof}

The two inclusion-minimal completion bundles are therefore
\[
 \{S_{\rm native},R,Q,U,Z,T\},\qquad
 \{S_{\rm lagged},R,Q,U,Z,T\}.
\]
This is an exact dependency statement, not Gate-A closure.

\section{RH-162--RH-171: physical Riesz shells}

This block translates the assembly variables into shellwise physical
obligations.  It distinguishes rank isolation, graph stability, coefficient
transport, complement control, and temporal marking.  The RH-171 review
contains $3{,}584$ finite cases and $63$ omission witnesses.  Its four-leaf
frontier is a conditional implication: satisfying the named physical leaves
would yield shell conclusions.

The finite cases test formulas and logical minimality.  They do not provide a
uniform contour or a small-noise coupling theorem.  A globally bounded
projection norm is neither proved nor required as a substitute for the typed
local graph estimates.

\section{RH-172--RH-181: history and cycle realization}

The history/cycle phase proves exact algebra connecting finite temporal
histories, cyclic closures, markers, and determinant contributions.  It
identifies which finite words retain orientation and which quotient
operations erase it.  The $2{,}600$-item review verifies the declared finite
identities.

A literal infinite-history Fredholm construction is stopped in its named
form: the needed compactness and uniform trace-class structure are absent.
The negative conclusion does not reject every adapted history space.  The
surviving route uses finite separating words transported through an actual
common-space limit.

\section{RH-182--RH-191: biorthogonal and Feshbach structure}

These papers develop balanced left/right frames, exact Feshbach identities,
and Schur-budget criteria for nonnormal packets.  The point is not to make the
operator normal, but to control the correct biorthogonal coordinates and
resolvent factors.  The RH-191 audit contains $2{,}960$ finite items.

The conditional Riesz conclusions still require validated physical block
separation and coupling.  Orthogonal finite clocks and unqualified condition
numbers cannot replace those inputs.  A lower bound on conditioning is not an
upper stability theorem.

\section{RH-192--RH-201: source-channel quotients}

This phase distinguishes source-cyclic from source-observation channels and
constructs exact quotient and packet dictionaries.  It proves canonicality
relative to the declared finite source algebra and audits $1{,}352$ items.

The word ``canonical'' remains typed.  It does not prove independence from
every mesh, schedule, reset, or cutoff in the physical continuum family.
Those are the $Z$ obligations of~\eqref{eq:assembly}.  Nor does a source
channel by itself supply the cloud coefficient bridge $Q$.

\section{RH-202--RH-211: transport failures and divisor-first pivot}

Exact intertwining and transport budgets expose the cost of moving quartets
and edge clouds between representations.  Several natural routes fail:
naive Haar transport, scalar residue renormalization, and an expanding-edge
cloud transport lack the required uniformity or normalization.  RH-210 and
RH-211 therefore pivot to a divisor-first formulation.  The review contains
$649$ finite records.

This pivot is a change in proof organization, not a zeta-divisor theorem.
The physical divisor, coefficient bridge, and complete no-missing/no-spurious
statement remain absent.

\section{RH-212--RH-221: quartet shapes and fixed degree}

Centered conjugate quartets admit intrinsic shape coordinates, monotone axial
clocks, boundary strata, and gauge-complete reconstruction formulas.  The
phase also proves transverse quenching and recurrence obstructions.  These
are exact results in the quartet data type, supported by a $2{,}140$-item
review.

A fixed quartic has fixed degree and cannot by itself encode a rank-growing
spectral cloud.  This is a structural obstruction to a fixed-quartic route,
not to all rank-growing packet constructions.  Gauge-complete reconstruction
of one divisor shape does not identify a physical all-level divisor.

\section{RH-222--RH-231: reciprocal clouds and determinants}

This block builds rank-growing conjugate cloud atlases, reciprocal
dictionaries, empirical tightness criteria, and regularized determinant
formulas.  Projection-free descriptions reduce dependence on unstable
projector coordinates.  The RH-231 review audits $9{,}870$ finite items.

The determinant formulas are exact for their declared clouds.  They do not
show that the cloud is the actual physical resonance divisor, that its rank
matches a canonical spectral count, or that its complement has a uniform
common-space ideal limit.  Direct promotion from resonance samples to a
completed divisor is therefore stopped.

\section{RH-232--RH-241: the trace-envelope wall}

The final phase constructs projection-free finite $\det_2$ factors and
analyzes which coefficient information controls a normal analytic family.
Finite jets can match while higher coefficients vary arbitrarily; local
polynomial data therefore do not imply determinant convergence.

RH-240 states the positive all-order criterion.  Schematically, a uniform
geometric envelope for every moving noisy trace coefficient, together with
the correct coefficient identification and analytic tail, yields local
normality and a relative-determinant limit.  RH-241 audits $7{,}280$ finite
items and obtains a subunit envelope only for orders $2$--$12$.

\begin{theorem}[Finite-jet nonpromotion theorem]\label{thm:jet}
Control of a fixed finite set of Taylor or trace coefficients, even with
arbitrarily accurate finite fits, does not imply a uniform all-order analytic
envelope or convergence of the associated determinant family.
\end{theorem}
\begin{proof}
For any fixed jet order $m$ and nonzero $z_0$, the zero-free entire functions
\[
 F_K(z)=\exp\!\left(K(z/z_0)^{m+1}\right)
\]
have identical Taylor coefficients through order $m$ while their values and
higher coefficients are unbounded as $K$ varies.  Thus finite jets do not
control a normal family.  The same logical gap applies to the orders
$2$--$12$ ledger unless a uniform all-order source theorem is added.
\end{proof}

The endpoint has two separate missing leaves:
\begin{enumerate}[label=(\roman*),leftmargin=2.2em]
\item the moving noisy all-order trace envelope; and
\item a no-over-extraction coefficient anchor identifying the selected noisy
cloud coefficients with the determinant numerator.
\end{enumerate}
The deterministic coefficient dictionary developed later in Volume III
solves a target-side problem and does not retroactively prove either leaf.

\section{Frontier and reopening conditions}

The physical route may reopen through an actual moving contour/coupling
theorem, a common-space $\mathcal S_1$ or $\mathcal S_2$ complement limit,
complete $Q/Z/T$ identifications, or a direct all-order noisy envelope with
coefficient bridge.  Each input must use one clock, normalization, and
operator data type.

Fixed finite endpoint extensions, synthetic clouds, local probability
models, and finite regression do not reopen the all-order leaf.  The route
coordinate is
\[
 \boxed{\texttt{physical\_riesz\_cloud\_assembled\_moving\_noisy\_trace\_envelope\_open}}.
\]

\section{Reproduction and claim boundary}

The volume audit verifies the source interval, nine anchors, nine phase
ranges, finite review counts, determinant-type adapter, open physical
interfaces, fixed-order boundary, Gate values, and forbidden macro claims.
It binds selected upstream files and all local publication artifacts by
SHA-256.  The tests enforce that the fixed-order maximum remains $12$ and
that neither the all-order envelope nor coefficient anchor is promoted.

The audit verifies identity and declared scope rather than re-proving the
source corpus.  The moving noisy all-order trace envelope remains open.
Gates A--E remain false/open.  Volume II does not construct a Hilbert--Polya
operator, identify Riemann zeros, prove a von Mangoldt trace, prove equality
with the completed-zeta divisor, or prove the Riemann Hypothesis.

\appendix
\section{Atomic source index}

This machine-generated table exposes every canonical numbered source in the
volume.  It is a provenance index; theorem status remains in the source
ledger and the phase crosswalk.

\input{results/atomic_index.tex}

\end{document}
