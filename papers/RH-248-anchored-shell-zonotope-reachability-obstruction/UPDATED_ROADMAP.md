# Roadmap after RH-248

## Current coordinate

```text
anchored_shell_zonotope_unreachable_open_expanded_window_or_signed_grouping
```

The anchored obstruction is no longer a prefix-order artifact.  The entire
frozen shell-complete box zonotope misses the target at the archived noise
tolerance, with explicit dual witnesses.  The next controlled relaxation is
to allow nonnegative shell weights without the upper bound one.  If that
requires huge multiplicities or remains infeasible, it will identify the next
precise boundary of the spectral-cloud interpretation.

Gates A--E remain open.
