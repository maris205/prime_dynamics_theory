\section{Claim boundary}
\label{sec:claim-boundary}

\subsection*{Established in this synthesis}

\begin{itemize}[leftmargin=2em]
\item The dependency table separates exact algebra, literal branch
  returns, conditional interfaces, and missing arithmetic inputs.
\item The conductor-one and nonconstant \(q_X\mid u\) branches are
  disjoint and already returned at \(X^{o(1)}Q_X^2\).
\item The remaining invertible positive resonance carrier obeys the
  three-ledger master inequality \eqref{eq:master}.
\item Principal mass, a subpower opened-atom cap, and
  width-weighted cross-cell excess are sufficient for complete
  positive resonance closure.
\item On a branch with \(N\ge J_X\), the exact fixed-filter theorem
  saves \(J_X^{1/2}\) over the former worst-eigenvalue factor.
\item The recommended order audits cheap literal kill conditions
  before attempting a deep cross-cell theorem.
\end{itemize}

These are \(\Lone\) synthesis and route-selection results resting on
the cited literal papers.

\subsection*{Not established}

\begin{itemize}[leftmargin=2em]
\item None of
  \(\mathfrak P_X\le X^{o(1)}Q_X^2\),
  \(W_X\le X^{o(1)}\), or
  \(\mathfrak X_X\le X^{o(1)}Q_X^2\)
  is proved here for the growing packet.
\item Positive resonance closure is not the same as a signed
  low-window estimate.
\item The long generic affine sector and the high-frequency tail are
  not closed by \eqref{eq:master}.
\item TPC-18 full-block reassembly, primitive determinant
  dispersion, the actual determinant lower bound, and strict
  endpoint loss remain independent.
\item No growing fixed-\(h_0\) affine M\"obius cancellation theorem,
  sieve-parity breakthrough, Hardy--Littlewood asymptotic,
  prime-pair lower bound, or twin-prime conclusion is proved or
  implied.
\item No relation between Riemann zeros and a dynamical spectrum is
  claimed.
\item Nothing is specialized to \(h_0=2\).
\end{itemize}

\subsection*{Current honest verdict}

The route is still open and has narrowed substantially.  It has not
reached a pure negative result, because two literal branches are
closed and the crude worst-eigenvalue bound has been replaced by
the exact width-sensitive norm.  It has also
not reached \(\Ltwo\), because the three master ledgers and the
generic signed sector remain unproved.  The next decisive question
is local and falsifiable: whether the actual normalized opened atoms
have subpower size.
