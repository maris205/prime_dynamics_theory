# RH-377 repository-locked integrity audit

This ARS-style audit follows the repository-only source policy.  It does not
claim a fresh web or publisher verification.  Davenport and Mirsky metadata
are copied from the frozen RH-374/RH-376 bibliography, and the exact
capacity definitions are locked to RH-371.  All thirteen repository inputs
are SHA-256 locked in `results/result.json`.

## Claim and source alignment

| High-impact claim | Evidence | Verdict |
|---|---|---|
| All-prefix mixed identity | Pointwise ternary expansion, 19,680 exhaustive window/sign cases | VERIFIED |
| `H_(k,0)/N -> e_k/2` | Odd-start CRT sieve, distinct residues for `k<=8`, explicit large-square tail; Mirsky metadata lock | VERIFIED |
| `H_(k,1)=o(N)` | Bounded periodic prime-square masks, fixed-AP Davenport cancellation, union-bound cutoff removal | VERIFIED |
| Thirteen-channel equivalence | Sum/difference of both signs at each `k`, with `B_2=0` and no aggregate at `k=1` | VERIFIED |
| Formal rank 13/kernel 453 | Exact rational row reduction with pairwise disjoint nonempty supports | VERIFIED / FORMAL ONLY |
| Exact `P,Q,U,V` decomposition | Symbolic grouping and 262,144 independent prefix checks | VERIFIED |
| Capacity residual bound | Frozen RH-371 path formula plus two reverse-triangle inequalities | VERIFIED |
| Capacity iff envelope convergence | Exact residual and unconditional `P_N=r_0N+o(N)`, `Q_N=o(N)`, `M_N=o(N)` | VERIFIED |
| Stationary countermodel | Exact rational transition, stationarity, moment recursion, and finite reproduction | VERIFIED / SYNTHETIC ONLY |
| No unconditional capacity claim | Explicit firewall in manuscript, theorem ledger, README, and result artifact | VERIFIED |

Every bibliography entry is cited, every citation has a bibliography entry,
and every stored finite row is regenerated by `experiments/build_result.py`.

## Seven AI-research failure modes

| Mode | Status | Repository-locked evidence |
|---|---|---|
| 1. Implementation bug passing self-review | CLEAR | Independent Boolean, formal-rank, path-DP, channel, residual, and rational-chain checks agree |
| 2. Hallucinated citation | CLEAR (repository-locked) | Only four frozen bibliography entries are used; source hashes are recorded |
| 3. Hallucinated experimental result | CLEAR | Exact integer/Fraction/Decimal outputs regenerate; no regression or fit is used |
| 4. Shortcut reliance | CLEAR | Native endpoints are fixed; cutoff quantifiers are stated; finite enumeration is separated from analytic proof |
| 5. Bug reframed as insight | CLEAR | Symbolic proofs and claim boundaries precede the computation |
| 6. Methodology fabrication | CLEAR | Described loops, counts, schema, source locks, and archive are executable |
| 7. Early frame-lock | CLEAR | Coordinatewise cancellation, direct envelope control, and scoped rigidity remain distinct future routes |

The adversarial pass rejects five promotions: treating the formal rank as an
arithmetic minimality theorem, inferring separate `A/B` convergence from one
sign, claiming the conditional constant unconditionally, treating the
stationary witness as Möbius, and attaching an operator/trace/zero/RH
interpretation.  Route A is `GO` for the stated algebraic and analytic
reductions only; Route B is `STOP_SCOPED`.
