# TPC-358 computational protocol

Origins are the fixed tuple `(52001,120001,220001)`, generated by
`52001+100000j` for `j=0,1,2`.  Counts are `256,512,1024,2048`; shell anchors
are `24,54,80`; exponents are `1,2`; height is `66`; laws are the four TPC-355
laws.  The source response is not evaluated.

The Cartesian product gives 288 rows.  Schur/Frobenius metrics cover every raw
and normalized law matrix; the all-plus law contributes 144 spectral metrics.
The forward producer and reverse-shell checker use different shell summation
orders.  Parent TPC-357 code and certificate hashes are locked.  The exact
anchor is the rational `Q=4`, interval `[52031,52044]` shell `{5,7}`.
