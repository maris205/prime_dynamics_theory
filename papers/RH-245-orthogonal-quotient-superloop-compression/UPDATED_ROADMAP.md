# Roadmap after RH-245

## Current coordinate

```text
orthogonal_quotient_exact_open_uniform_block_power_certificate_and_anchor
```

The selected spectral sector can be removed without a Riesz projector in the
estimate: first pass to its invariant algebraic root space, then realize the
quotient by the norm-one orthogonal compression.  This exactly groups the
physical loops and atomic counterloops before absolute values are taken.

The one-step quotient norm is above one at all 17 audited endpoints, so the
next paper should formulate the correct `m`-block trace-envelope criterion
and audit the first contractive quotient depth.  Uniform control in noise,
not the finite depth observation, is the missing theorem.

The anchored cloud problem from RH-244 remains independent and open.
Gates A--E remain open.
