# Roadmap after RH-211

## Current coordinate

```text
local source-observable edge quartet                  [finite positive]
naive fixed / expanded Haar shell transport           [finite negative]
unique conjugate branch labels                        [finite positive]
dual-channel quartic divisor                          [finite positive]
raw adjacent coefficient stationarity                 [finite negative]
intrinsic center/radius coefficient renormalization   [open, next]
dense small-noise divisor flow                        [open, next]
growing locally-uniform determinant family            [open]
dynamical/Fredholm realization / Gate A               [open]
Gates B--E                                             [untouched]
```

Route coordinate:
`finite_dual_channel_divisor_flow_open_renormalization`.

## RH-212 target

Add predeclared intermediate/finer noise anchors on both channels and test:

1. determinant-radius normalization;
2. centered second-moment normalization;
3. branch assignment margins;
4. left/right normalized coefficient discrepancy;
5. adjacent normalized coefficient variation;
6. whether one formula works at every level without levelwise fitting.

The fixed quartet remains a local laboratory. A positive normalization must
later be extended to a growing cloud before Gate A can close.
