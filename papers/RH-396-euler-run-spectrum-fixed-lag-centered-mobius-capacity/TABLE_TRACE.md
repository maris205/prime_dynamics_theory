# RH-396 theorem, equation, and artifact trace

| Item | Manuscript | Executable artifact |
|---|---|---|
| terminal clock and limit order | equations `clock`, `functional`, `capacity` | certificate group `domain_source_firewall` |
| centered lag window and distance-`2h` safety | equations `output`, `safety` | model and quantifier rows |
| shift convention and collision-aware local data | equations `coordinate-shifts`, `local-data` | group `theta_pi_lambda` |
| `Theta`, `kappa_h`, and phase sum | equations `theta`, `kappa`, `phase-sum` | density contract and collision rows |
| exact-support `Pi`, `lambda`, and transition | equations `Pi`, `Sxy`, `lambda`, `transition` | density and tropical rows |
| fixed-table terminal law | Theorem `fixed-clock` | theorem and source-role contracts |
| exact full eight-state tropical trace | equation `full-eight`, Proposition `tropical` | group `projection_safety_saturation_full8_reflection` |
| four-state non-self-loop scope | Theorem/Proposition `compression` | compression rows |
| strict `h=2,q=4` self-loop obstruction | equation `self-loop-obstruction` | group `selfloop_compression_small_clock` |
| positive projection | Lemma `projection`, equations `relation`, `projected-limit` | projection rows |
| relation safety and saturation | Lemma `saturation`, equations `relation-safety`--`saturated-relation` | relation oracle |
| reflection and absolute capacity | Lemma `reflection`, equation `reflection-sign` | reflection rows |
| raw `N`, raw `alpha`, weighted `M` | equations `NQ`, `alphaQ`, `MQ` | normalization rows |
| divisibility and one-site embedding | Lemma `clock-divisibility` | lift rows |
| per-state shared marginal | equations `marginal-zero`--`marginal-total` | group `marginal_square_saturation_lifts` |
| pair and path charge | equation `pair-charge`, Lemma `path-charge` | marginal-charge rows |
| qualified square-support equality `C=M` | equation `square-saturation`, Proposition `square-saturation` | square-saturation rows |
| same-support cover without gcd condition | equations `cover-counts`, `cover-capacity` | cover rows |
| `h=6` pre/post-`p0` fixtures | Remark `p0-counterexample` | exact lift rows |
| finite `D` and `R` | equations `finite-D`, `finite-R` | group `D_R_finite_infinite_endpoint` |
| run count and raw MWIS identity | equations `run-count`, `alpha-run` | finite-run rows |
| finite Euler endpoint | equations `finite-B-equality`, `finite-B` | endpoint rows |
| infinite products and cofinal limit | Lemma `infinite-density`, equation `cofinal-limit` | infinite-density rows |
| `B_infinity(h)` and all-clock endpoint | equations `Binfinity`, `all-clock-endpoint` | endpoint contract |
| fresh-prime recurrence | equations `Nprime`, `alphaprime`, `normalized-gain` | group `strict_lift_CRT` |
| plateau and CRT even run | Remark `plateau`, Lemma `CRT-two-run` | plateau and CRT rows |
| eventual strictness and arbitrary-clock bridge | Proposition `eventual-strictness`, proof of Theorem `endpoint` | strictness/firewall rows |
| fixed-lag strict baseline | Lemma `isolated-positive` | group `lag_inf_claim_ceiling` |
| outside-prime tail and lag infimum | equations `hY`, `outside-tail`, `lag-infimum` | tail and lag rows |
| source roles and claim ceiling | Section 7 | source-role and firewall contracts |

The artifact records the finite identities and interfaces in this table.
Analytic terminal limits come only from RH-394; the manuscript supplies the
proofs connecting them to the finite optimizer, Euler-run endpoint, strict
nonattainment, and fixed-lag landscape.
