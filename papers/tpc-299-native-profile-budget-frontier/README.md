# TPC-299 — Native profile budget frontier

**Author:** Liang Wang, School of Mathematics and Statistics, Huazhong
University of Science and Technology (HUST), Wuhan, China

## One-line result

TPC-299 adds the missing native-source cost layer to the TPC-298 angle
ladder.  If `U_k` is a literal profile prefix, `V_k=A^T U_k`, and
`M_k=U_k^T U_k`, then the minimum source norm needed for normalized target
RMS at most `tau` is

```
B_{k,tau}(b) = min{c^T M_k c : ||V_k c-b|| <= tau ||b||}.
```

The exact KKT frontier is generated by
`c_lambda=(V_k^T V_k+lambda M_k)^(-1)V_k^T b`, and the budget cannot
increase when a nested source prefix is enlarged.

On the inherited 18-row, 1,380-edge grid at `tau=1/2`, the weighted target's
threshold budget is above `9e-5 ||beta||^2` on all rows, above `5e-4` on
15/18, and above `1e-3` on 14/18.  Even the full available 17-profile
prefix remains above `1e-3` on 11/18.  The all-positive control is below
`1e-4` on 18/18, and the weighted/positive threshold-budget ratio exceeds
20 on 18/18.

This is a finite restricted-profile obstruction.  It does not prove a
growing native budget theorem, arithmetic `L2`, Gate B, a fixed power, or
the twin-prime conjecture.

## Notation erratum (TPC-300)

The producer field named `lagrange_multiplier` records the positive ridge
parameter `rho` in `(V^T V + rho M)c = V^T b`.  The KKT multiplier for the
constraint `||Vc-b||^2 <= R^2` is instead `mu=1/rho`.  TPC-300 proves this
reciprocity and compiles the frozen values as exact dual lower-bound
certificates.  This is a notation correction only: the TPC-299 frontier
budgets and its finite numerical certificate are unchanged.

## Claim ceiling

```
PROVED_EXACT_FINITE = KKT/ridge budget frontier, feasibility, nesting
NUMERICALLY_CERTIFIED_FINITE = 18-row threshold and full-prefix budget atlas
NUMERICAL_OBSERVATION = comparative weighted/positive budget separation
MODELING_CHOICE = literal cutoff ladder, tau=1/2, beta-norm normalisation
OPEN = growing profile budget, arithmetic L2, full Gate B
TWIN_PRIME_RESULT = NONE
```

## Research extraction

```
STRONGEST_POSITIVE_RESULT = EXACT_NATIVE_PROFILE_BUDGET_FRONTIER_COMPILER
STRONGEST_OBSTRUCTION = WEIGHTED_HALF_RMS_BUDGET_ABOVE_1E_MINUS_3_ON_14_OF_18_AND_11_OF_18_FULL_PREFIX
OPEN_THEOREM = UNIFORM_GROWING_SHELL_PROFILE_BUDGET_BOUND
REUSABLE_STRUCTURE = PROFILE_IMAGE -> WHITENED_MAP -> KKT_RIDGE_PATH -> BUDGET_FEASIBILITY
ROUND2_CLUE = TEST_BUDGET_CONSTRAINED_PROFILE_FRONTIER_ON_GROWING_SHELLS_AND_SOURCE_NORMALIZATION
```

## Reproduction

```
export PYTHONDONTWRITEBYTECODE=1
python -B code/tpc299_native_profile_budget_frontier_certificate.py --write
python -B code/tpc299_native_profile_budget_frontier_certificate.py --check
python -B experiments/tpc299_independent_checker.py
python -B experiments/tpc299_budget_stress.py
```

The manuscript is [paper/paper.pdf](paper/paper.pdf).  The Session-named
Route-A/Route-B evaluator files are absent from this checkout; the local
proof, certificate, source-first replay, stress fixtures, PDF audit, and
Bridge-B checker are the available fail-closed validation path.
