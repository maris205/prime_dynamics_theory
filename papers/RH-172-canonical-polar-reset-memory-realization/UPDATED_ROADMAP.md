# Roadmap after RH-172

```text
reset memory packet
  -> canonical finite history polar packet       [proved]
  -> exact normalized history cocycle             [next]
  -> history-to-transfer intertwiner               [open]
  -> physical Riesz data/margins/shell transport   [open]
```

The next layer should derive the exact time-update map between consecutive
history spaces and test whether independently reset packets are invariant
under that rectangular cocycle.
