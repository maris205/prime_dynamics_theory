# RH Program Roadmap after RH-385

## Completed theorem chain

1. RH-366 freezes uniform additive-frequency Davenport cancellation.
2. RH-375 supplies squarefree arithmetic-progression densities.
3. RH-378 freezes the exact lag-two interpolation and the `c11` Chowla
   boundary.
4. RH-379 classifies and optimizes the universally safe phasewise
   `c11(r)=0` families at each fixed clock and identifies `B_infinity`.
5. RH-384 closes the fixed square-clock prime-tail scale analysis used only
   as context here.
6. RH-385 proves uniformity over every fixed polylogarithmic clock window,
   transfers that uniformity to optimizers, and gives the nonempty
   square-clock diagonal witness.

## Route decisions

- Route A: `GO`. The exact `4/13/6/4` ledger and fixed-`B`
  polylogarithmic-clock theorem are complete.
- Route B: `STOP_SCOPED`. The first missing analytic budgets are polynomial
  Fourier mass and active shift-two correlation cancellation.

## Closed local questions

- All 512 tables and 4,608 interpolation values are exactly reproduced.
- The 192 zero-`c11` rows yield 24 vectors of multiplicity 8.
- The legal `c21=-2` witness forces the third DFT channel cost to be 2.
- `Q=lcm(q,M_P)` is only a valid period; it need not be minimal.
- The squarefree union bound costs `X*tau_P` without an endpoint counting
  remainder.
- The square-clock diagonal has an explicit empty-budget sentinel and is
  defined only after clock 36 becomes admissible.

## Still unpaid

The following require new inputs and are not continuations licensed by
RH-385:

- clocks of size `N^epsilon` or unrestricted growing clocks;
- a theorem uniform in `B=B(N)`;
- cancellation for active phasewise `c11` and the shift-two Chowla sum;
- an effective Davenport threshold or effective convergence rate;
- an adaptive-capacity theorem for `K_N/N`;
- a projectively compatible infinite optimizing selector;
- any intrinsic determinant, scattering completion, self-adjoint generator,
  weighted prime-power trace, completed-zeta divisor, zero identification,
  or RH implication.

Gates A--E remain false/open. A successor must source-lock and prove a new
edge paying one of these budgets; finite certificate rows cannot do so.
