# RH-385 Integrity Audit

## Claim-to-evidence audit

- The only new discovery claim is fixed-`B` polylogarithmic-clock
  uniformization and its optimizer/diagonal consequences.
- Davenport cancellation is attributed to RH-366 and classical sources;
  exact table interpolation to RH-378; local densities and the fixed-clock
  optimizer to RH-375/RH-379.
- The RH-383/RH-384 cubic corollary appears only as inherited context.
- Every analytic step of the `4/13/6/4` ledger is displayed in the
  manuscript. The certificate only reproduces finite interfaces.
- The square-clock witness is explicitly absent below clock 36; no
  nonempty maximum is silently fabricated.

## Citation audit

Every bibliography entry is cited. Classical entries carry their published
metadata and DOI. Repository papers are identified by exact frozen titles
and release commits. No mutable policy or handoff file is treated as a
scholarly source or immutable theorem input.

## Exact-computation integrity

- Truth values and all interpolation coefficients use exact integers and
  `Fraction`; no floating approximation proves a row.
- The certificate derives coefficient alphabets, histogram, vector
  multiplicities, and norm maxima from enumeration.
- Normalized DFT fixtures use exact rational real/imaginary parts and retain
  `||w||_infinity`.
- Integer public APIs reject Boolean aliases; prime bounds use `isqrt`.
- The JSON loader rejects duplicate keys, nonfinite values, and non-object
  roots. Canonical fresh equality compares serialized exact types.
- The schema is recursively closed, strictly typed, and validated by an
  official Draft 2020-12 validator.
- All 24 certificate mutations change the canonical payload and fail the
  semantic verifier without relying only on fresh-payload equality.
- Source commit, group membership, duplicate-path, and path-escape mutations
  are tested separately.

## Source and archive integrity

Exactly 67 immutable release blobs are locked in groups `51/8/8`. Live
files equal their release blobs, per-file SHA-256 values roll into sealed
group digests and aggregate digest
`14a401e81d5d1868a8b3148478ca26f8975d0bde08b0a0117d4808571a2c5d79`.
The individual publication manifest freezes 29 local members plus the same
67 external inputs and requires byte identity of the two PDF names.

## Boundary integrity

`B>0` is fixed. `Q` is a common period, not necessarily minimal. No
unrestricted clock, `N^epsilon` clock, varying `B`, active `c11`, effective
threshold, adaptive capacity, projective selector, operator, trace, zero,
or RH claim is present. Gates A--E remain false.

## Required disclosures

- Data/code availability: present.
- Ethics: not applicable; no humans, animals, personal data, or biological
  materials.
- Author contributions: present.
- Funding: no specific external funding.
- Competing interests: none declared.
- AI assistance: disclosed for writing, coding, validation, and adversarial
  review; author responsibility retained.

No research-integrity blocker remains.
