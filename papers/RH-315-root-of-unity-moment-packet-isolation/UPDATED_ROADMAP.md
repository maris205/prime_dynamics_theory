# Roadmap after RH-315

One moment can now be corrected without changing any lower moment, using a
legal integer-multiplicity normal spectrum inside the modulus cap.  RH-316
iterates these triangular packets to realize every finite deterministic
prefix exactly.
