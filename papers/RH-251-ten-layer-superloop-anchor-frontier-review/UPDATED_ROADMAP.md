# Roadmap after RH-251

## Current coordinate

```text
exact_superloop_quotient_frozen_anchor_class_obstructed_open_new_selector_uniform_tail
```

## What remains alive

1. The exact fixed-noise superloop and orthogonal quotient identities.
2. The independent deterministic numerator anchor dictionary.
3. The RH-246 block-power theorem as a precise uniform-envelope target.
4. The RH-250 finite-head/analytic-tail gluing interface.

## Route firewall

- Do not rerun zero-target prefixes and call them anchored.
- Do not keep reweighting the same frozen shell window: its full single-use
  zonotope is dual-certified to fail, and unbounded weights are pathological.
- Do not majorize physical and atomic sectors separately; the Perron term
  forces rate `r_H^{-1}>1`.
- Do not promote the 17-endpoint block diagnostic to a uniform theorem.
- Do not invoke RH-240 until an anchored head, uniform quotient tail, and
  deterministic target tail are all certified.

## Reopening triggers for RH-252+

A new batch should pursue one of the following genuinely new inputs:

1. an expanded resolved candidate window with an anchored reachability audit;
2. a signed/complex selector derived from invariant quotient structure rather
   than arbitrary shell weights;
3. a uniform small-noise theorem for one quotient block power;
4. an analytic all-order tail bound for the deterministic numerator target.

Gates A--E remain open.
