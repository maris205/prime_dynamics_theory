# TPC-294 route evaluation

The Session-named `propose.md`, `skills/route-a-evaluator.md`, and
`skills/route-b-evaluator.md` are absent from this checkout.  Accordingly no
official Route-A or Route-B pass is claimed.  The available fail-closed local
fallback is:

- direct proofs of the identity, global finite enumeration, Gray update, and
  Gram nonnegativity;
- a canonical exact-rational producer and payload hash;
- an independent source-first physical replay;
- an adversarial Gray/brute/PSD stress test; and
- a Bridge-B checker that runs normal and optimized variants.

Route position: this release closes the finite weighted-sign question on the
declared grid, but pays no arithmetic credit.  The strongest reusable object
is now a weighted shell Rayleigh optimizer; its source-image feasibility and
growing behavior are the next gates.

```text
ROUTE_A = NOT_EVALUATED_OFFICIAL_FILE_ABSENT
ROUTE_B = LOCAL_FAIL_CLOSED_CERTIFICATE_ONLY
FIXED_POWER_CREDIT = 0
FULL_GATE_B = OPEN
TWIN_PRIME_RESULT = NONE
```
