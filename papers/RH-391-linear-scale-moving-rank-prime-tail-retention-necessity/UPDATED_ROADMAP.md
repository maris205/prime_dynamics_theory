# RH-391 updated roadmap

## Route A: GO -- linear-scale same-rank pair necessity

RH-391 closes the executable route

    Maynard bounded consecutive gaps
      -> finite-pigeonhole fixed h_* extraction
      -> exact strict successor at one common rank
      -> linear-rank smooth interval control
      -> elementary integer tails
      -> uniform gamma_r lower bound
      -> common-head Taylor lift
      -> natural two-scale pair inequality
      -> finite-slope, coarse, and sublinear profiles.

The same exact `r` is used at both endpoints, with `r->infinity` and
`r<=C*x`.  This closes moving-rank necessity throughout `r=O(x)` for the
frozen `P/J/I` hierarchy without using `P_r~K_r` at linear rank.

## Route B: GO -- exact profile and next-rank separation

The natural edge factors

    a=(x^2/(q^2-1))^r, rho=(x/q)^(2r)

retain the asymmetry between left and right endpoint normalizations.  If
`r/x->lambda`, the pair lower profile is exactly
`a_0/(1+a_0)` with `a_0=exp(-2*lambda*h_*)`.  General linear ranks have
the coarse lower `exp(-1200*C)/2`, and sublinear ranks have `1/2`.

The integer upper bound for `P_(r+1)` proves unbounded separation from the
next prime-tail rank for scalar and gamma-normalized endpoint pair errors.

## STOP_SCOPED boundary

Stop rather than extrapolate if a consumer requests:

- a single-vertex conclusion for an arbitrary rank schedule;
- different ranks at the two endpoints;
- an obstruction for an arbitrary surrogate;
- a superlinear theorem with `r/x->infinity`;
- a linear-rank prime-tail asymptotic `P_r~K_r`;
- complex channels or an active `c11`;
- ordinary Cesaro, a growing clock, or prefix-dependent `K_N`;
- an RH-389, TPC-137, or Tao active-log dependency;
- an operator, trace, zero-divisor, or RH conclusion.

## Source route

The proof-minimal closure inherits 97 Git objects from frozen RH-390
release `a3aa5977e9b3338e4c3035c6c42b60d50bc3ac3b`, in groups `87+8+2`,
plus two ordered remote logical locks.  Maynard supplies the bounded-gap
input.  Johnston--Yang is provenance-only for this theorem.  Neither
remote payload is vendored.

## Artifact and release route

Release completion requires simultaneous agreement of:

1. 60 independently checked rows and 24 semantic mutations;
2. fresh result and recursively closed official Draft 2020-12 schema;
3. 97 release-bound Git objects and two ordered remote logical locks;
4. two default-offline verifiers making zero network requests;
5. byte-identical frozen main and semantic PDFs;
6. a fixed 34-member publication manifest plus outer verification;
7. exact Stage 1 and manuscript hashes;
8. rights/nonvendoring and recursive exclusion of all four payload hashes;
9. theorem, integrity, review, format, source, visual, and replay audits.

Every finite row remains exact reproduction, not analytic proof.
