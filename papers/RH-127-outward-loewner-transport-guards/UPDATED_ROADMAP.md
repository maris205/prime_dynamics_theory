# Roadmap after RH-127

The validation logic is now complete.  The next paper should state the exact
eventual-support theorem from a contractive affine gamma recurrence and a
positive normalized-frame liminf.  The final layer will audit which inputs
are algebraic, validated, finite-only, or still physical.

