[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rh161-packet-riesz-relative-determinant-assembly"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["matplotlib"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
