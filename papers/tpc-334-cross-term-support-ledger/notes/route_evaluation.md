# TPC-334 route evaluation

The local fallback consists of the source producer, independent support
replay, exact support proof package, mutation stress, and fail-closed
Bridge-B wrapper.

The Session files `propose.md`, `skills/route-a-evaluator.md`, and
`skills/route-b-evaluator.md` are absent in this checkout:

```text
Route-A = NOT_EVALUATED_OFFICIAL
Route-B = LOCAL_FAIL_CLOSED_FALLBACK
ARITHMETIC_ADVANCE = NO
FIXED_POWER_CREDIT = 0
FULL_GATE_B = OPEN
```
