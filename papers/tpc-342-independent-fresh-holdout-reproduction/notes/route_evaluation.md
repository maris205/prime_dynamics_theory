# TPC-342 route evaluation

The available evidence is:

- an exact finite orthogonal-projection identity;
- a TPC-341 protocol-locked producer on three disjoint new windows;
- a reverse-shell independent replay;
- six fail-closed mutation tests;
- a compiled and audited PDF; and
- a local Bridge-B wrapper with normal/optimized equality.

The Session-named propose.md, skills/route-a-evaluator.md, and
skills/route-b-evaluator.md files are absent in this checkout.  Therefore:

~~~text
Route-A = NOT_EVALUATED_OFFICIAL
Route-B = LOCAL_FAIL_CLOSED_FALLBACK
ARITHMETIC_ADVANCE = NO
FIXED_POWER_CREDIT = 0
FULL_GATE_B = OPEN
~~~

The strongest positive result is independent finite replication of the
aggregate-versus-holdout split.  The strongest obstruction is that all 27
held-out tests remain above the predeclared residual guard.  The next clue is
cross-panel pooling, not an arithmetic promotion.
