# Roadmap after RH-130--RH-139

## Revised mathematical frontier

The exact-matrix directional route now needs only:

1. an eventual controlled envelope with `limsup y_n <= y_* < 1`;
2. a positive normalized-base liminf `liminf a_n >= a_* > 0`.

Per-step subunit `rho_n` is no longer required.  Isolated expanding steps may
be crossed if the incoming envelope remains inside their safety radius.

## Validation additions

- Common route: exact or interval enclosure from the source model through packet assembly.
- Independent route: the same source connection plus all-level outward radii and residual guards.

## Next research block

1. Intervalize the source snapshots and packet/frame construction.
2. Prove an all-level block/reset or delayed-start viability policy.
3. Attack the weakest-direction base liminf without splitting volume and capacity.
4. Determine whether the two coarse superunit births disappear after an eventual start.
5. Repeat the route review at RH-149 and the larger program review every fifty papers.

## Claim boundary

The 28/30 finite certificate is strong evidence about this frozen packet
family, not an asymptotic Stage A or later spectral-identification theorem.
