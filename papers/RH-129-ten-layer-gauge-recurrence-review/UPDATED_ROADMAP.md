# Roadmap after RH-120--RH-129

## Priority 1: remove the finite conditioning ambiguity

Rebuild the RH-121/RH-125 cross-scale audit through one common high-precision
or interval assembly, without adding a positive Gram floor.  Work on the
actual supported subspace and attach RH-127 radii.  This decides whether the
encouraging `24/24` finite directional chains survive as genuine finite
certificates.

## Priority 2: derive a natural dynamical gauge

Use the memory recursion and packet update to transport frames directly.
Avoid choosing an arbitrary exact-Gram optimizer after the fact.  The target
is source-level bounds for the RH-123 coefficients `rho_n` and `q_n`.

## Priority 3: prove the normalized-base liminf

Control `V_n/(L_n^4 C_n)` on the same assembly path.  RH-124 warns against
splitting volume and capacity unless their common correlation is retained.

## Holds

- Keep scalar-profile direct recurrence on hold unless an operator-valued
  alignment law appears.
- Keep trace-concentration on hold without a new physical trace source law.
- Do not infer all-level coefficients from the four finite scale edges.
- Do not advance Hilbert--Polya or zeta-zero language before the RH-128
  physical packet is actually proved.

