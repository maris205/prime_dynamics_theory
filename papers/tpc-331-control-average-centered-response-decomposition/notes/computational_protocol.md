# TPC-331 computational protocol

1. Verify the hash-locked TPC-330 producer and certificate.
2. Freeze the TPC-330 two-origin/two-scale panel, four laws, and five controls.
3. Rebuild each finite source vector and coherent matrix in the producer.
4. Form the five placed vectors, their mean, and centered vectors.
5. Compute `E`, `D`, and `O` separately for average, coherent, and centered
   components; never average ratios.
6. Classify with the outward ratio guard `5e-8`.
7. Check the exact rational anchor and canonical JSON.
8. Replay in reverse shell order with the independent checker.
9. Run mutation stress and Bridge-B in normal and optimized modes.

The largest float64 identity residuals are recorded in the certificate; the
symbolic anchor carries the exact theorem evidence.
