# RH-122 theorem ledger

## Proved

- Sharp best constants for the identity-gauge Loewner comparison.
- A swapped-axis family with `gamma'=gamma` but fixed transfer factor
  `sqrt(b/a)=1/epsilon`.
- Exact recovery by the coordinate-swapping gauge.

## Audited

- 512 anisotropies from `1e-1` to `1e-14`, plus 64 smoke values.
- Zero formula, invariance, or gauged-recovery failures.

## Boundary

The result rules out an information class.  It supplies no physical
cross-level gauge and no Stage A, Hilbert--Polya, zero, or RH conclusion.

