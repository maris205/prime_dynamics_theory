# TPC-353 paper plan

## Question

After TPC-352 froze the finite reciprocal-incidence branch, can the actual
finite V59 residual be attached to the literal two-endpoint masked operator in
a way that exposes the missing arithmetic `L2` term without replacing it by a
source-only or unmasked proxy?

## Frozen protocol

Use the inherited V59 declared model
`beta(t)=Lambda(t+2)-b^(2)(t)` and the literal deleted-diagonal shell operator
with both divisibility masks retained.  The new panel has origins
`6001,8001,10001`, source counts `256,512,1024`, shell anchors `24,54,80`,
kernel exponents `1,2`, height `66`, and the four predeclared laws
`all_plus`, `alternating_index`, `mod4_character`, and `half_split`.  This
gives `216` law-level rows.

## Claim-bearing components

1. Prove the finite operator polarization identity for `beta=Lambda-b`.
2. Prove the normalized Cauchy envelope for the residual output fraction.
3. Rebuild all rows and compare source-level and output-level polarization.
4. Use a separate reverse-shell implementation and mutation stress suite.
5. Keep the arithmetic gate, fixed-power credit, Route-B reassembly, and
   twin-prime endpoint explicitly open.

## Expected decision rule

If output alignment is consistently positive, record a finite attachment and
ask whether it survives a genuinely higher-origin panel.  If signs vary,
record the smallest finite obstruction and move to a position-aware bound.
Neither outcome is an asymptotic theorem.
