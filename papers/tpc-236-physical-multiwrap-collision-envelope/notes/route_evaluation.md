# TPC-236 route evaluation

## Route A

`(A0,A1,A2,A3,A4)=(false,false,false,false,false)`. Route A is not addressed.

## Route B

- `L0`: source-valid physical `h`-fiber support compiler with explicit `C_h`.
- `L1`: exact gcd-fiber multiplicity theorem and weighted Bessel envelope.
- `L2`: `NONE`.
- full Gate B: `OPEN`.
- strict `1/400`: `UNPAID_GLOBAL`.

## Extracted state

- strongest positive result: unnormalized physical-fiber Bessel factor `8Q^2/H`, sharpened at V59 to `(4+o(1))x^(1/96)`.
- strongest obstruction: exact Q101 triple collision refutes multiplicity two; V59 toll is `x^(1/96)`.
- open theorem: cross-`h` reduced-frequency reassembly with cancellation in explicit `C_h`.
- reusable structure: gcd-fiber reduction `h -> h/gcd(a,h)` and coordinate Bessel compiler.
- `ROUND2_CLUE`: `COMBINE_PHYSICAL_H_FIBER_ENVELOPE_WITH_REDUCED_FREQUENCY_LARGE_SIEVE_AND_TEST_C_H_WEIGHTED_CANCELLATION`.

## Verdict

`CONTINUE_WITHIN_FAMILY`.  The physical single-fiber compiler is real progress, but
`ARITHMETIC_ADVANCE=NO` because the `x^(1/96)` multiplicity toll and cross-`h`
reassembly remain unpaid.
