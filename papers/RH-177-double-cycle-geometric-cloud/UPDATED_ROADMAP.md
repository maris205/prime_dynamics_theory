# Roadmap after RH-177

```text
exact doubled geometric determinant           [proved]
  -> orientation-separating marked traces      [next]
  -> clock-rank / cycle-length calibration      [open]
  -> physical cycle-to-transfer realization    [open]
  -> Riesz shell and complement limits          [open]
```

The scalar determinant does not determine the time arrow.  The next paper
must add a stable directed marker without changing the cloud determinant.
