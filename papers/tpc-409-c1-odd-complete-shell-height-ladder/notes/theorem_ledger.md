# TPC-409 theorem ledger

- `Q=65536`, complete shell count `r=5709`, all shell primes retained.
- Heights are `H=16,32,66,128`, with `N=4H`.
- The odd/even index counts are `m_minus=2854`, `m_plus=2855`.
- Exact conclusion in every row: `0<=z<=t1/(a_min sqrt(S0 S1))<=4/(a_min H)<=4/H`.
- Scope is finite and proxy-level; full operator, physical, arithmetic,
  fixed-power, Route-B, and twin-prime gates remain unpaid.
