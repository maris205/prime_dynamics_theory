# TPC-227 source lock

## Physical V59 objects

The repository record
`research/tpc-big-road/bridge_b_polarized_local_bdh_scalar_compiler.md` freezes

```text
a_n^(j) = beta(n) + i^j w(n)
C_x = 1/4 sum_j i^j V_Q,H^circ(a^(j))
```

and defines every `V_Q,H^circ` with the same integration profile `psi_+(v)`.

## Structural lift

`research/tpc-big-road/bridge_b_prime_shell_packet_lift.md` permits arbitrary bounded
`psi_j` in a finite structural row family. That declaration is a modeling lift and does
not identify `psi_j` with the literal source packets.

## Finite witness

TPC-226 supplies the exact resonance

```text
Q=25, h=400, p=37, r=47, multipliers 3 and -7.
```

TPC-227 uses only its one-coordinate aligned/odd synthesis block. No asymptotic or
arithmetic conclusion is imported.
