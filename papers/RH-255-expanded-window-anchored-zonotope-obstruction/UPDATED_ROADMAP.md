# Roadmap after RH-255

Doubling the resolved window improves the anchored distance at every endpoint
but does not rescue any single-use shell selector.  The entire expanded box is
outside tolerance, so further prefix, interval, or binary scans of this window
would be redundant.

The next live route is a signed or complex selector derived from invariant
quotient structure.  Arbitrary signed least squares is not enough: RH-256 must
separate algebraically forced quotient coefficients from unconstrained moment
fitting.

Route coordinate:

```text
expanded_single_use_shell_class_obstructed_open_invariant_signed_complex_selector
```

Gates A--E remain false/open.
