[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rh371-eight-run-distance-two-capacity"
version = "0.1.0"
description = "Eight-run reduction and cyclic pair-ledger obstruction for distance-two Mobius capacity"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
