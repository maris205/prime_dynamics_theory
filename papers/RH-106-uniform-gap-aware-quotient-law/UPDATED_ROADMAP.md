# Route update after RH-106

## Quotient gate status

The abstract gap-aware quotient law is now explicit:

```text
local price = c^2/g,
total price <= candidate count * propagation factor * sup(local price),
total price <= stopped allowance  =>  no stop and endpoint safety.
```

If the total-price inequality fails, the stopped prefix remains safe. This
separates two questions that were previously conflated:

1. uniform endpoint safety, now closed abstractly;
2. uniform physical supply/density of affordable quotients, still open.

## Current Stage-A frontier after RH-104--RH-106

- `P`: exact source-weighted finite-prefix target identified; physical
  all-level law open.
- `Omega*tau`: signed cancellation algebra closed; physical observation and
  residual exponents open.
- `Q`: abstract price/no-stop/stopped theorem closed; physical exponents
  `(chi,gamma,kappa,s)` open.
- `Z` and the inherited upstream bridge remain separate scalar leaves.

## Recommended next layer

RH-107 should attempt to derive one physical exponent pair for the quotient
price, preferably a direct all-level estimate of `c_sigma^2/g_sigma` on the
source-seeded weak-mode events. A fixed gap theorem is not the target. If no
such structural estimate is available, return to the RH-104 physical prefix
Gramian rather than reopening moving-cloud or zero-identification work.

The route has not reached unconditional Stage A. Hilbert--Polya, arithmetic
traces, zero identification, and RH remain deferred.
