# RH-375 repository-locked integrity audit

This audit applies the ARS integrity and seven-mode failure checklist within
the governing repository-only source policy.  It does **not** claim a fresh
web or publisher re-verification.  The Davenport and Mirsky metadata are
reused exactly from the source-locked RH-374 bibliography; repository papers
and results are frozen by SHA-256, and source commits by Git object IDs, in
`results/result.json`.

## Claim and source alignment

| High-impact claim | Evidence | Verdict |
|---|---|---|
| The fixed-`q` limit exists | RH-372 one-site AP theorem; proof restated in `main.tex` | VERIFIED |
| `F(q)` is a weighted phase MWIS | Active-set equivalence and two realizing orientations | VERIFIED |
| `q|Q` implies `F(q)<=F(Q)` | Literal period lift plus residue-density aggregation | VERIFIED |
| Same-support multiples satisfy `F(Q)=B_y` | Uniform weight scaling and separate even/odd zero-split counts | VERIFIED |
| The cover argument applies to arbitrary finite `q` | Prime-support factorization of `lcm(q,q_y)` | VERIFIED |
| `sup_q F(q)=B_infinity` | Cofinal lower family plus the preceding arbitrary-`q` upper bound | VERIFIED |
| No finite clock attains the supremum | RH-374 strict `B_y<B_infinity` combined with `F(q)<=B_y` | VERIFIED |
| Finite scan is not theorem evidence | Explicit diagnostic label in manuscript and result ledger | VERIFIED |

All body citations have bibliography entries, and every bibliography entry is
cited.  The exact data rows are regenerated by `experiments/build_result.py`;
the individual publication archive freezes every declared input and output.

## Seven AI-research failure modes

| Mode | Status | Repository-locked evidence |
|---|---|---|
| 1. Implementation bug passing self-review | CLEAR | Fraction arithmetic, independent factor/subset exhaustions, aggregation checks, and test suite agree |
| 2. Hallucinated citation | CLEAR (repository-locked) | Davenport, Mirsky, and RH-366--RH-373 metadata are inherited from locked sources; the RH-374 title/release is checked against the frozen RH-374 inputs; no unauthorized external-verification claim |
| 3. Hallucinated experimental result | CLEAR | Every numerical row is generated and archived; there are no empirical observations or fitted results |
| 4. Shortcut reliance | CLEAR | The all-clock conclusion uses the cofinal proof; the bounded scan is not used as evidence |
| 5. Bug reframed as insight | CLEAR | No surprise-based inference; exact identities precede diagnostics |
| 6. Methodology fabrication | CLEAR | Manuscript audit protocol matches the executable scripts, caps, and exact output ledger |
| 7. Early frame-lock | CLEAR | The theorem answers the frozen all-clock one-site question and explicitly leaves memory/adaptive classes open |

The adversarial scope check also rejects four tempting promotions: a general
cyclic-cover MWIS law, a growing-clock selector, an adaptive-capacity limit,
and any operator/trace/zero/RH interpretation.  Route A is therefore `GO`
only in the declared scalar one-site data type; Route B is `STOP_SCOPED`.
