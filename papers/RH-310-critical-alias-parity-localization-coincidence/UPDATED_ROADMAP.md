# Roadmap after RH-310

The typed route can reopen only through a joint first-alias boundary-layer
trace theorem; pre-alias localization and separate parity/shell majorants
cannot supply it.  The direct route can reopen through endpoint `H2`
convergence.  RH-311 compares these two remaining inputs without merging
their typed ledgers.
