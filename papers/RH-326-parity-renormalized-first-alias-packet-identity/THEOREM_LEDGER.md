# Theorem ledger

| Claim | Status |
|---|---|
| Exact Hardy-scaled raw-trace plus parity decomposition | Proved |
| Exact radial-correction plus roots-of-unity alias decomposition | Proved |
| Exact first-alias residual with the even parity sign retained | Proved |
| Uniform quadratic remainder for the moving-order parity expansion | Proved |
| Alias and parity have common weighted exponent `log(beta R)/log(lambda)` | Proved; displayed decimal is diagnostic |
| Phase law `parity/alias -> C_* C_M lambda^eta` | Proved |
| Canonical `|eta| <= 1` scalar-only obstruction | Proved conditionally on `C_* C_M lambda < 1`; archived decimals do not certify the hypothesis |
| `eta_* > 1` or `C_* C_M lambda < 1` from archived decimal values | Not certified; ordinary floating-point diagnostic only |
| Clearance phase and retained `(V,U,W)` frame | Recorded in a typed RH-327 interface |
| Local forward-probability packet identified with the raw trace packet | Open; the trace observation is missing |
| Neighboring shell and joint alias/parity/shell trace law | Open; assigned to RH-327 and RH-328 |

Gates A--E remain false/open.
