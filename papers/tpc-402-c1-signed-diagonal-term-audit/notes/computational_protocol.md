# Computational protocol

The producer uses exact `Fraction` arithmetic, two sign laws, six origins,
five positions, and all 872 shell primes. The independent checker reads a
canonical certificate, checks both law row counts, and replays the shell in
reverse order. The stress check mutates three contract fields. No floating
eigensolver or numerical certification is used.
