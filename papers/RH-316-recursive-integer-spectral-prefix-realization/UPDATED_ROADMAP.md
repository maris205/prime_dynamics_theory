# Roadmap after RH-316

The deterministic prefix has a genuine finite spectral realization at every
order.  The next issue is quantitative: determine the least possible rank and
squared mass.  Those costs decide the endpoint approximation clock.
