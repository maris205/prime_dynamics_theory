# Route Evaluation

## Route A

```text
A0 = NOT_APPLICABLE
A1 = NOT_APPLICABLE
A2 = NOT_APPLICABLE
A3 = NOT_APPLICABLE
A4 = NOT_APPLICABLE
```

This remains the analytic prime/twin-prime Route-B session, not the dynamical
spectral Route-A family.

## Route B

```text
STRUCTURAL_THRESHOLD_A = PASS
PHYSICAL_PROFILE_EMITTER_PULLBACK = PROVED_EXACT
RESIDUE_LIFT_GCD_ALIASING = PROVED_EXACT
CROSS_DIVISOR_FREQUENCY_GRAM = PROVED_EXACT_FINITE
PHYSICAL_DIRECT_SUM_REPLACEMENT = REFUTED_SCOPED
LITERAL_V46_ASYMPTOTIC_GRAM_BOUND = OPEN
PRIME_SHELL_REASSEMBLY = OPEN
FULL_GATE_B = OPEN
STRICT_1_OVER_400 = UNPAID
ARITHMETIC_ADVANCE = NO
L2 = NONE
FIXED_ATOM_CREDIT = 0
```

## Strongest positive result

The literal residual can be pulled back to one common physical support before
any outer absolute value, and its cross-divisor Gram is exactly a frequency
intersection sum.  The residue-lift cross blocks are governed by gcd/lcm
compatibility rather than orthogonality.

## Strongest obstruction

The cross-divisor terms are not automatically a saving: the exact unit-weight
fixture has positive off-diagonal Gram values `560` and `770`.  Therefore a
future estimate must exploit the actual smooth weights, signs, and profile
structure; it cannot infer cancellation from coupling alone.

## Open theorem

Prove a fixed-power bound for the literal joint pullback kernel in the V46
transition window, retaining `psi`, `mu(d) log(d)/d`, four-packet signs, the
zero-axis, and the prime shell.

## Reusable structure

```text
common physical support
  -> residue lift C_d
  -> additive transform
  -> reciprocal emitter pullback K_d
  -> common-source collapse K=sum_d K_d
  -> gcd/lcm lift aliasing and frequency-intersection Gram
  -> literal joint Gram theorem required
```

## ROUND2_CLUE

Use the frequency-intersection formula to group nested divisor pairs by a
shared rational frequency before any `d`- or `q`-triangle.  Test whether the
`mu(d) log(d)` signs and the V46 smooth `psi` produce a signed cancellation on
these shared-frequency clusters; if not, record the resulting positive-Gram
obstruction and pivot to prime-only reassembly.
