# Roadmap after RH-170

The all-level `R` target must be shellwise:

```text
for every fixed cloud shell m:
    same-rank Riesz lift + common-coordinate summable transport
then:
    coherent finite partial clouds
```

It must not demand operator-norm convergence of the full rank-growing cloud
projection.  RH-171 should assemble RH-162--170 into one conditional
physical-R closure theorem, audit the remaining physical leaves, and choose
the true next target.
