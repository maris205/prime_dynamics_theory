# RH-364 theorem ledger

| Item | Status | Exact scope |
|---|---|---|
| Four-volume foundation | FROZEN / PRESERVED | RH-1--RH-361 and the four-volume outer replay remain unchanged. |
| RH-363 endpoint | PROVED UPSTREAM / LOCKED | RH-363 remains the immediate numbered input and closes no Gate. |
| Certified survivor | PROVED UPSTREAM / LOCKED | The four rational state rectangles support a local mixing uniformly hyperbolic survivor conjugate to the subshift of `A`; no global Hénon nonwandering-set claim is made. |
| Exact symbolic traces | PROVED | `F_n=tr(A^n)=Lucas_n+2 cos(n pi/2)` and `p_n=(1/n) sum_(d|n) mu(d)F_(n/d)`. |
| Primitive-count envelope | PROVED | `0<=p_n<=F_n/n<=4 phi^n/n`. |
| Multiplier envelope | PROVED UPSTREAM / SPECIALIZED | Every primitive survivor orbit satisfies `L_o>=kappa^(n_o)`, `kappa=773/224`; `L_o` is a Hénon monodromy multiplier, not an eigenvalue of `A`. |
| Weighted Euler disk | PROVED | For real `beta>=0`, `Z_beta` and `D_(E,beta)` converge normally and are zero-free on `|z|<kappa^beta/phi`, with the explicit primitive-period tail in the manuscript. |
| Flat correction | PROVED SCOPED | The correction product defining `C_F` is analytic and zero-free on `|z|<kappa^2/phi`; the quotient identity `C_F=D_F/D_(E,1)` is directly licensed only on the common disk `|z|<kappa/phi`. |
| Reported root near `3.429` | NOT CERTIFIED | It lies outside the weighted Euler certified disk `kappa/phi=2.132769...`; the correction theorem does not continue it. |
| Prime-copy Schatten law | PROVED | `T_s=direct_sum_ell ell^(-s)A` is bounded iff `Re(s)>=0`, compact iff `Re(s)>0`, and belongs to `S_q` iff `q Re(s)>1`. |
| Ordinary Fredholm identity | PROVED IN DECLARED REGION | On `Re(s)>1`, `det_F(I-T_s)=prod_ell det(I-ell^(-s)A)=prod_n zeta(ns)^(-p_n)`. |
| Scalar continuation | PROVED SCOPED | The zeta-factor scalar expression is meromorphic on `Re(s)>log_2(phi)`; below `Re(s)>1` it is not promoted to an ordinary Fredholm determinant. |
| Regularized determinants | PROVED | For integer `m` and `m Re(s)>1`, the branch-free block product defines `det_m(I-T_s)`. |
| First arithmetic trace defect | PROVED | `(F_1,F_2,F_3)=(1,1,4)`: primes and squares match, cubes carry `4 log p` instead of `log p`. |
| Natural weighted singularity | PROVED SCOPED | For real `beta>0`, the unnormalized prime lift has fractional exponent `L_*^(-beta)` at `s=1` and no meromorphic crossing there; `beta=0` has a simple pole. |
| Common scalar normalization | PROVED SCOPED NEGATIVE | `Q_1=1` forces `c=L_*^beta`, then `Q_2=1` but `Q_3=1+3(L_*^3/L_3)^beta>1` for every real `beta>=0`. |
| Normalized near-`s=1` product | PROVED ONLY BELOW THRESHOLD | Current all-order bounds license it for `0<=beta<beta_0=0.290834898770...`; above that threshold only the exact coefficient/far-right statement is claimed. |
| Prime copy as arithmetic dynamics | FALSE / NOT CLAIMED | The copy is not a finite-field reduction, Hasse--Weil factor, full-map zeta, or canonical global Hénon operator. |
| Original physical triggers 1--4 | UNTOUCHED | No `D_(4k)`, typed `q/E_off`, complete unnormalised prefix, or RH-241 noisy bridge is supplied. |
| Gates A--E | FALSE/OPEN | Gate A fails at the engineered-copy data type; Gate D also fails exactly at cubes; no other Gate closes. |

Finite computations are reproduction checks only.  No finite root table is
used as evidence for analytic continuation or an RH conclusion.
