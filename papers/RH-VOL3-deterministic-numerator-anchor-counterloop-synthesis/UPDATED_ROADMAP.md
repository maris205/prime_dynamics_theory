# Roadmap after Volume III

The deterministic target side is closed at its declared coefficient and
counterloop types. Further finite target fits, larger unstructured selector
searches, or looser Cauchy bounds do not change the frontier.

Admissible new inputs are:

1. a legal operator-derived anchored head outside the excluded selector classes;
2. an exact actual cloud-to-deterministic coefficient bridge;
3. aggregate noisy-cloud transport into the monodromy shell;
4. common-space `S2` control, a moving isolating contour, and a contractive
   power for the variable-rank quotient;
5. an independent proof that the graded counterterm is canonical for the
   physical noisy dynamics.

The route coordinate is

```text
deterministic_all_order_closed_actual_cloud_identification_open
```

Deterministic `s` remains distinct from the actual noisy head `h`. Gates A--E
remain false/open.
