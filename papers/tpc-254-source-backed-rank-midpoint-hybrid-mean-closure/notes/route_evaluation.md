# Route Evaluation

## Strongest positive result

For every fixed finite admissible `K` and every fixed `M>0`, the literal
hybrid residual obeys

```text
|<z_mid,w>| <<_(M,K) x^(1/2)(log x)^(-M).
```

This is a source-backed arithmetic advance on the exact hard rank-midpoint
test, obtained from maximal interval Type I rather than a whole-shell mean.

## Strongest obstruction

Every remaining midpoint transfer contains the unestimated literal form

```text
<A_x^*z_mid,beta>.
```

Norm-only control is sharp even for real zero-diagonal matrices, and no
frozen V21/TPC theorem has this test type.

## Open theorem

Estimate the exact literal `beta`-linear form against `A_x^*z_mid` on the same
V59 clock, retaining all kernel factors and improving on Cauchy at a declared
natural scale.

## Reusable structure

```text
hybrid maximal interval Type I
-> nonnegative m=1 extraction
-> deterministic child means
-> normalized Haar moment
-> push fixed output test through A_x^*
-> one literal beta-linear form
```

## ROUND2_CLUE

```text
PUSH_THE_FIXED_RANK_MIDPOINT_HAAR_TEST_THROUGH_A_X_STAR_AND_ESTIMATE_THE_LITERAL_BETA_LINEAR_FORM_ON_THE_SAME_CLOCK_BEFORE_ANY_COVARIANCE_OR_MARGIN_PROMOTION__DO_NOT_REUSE_WHOLE_SHELL_OR_AP_AVERAGES
```
