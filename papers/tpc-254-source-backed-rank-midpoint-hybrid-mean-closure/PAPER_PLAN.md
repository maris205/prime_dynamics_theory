# Paper Plan

## Question

TPC-253 isolated two rank-midpoint moments. Determine which one is already
paid by the frozen source corpus, without replacing the hard rank test by a
whole-shell or arithmetic-progression average.

## Contribution sequence

1. Freeze finite admissible `K`, the cutoff `Z_x=(log x)^K`, and the ordered
   rank children for arbitrary real `x`.
2. Extract the nonnegative unit-weight `m=1` row from the source-backed
   maximal Type-I theorem at `gamma_0=1/4`.
3. Pay both child sums, divide by their exact cardinalities, and apply the
   normalized Haar identity to prove arbitrary fixed logarithmic saving for
   the literal `w` moment.
4. Record the full quantifier order and prove that arbitrary fixed logarithmic
   saving cannot be promoted to a fixed power saving.
5. Push the second moment through `A_x^*`, retain only Cauchy, and type-audit
   every nearby source.
6. Prove a synthetic real zero-diagonal derangement obstruction and exact
   `N=2` sharpness of the Cauchy constant.
7. Release an exact finite certificate, an independent mutation checker, a
   192-family stress suite, and a compiled paper.

## Claim discipline

The maximal Type-I estimate is imported from the frozen analytic compiler;
TPC-254 proves a corollary attachment, not that upstream theorem. Finite
scripts check algebra, endpoint rules, quantifier schema, adjoint orientation,
and synthetic controls only. They do not sample the physical residual and do
not validate an asymptotic estimate.

## Exit theorem

The `w` lane obtains

```text
|<z_mid,w>| <<_(M,K) x^(1/2)(log x)^(-M)
```

for every fixed `M`, while the literal `g=A_x beta` lane is reduced exactly to
the open linear form `<A_x^*z_mid,beta>`.
