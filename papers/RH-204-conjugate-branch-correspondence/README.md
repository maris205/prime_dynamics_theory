# RH-204: conjugate branch correspondence

Reducing each real nonreal quartet to its two upper-half-plane
representatives gives a unique adjacent-level matching in all four physical
cases. The same branch labels synchronize the left and right channels to
within `0.007578`. This is a finite spectral positive despite failed raw
state transport.
