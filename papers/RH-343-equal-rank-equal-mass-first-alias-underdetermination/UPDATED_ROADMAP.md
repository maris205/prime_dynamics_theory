# Roadmap after RH-343

RH-343 closes one coarse-data inference and nothing stronger.  Even after fixing
the rank, squared spectral mass, a common root cap and maximum modulus,
simple conjugation-closed normal realizability, every pre-alias moment, and
all eventual fixed-order data, the moving strict-prefix budget at `u=4k`
can be exactly zero or can diverge.  Therefore those invariants cannot replace
an actual alias-inclusive head-transport theorem.

The result is deliberately an information-class theorem.  It does not
produce two physical noisy operators and does not decide the actual
`D_(4k)`.  A future actual rank cap

    r_sigma <= 2k-2

would exclude both examples immediately, because each has rank `6k-2`.
Likewise, a genuine actual root-matching theorem, a moving weighted moment
estimate, or direct annular convergence would add information outside this
counterexample class.

The head checkpoint after RH-343 therefore remains:

    no actual head transport
      => no p/q strict-prefix equivalence claim.

The next default route is the RH-344 critical combined-complement
decomposition on the corrected physical Hardy data type.  It must freeze the
actual order-`2k` components before evaluation and isolate the signed diffuse
families capable of compensating the mandatory boundary-orbit atom.  It may
not substitute these model shells for the physical complement.

Independent reopen inputs for the head route remain:

- an actual spectral-head rank theorem on the physical `k` clock;
- an actual zero-padded root matching with the required cap and rate;
- a genuine moving-order aggregate theorem for the strict prefix; or
- direct annular convergence of the physical logarithmic mismatch.

Until such an input appears, alias-inclusive physical head transport is
`NOT_TESTABLE`/open.  RH-288 remains inactive and Gates A--E remain false.
