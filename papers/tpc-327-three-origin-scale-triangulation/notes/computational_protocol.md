# TPC-327 computational protocol

1. Verify the locked TPC-326 producer/certificate and the TPC-325 certificate.
2. Freeze origin `20001`, the four nested intervals, shell anchors, exponents,
   sign laws, and inherited envelope thresholds before recomputation.
3. Recompute the full 32-row panel and write canonical full-row JSON.
4. Reconstruct every row independently from the literal block formula using
   reverse Gram accumulation and compare labels, outward intervals, trace
   census, and exact anchor.
5. Pool the three released scale ladders and recompute each per-scale origin
   range independently.
6. Stress disjointness, nesting, one-step residue perturbation, nonzero
   origin spread, threshold compliance, and the claim firewall.
7. Require empty stderr and byte-identical normal/optimized output in Bridge-B.
