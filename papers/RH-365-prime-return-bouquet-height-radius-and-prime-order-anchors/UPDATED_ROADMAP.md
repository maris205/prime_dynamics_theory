# Roadmap after RH-365

RH-365 turns the RH-362 formal marked-cycle bouquet into a genuine analytic
germ for the specific reversing-axis seed P0=(0,0).  It does not alter the
physical route coordinate

    actual_same_clock_unnormalized_head_transport_open

and it closes no Gate.

The theorem separates two arithmetic facts.

1. Reversibility compresses every gcd term to one midpoint coordinate
   difference.  Escape then supplies a double-exponential height upper bound,
   an O(n 2^(n/2)) fixed-point envelope, and a positive zero-free disk.
2. Every odd prime order has at least one primitive divisor, but the argument
   uses primality of the order.  It gives no composite-order Zsigmondy
   theorem.

The shortest genuine continuations are:

1. **Composite primitive-divisor route.** Prove an eventual new-prime theorem
   or a p-adic lifting estimate strong enough to force primitive divisors at
   specified composite orders.
2. **Sharp radius route.** Obtain lower information on the rank multiplicities
   c_n sufficient to determine the exact origin Taylor radius or a genuine
   boundary theorem.  The present height argument is only an upper envelope.
3. **Finite entropy-data route.** Quantify finite recovery or exclusion of
   prime/rank prefixes from the RH-363 entropy samples.
4. **Intrinsic globalization route.** Replace the marked disjoint union by a
   source-backed compact or trace-ideal global operator whose local factors
   arise intrinsically.
5. **Original physical route.** Return to same-clock unnormalised head
   transport, typed q/E_off, the complete direct prefix, or the RH-241 moving
   noisy envelope/coefficient bridge.

Do not call a primitive Euler exponent a raw zeta coefficient.  Do not
promote the origin radius bracket to analytic continuation or a natural
boundary.  Do not reinterpret fixed-point counts as a trace-class
Hilbert-space trace.  Gates A--E remain false/open.
