# RH-342 theorem ledger

| Item | Status | Exact scope |
|---|---|---|
| Actual common-Hardy head | SOURCE LOCKED | Algebraic nonperipheral eigenvalues of `K_sigma/r_H` with modulus `>q=1/2`, counted with algebraic multiplicity. |
| Graded counterloop shell | PROVED EXACTLY | `Y_k={beta_k exp(+-ij pi/k):1<=j<=k-1}`, rank `2k-2`, with the displayed exact power ledger. |
| Strict first-alias prefix | PROVED EXACTLY | `2<=n<4k` contains `2k` and excludes `4k`. |
| Genus-one quotient interface | PROVED EXACTLY | Its local logarithmic coefficients are `-d_(sigma,k,n)/n` for `n>=2`; analytic comparison only. |
| Zero-padding rank lock | PROVED | `d1^0(H_sigma,Y_k)>=q(r_sigma-m_k)_+ + beta_k(m_k-r_sigma)_+`. |
| Decaying matching forces rank equality | PROVED CONDITIONALLY | Any actual `o(1)` padded matching implies eventual `r_sigma=2k-2`. |
| Actual head rank law | OPEN | The source gives only `r_sigma<=4/sigma`; RH-16 endpoint singular rank is a different type. |
| Shifted-moment uniqueness | PROVED | Ranks `<=N` plus power sums `2,...,2N+1` identify nonzero finite multisets. |
| Rank-capped strict-prefix identification | PROVED CONDITIONALLY | With both ranks `<=2k-2`, exact equality for `2<=n<4k` identifies the multisets. |
| Hidden-shell counterexample | PROVED EXACTLY | Adds `4k` roots of radius `3/4`; strict-prefix moments agree while padded root cost is at least `2k`. |
| Hidden-shell analytic factor | PROVED EXACTLY | The added genus-one factor is `1-(3z/4)^(4k)`. |
| Physical noisy-operator realization of the shell | NOT CLAIMED | The counterexample is finite normal spectral information-class data only. |
| RH-299 global-cap threshold at `u=4k` | PROVED SPECIALIZATION | Markov spectral radius and `beta_k->beta<1/r_H` give the eventual common cap; then `gamma>2 log(R/r_H)/log(lambda)=1.926813889034...`. |
| RH-299 local-shell threshold | CONDITIONAL | `0.926813889034...` is the limiting threshold under the unproved cap `|mu|<=beta+o(1)` for every actual-head root; a strict exponent permits `B=beta+epsilon`. |
| Actual root matching with sufficient rate | NOT_TESTABLE | No asymptotic or interval root list supplies it. |
| RH-299 activation without rank law/cap/rate | STOP_SCOPED | The sufficient interface lacks mandatory hypotheses. |
| Aggregate moment/Fourier/Hardy route | NOT_TESTABLE | No actual moving-order estimate is available; the model counterexample is not a physical nonidentification theorem. |
| Direct annular route | OPEN / NOT_TESTABLE | No actual annular convergence theorem for the direct logarithmic mismatch is known. |
| RH-288 determinant gluing | OPEN | No head matching or aggregate direct-prefix closure is proved. |
| Gates A--E | OPEN | No status changes. |
