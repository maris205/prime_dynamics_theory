# TPC-326 computational protocol

1. Verify the TPC-325 producer and certificate provenance locks.
2. Freeze origin `16001`, four nested endpoints, shell anchors, exponents, and
   sign laws.
3. Recompute the complete 32-row panel with forward/reverse spectral paths and
   store canonical full-row JSON.
4. Rebuild every row independently in reverse einsum order; compare labels,
   outward metric intervals, trace-side census, and exact anchor.
5. Compare the new census and envelope values with the parent certificate.
6. Stress source nesting, freshness, one-step residue perturbation, trend
   direction, thresholds, and claim firewall.
7. Require empty stderr and byte-identical normal/optimized output in Bridge-B.
