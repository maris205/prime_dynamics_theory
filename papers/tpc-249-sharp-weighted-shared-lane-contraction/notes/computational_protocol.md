# Computational protocol

The producer freezes exact rational aligned, orthogonal, cancellation, affine,
global-budget, and complex-orientation fixtures in canonical strict JSON.  An
independent implementation imports no producer and rejects semantic, typed,
and digest-rebound attacks.  The stress checker enumerates exact integer probe
and weight families and verifies Gram contraction and triangle dominance.
