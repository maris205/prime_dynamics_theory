# Roadmap after RH-237

The trace jet is channel-stable enough to define an adaptive shell criterion.
RH-238 selects the first shell-complete prefix whose order-12 unit-disk jet is
at most the noise scale itself.
