# TPC-412 derivation package

The pooled two-shell profile remains valid as `H` grows through
`16,32,66,128` because every retained prime exceeds the largest window
`N=512`.  The shell-local amplitude contract is unchanged.  Equal pooled
parity counts and the same Cauchy--Schwarz cancellation therefore give the
uniform finite bound `z<=4/H` at all four heights.
