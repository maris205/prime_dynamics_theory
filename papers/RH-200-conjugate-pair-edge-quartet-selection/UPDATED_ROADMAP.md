# Roadmap after RH-200

```text
outer spectral edge + real conjugation parity          [finite three-scale support]
  -> canonical source-observation quartet               [exact conditional]
  -> temporal quartet alignment and determinant ledger   [finite positive]
  -> prove uniform edge gap and mode transport           [open]
  -> build cross-scale cloud/trace object                [open]
  -> Gate A, then B--E                                   [untouched]
```

The quartet selection no longer needs to be defined by post hoc nearest-root
matching at the finest scale.  It is still only a candidate rule until the
edge gap and source/observation visibility are controlled uniformly.
