# TPC-372 proof and certificate package

## Exact finite claims

1. The three origins and the eight-block partition are inherited deterministic
   declarations, independent of the observed response.
2. The full-window weighted geometry is a finite sum of nonnegative rational
   squares and is positive on the declared panel.
3. The block mask gives the exact finite identity `T=D+R`, with the same
   full-window normalization in all three matrices.
4. The inherited exact anchor is symmetric and positive for both beta values.
5. The reverse triangle inequality supplies the recorded lower bound
   `||R||_2 >= ||T||_2-||D||_2`.

These are finite statements and do not identify a causal mechanism.

## Numerical certificate

The producer evaluates all 18 rows.  The independent checker has its own
prime sieve and descending-shell accumulation, reconstructs `T`, applies the
fixed block mask, and compares shell data, weights, geometry, all three sets
of matrix metrics, decomposition identities, lower bounds, and phase counts.
The adversarial suite mutates protocol, parent lock, row census, component
metrics, failure keys, decomposition error, anchor, firewall, and clue.
Bridge-B reruns producer, independent, and stress checks in normal and
optimized Python modes and requires empty stderr plus byte-identical stdout.

The finite beta=2 census is:

* full: 6 spectral-cap failures and 0 Schur-cap failures;
* block diagonal: 0 spectral and 0 Schur failures;
* off block: 0 spectral failures;
* all 6 full failures satisfy the declared “required off-block” condition.

The beta=0 control has 9 full spectral and 9 full Schur failures, and its
block-diagonal component also retains a strong all-plus phase.

## Non-claims

The decomposition does not prove off-block causality, positivity, or an
asymptotic limit.  It supplies no arithmetic `L2`, fixed-power credit,
source-uniform theorem, official Route-A/Route-B verdict, or twin-prime
conclusion.  The official evaluator files named by the Session are absent;
the local bridge is fail-closed repository evidence only.
