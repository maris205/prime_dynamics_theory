# TPC-246 route evaluation

## Strongest positive result

Independent exact local covariance disks reassemble into one exact weighted
disk with center `sum lambda_h c_h` and radius `sum |lambda_h|r_h`.  The
TPC-243 leakage then yields the explicit robust margin
`|C_0|-R_0-epsilon||W||||B||`.

## Strongest obstruction

If `|C_0|<=R_0`, the abstract product model realizes exact cancellation, so
local moments and transverse energies alone cannot force a positive aggregate
covariance.  In the arithmetic source, block independence and the literal
two-lane attachment are not yet available.

## Open theorem

Construct source-native coefficient lanes and block projections, prove the
required local disks or a smaller actual feasible set, and establish a payable
strict margin exceeding both transverse radius and hard-window leakage.

## Reusable structure

`local disks -> weighted exact disk -> zero/margin dichotomy -> window-inflated disk`.

## ROUND2_CLUE

`SOURCE_NATIVE_WEIGHTED_LONGITUDINAL_DOMINANCE_BEYOND_TRANSVERSE_RADIUS_AND_WINDOW_LEAKAGE`.
