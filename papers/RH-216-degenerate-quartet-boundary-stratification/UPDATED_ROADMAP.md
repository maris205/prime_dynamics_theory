# Roadmap after RH-216

```text
candidate axial boundary                            [exactly classified]
physical convergence to boundary                    [open]
transverse eta relevance near boundary               [next]
fixed-factor spectral sufficiency                    [open]
```

RH-217 quantifies why the transverse coordinate becomes progressively less
visible in coefficient space even if `eta` itself does not converge.
