# Roadmap after RH-254

The expanded spectral window is available as a finite input, but the final
root of a fixed-count ARPACK window can split a conjugate shell.  RH-255 must
discard those incomplete boundary shells and run the deterministic anchor
reachability audit on the genuinely shell-complete expanded class.

Route coordinate:

```text
expanded_window_shell_boundary_observed_open_anchored_reachability
```

No uniform spectral or cloud theorem is claimed.  Gates A--E remain false/open.
