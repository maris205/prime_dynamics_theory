# TPC-408 theorem ledger

- `H=66`, `N=264=4H` are fixed.
- Both declared shells are complete and retained in full.
- Odd cardinality is handled by `m_minus=floor(r/2)` and
  `m_plus=ceil(r/2)`.
- Exact conclusion: `0<=z<=t1/(a_min sqrt(S0 S1))<=4/(a_min H)<=4/H`.
- Scope: one adjacent synthetic normalized proxy entry at two finite scales.
- Unpaid: full operator norm, physical `h_0`, arithmetic sign/L2, fixed
  power, Route B, and twin-prime result.
