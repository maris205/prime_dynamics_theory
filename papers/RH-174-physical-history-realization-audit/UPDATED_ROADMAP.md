# Roadmap after RH-174

```text
canonical finite history realization          [proved and stable]
  -> consecutive reset two-sided bundle       [not supported by 130 snapshots]
  -> square infinite-history completion       [next: test spectrum/trace ideals]
  -> finite cyclic completion                  [candidate alternative]
  -> physical transfer/Riesz interface        [open]
```

The next paper should determine whether the natural infinite weighted-history
shift can be compact or Schatten class.  A negative answer rejects that
specific determinant completion and motivates a finite cyclic closure.
