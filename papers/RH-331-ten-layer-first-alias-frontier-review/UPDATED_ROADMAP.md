# Roadmap after RH-331

The current coordinate is

```text
first_alias_transfer_criterion_exact_actual_replacement_open
```

The exact algebra is no longer the bottleneck.  A legitimate continuation
must prove actual replacement data on one common moving-order clock.  The
minimal obligations are:

1. identify the actual `n=2k` coefficient with the five-slot localized
   packet and freeze a physical actual/model map before measuring defects;
2. control the second physical critical leg and transport the clearance
   phase through all `2k` legs;
3. obtain physical signed group enclosures for both critical Duhamel
   channels, retaining all `4k` hybrid terms;
4. prove target-scale parity/alias replacement and a signed far-remainder
   theorem;
5. prove the off-alias weighted background vanishes on a window containing
   the first alias but no second alias; and
6. close the separate head/counterloop determinant-gluing budget.

A successful closing-model route needs `Theta_k=o(H_k)` and `E_off->0`.
The RH-329 model instead needs the tuned repair
`Theta_k=-e_model,k+o(H_k)`; alternatively, `Theta_k=o(A_k)` would transfer
its scoped negative divergence.  Neither estimate is currently available.

The alternative legitimate route remains actual fixed-order complement
transport plus endpoint energy tightness.  More finite fits, scalar repairs,
isolated exchange models, or separate unsigned majorants are not reopening
inputs.  Gates A--E remain false/open.

## Proposed RH-332--RH-341 actual-replacement batch

The next batch has a positive theorem and a scoped negative exit at every
layer:

1. **RH-332: Sharp physical repelling-return affine-leg remainder.**  Treat
   the second critical physical leg, including both sibling/fold directions;
   prove a phase-matched `O(sigma)` row remainder with its sharp coefficient,
   or isolate the first genuine scale obstruction.
2. **RH-333: Full boundary-cycle clearance-phase transport.**  Propagate the
   common phase and incoming-law moments through all `2k` legs; target the
   retained-path estimate `O(k sigma)=o(H_k)`, or prove a phase/stability
   route stop.
3. **RH-334: Gauge-fixed physical first-alias observation map.**  Freeze the
   windows, pullback, sectors, and observation before evaluation and prove
   the typed identity `q_(sigma,k,2k)=B+S+R+P-A`, or prove an exact
   sector/nonuniqueness obstruction.
4. **RH-335: Adapted-norm two-sibling trace-observation stability.**  Seek
   physical prefix/suffix weights of order `sigma^(-gamma)` with
   `gamma < 0.3503698834605293...`; an inherited lower bound cannot be used
   as this upper bound.
5. **RH-336: Physical signed two-channel Duhamel cancellation.**  Retain and
   pair all `4k` hybrid terms; prove a signed enclosure for
   `Delta_B+Delta_S`, or a nonzero normalized physical obstruction.
6. **RH-337: Target-scale parity--alias replacement.**  Upgrade the leading
   scalar laws to rigorous remainder/interval estimates sufficient for
   `Delta_P-Delta_A=o(H_k)`, or identify a nonzero target-scale remainder.
7. **RH-338: Signed far-complement trace remainder.**  Prove the aggregated
   `Delta_R=o(H_k)` without summing separate absolute far pieces, or isolate
   a real nonvanishing periodic-orbit family.
8. **RH-339: Punctured one-alias weighted-background theorem.**  Prove
   `E_off->0` on `2k<h<=4k`, including the neighboring sidebands
   `n=2k+j`, or establish a positive sideband lower obstruction.
9. **RH-340: Synchronized noisy-head/counterloop determinant gluing.**  Close
   the head budget on the identical clock and activate RH-288 only if the
   critical, off-alias, and head budgets all close.
10. **RH-341: Ten-layer actual first-alias replacement frontier review.**
    Audit the unique positive, negative, or still-open coordinate and update
    the handoff without changing Gates A--E by inference.

The main dependency is

```text
RH-332 -> RH-333 -> RH-334 -> RH-335 -> RH-336
RH-337 ----------------------------------|
RH-334 -> RH-338 ------------------------|
RH-336 + RH-337 + RH-338 -> RH-339 -> RH-340 -> RH-341
```

RH-332/333, RH-337, and read-only preparation for RH-338 can run in parallel.
The observation type must be frozen in RH-334 before RH-338 becomes a formal
replacement theorem.  Failure of any checkpoint changes the following paper
to a scoped obstruction audit; it does not authorize bypassing that layer.
