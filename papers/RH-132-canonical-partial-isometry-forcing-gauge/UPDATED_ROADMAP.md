# Roadmap after RH-132

## Immediate

Derive adjacent support frames directly from the memory and packet update.
Compare the resulting dynamical map with the polar partial isometry and
bound their projector/angle discrepancy.

## Affine decomposition

Express the next discarded-memory tail as transported old tail plus the
newly crossed memory slice.  Use the positive-part forcing theorem only as a
sharp diagnostic; seek a source formula for the forcing itself.

## Coefficients

Normalize multiplicative transport and birth forcing into `rho_n` and `q_n`.
The two RH-130 superunit birth states must be explained or bypassed before an
eventual contractive packet can be claimed.
