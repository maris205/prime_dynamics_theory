# TPC-317 citation verification

This is a self-contained finite linear-algebra and literal-kernel audit.  The
only external background citations in the manuscript are standard references
already used by the parent TPC-316 release; no external theorem is used to
support the finite numerical trend.  The exact inequalities needed for the
claim are proved in the manuscript and proof package.

The parent release is identified by a repository SHA-256 lock rather than by
an external signature.  The local Session evaluator files named in the route
plan were not present, so no official evaluator pass is claimed.
