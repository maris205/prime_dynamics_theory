# Route update after RH-110

## Closed at this layer

RH-109's scalar volume loss is now exactly localized.  A recent-memory
capacity upper endpoint, combined with the spectral-volume lower endpoint,
reconstructs every archived direct support decision and loses at most
`0.176%` on the fine scales.

## Remaining independence problem

The present capacity interval uses the same singular spectrum as the direct
Weyl certificate.  It proves that the factorization is mathematically sound,
but not that `Lambda23` can be controlled from simpler physical data.  At
fixed volume its universal upper bound remains one and is sharp.

## Next layer

Measure the other RH-109 loss:

```text
kappa4 = e4(K*K)/(s1^2 s2^2 s3^2 s4^2).
```

This exterior concentration lies in `[1,binom(r,4)]`.  RH-111 should derive
a tail-energy refinement, audit the physical values, and decide whether the
reduced `e4` trace can replace a spectral exterior solve without paying the
worst combinatorial factor.
