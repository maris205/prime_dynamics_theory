# RH-389 updated roadmap

## Route A: GO -- active terminal-log fixed clocks

RH-389 closes the executable route

    exact nine-point interpolation
      -> terminal Abel transfer for all non-active channels
      -> TPC-137 determinant-two cancellation for active c11
      -> pointwise projection of 512 tables to eight actions
      -> directed compatibility from edge triples
      -> injective r-to-r-2 predecessor charge
      -> input reflection
      -> identical capacity for every fixed q.

The analytic theorem is pointwise in each fixed `q`, fixed safe family,
and admissible `omega`.  The finite family `A_q` permits the fixed-clock
maximum after those limits.  Only then is the supremum over fixed clocks
taken.

## Novelty boundary

RH-378 already identified `6/pi^2-kappa_2/2` and the same table for
`q=1` under an ordinary-Cesaro `D_2=o(N)` assumption.  RH-389 does not
claim discovery of that constant.  Its new edge is unconditional
terminal-log control of active periodic `c11`, together with the
eight-action charge proving the same capacity for every fixed clock.

## Route B: STOP_SCOPED

Stop rather than extrapolate if a consumer requests:

- an ordinary Cesaro active-`c11` theorem;
- a quantitative decay rate or power saving;
- uniformity over an unbounded or `X`-dependent clock set;
- `lim_X sup_q`, adaptive max-before-limit, or a projective `K_N`;
- a nonperiodic residue weight;
- an operator, trace, zero-divisor, or RH conclusion.

The fixed-`q` finite maximum may commute with its finitely many established
pointwise limits.  This does not authorize any unbounded-`q` interchange.

## Source route

The full Mobius-correlation input is the frozen TPC-137 release theorem.
Tao's Cambridge VOR is locked only as its upstream Liouville provenance.
Johnston--Yang and Maynard are inherited closure-only objects.  The Git
closure has 95 blobs; three remote logical objects give 98 logical inputs.
No remote payload is vendored.

## Artifact and release route

Release completion requires simultaneous agreement of:

1. 602 independently checked rows and 24 semantic mutations;
2. fresh result and recursively closed official Draft 2020-12 schema;
3. 95 release Git blobs and three ordered remote logical locks;
4. three default-offline verifiers making zero network requests;
5. byte-identical main and semantic PDFs;
6. a fixed 37-member publication manifest plus outer verification;
7. recursive exclusion of all five remote payload hashes;
8. theorem, integrity, review, format, source, visual, and replay audits.

Every finite row remains reproduction, not analytic proof.
