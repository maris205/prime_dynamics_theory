# TPC-280 claim firewall

```text
PROVED_CONDITIONAL = two-term and dominant-exponent compiler under declared hypotheses
NUMERICALLY_CERTIFIED = exact rational fixtures and TPC-279 coordinate transfer
HEURISTIC = none used for the certificate
OPEN = literal growing source-level decomposition and arithmetic L2
REFUTED = ignoring a slower additive leakage term as if it had the main exponent
FIXED_POWER_CREDIT = 0
FULL_GATE_B = OPEN
TWIN_PRIME_RESULT = NONE
```

The Session-named `propose.md` and Route A/Route B evaluator files are absent
from this checkout.  The local proof package, theorem ledger, exact certificate,
independent replay, stress audit, bridge checker, and `AGENTS.md` provide the
fail-closed scoped evaluation.
