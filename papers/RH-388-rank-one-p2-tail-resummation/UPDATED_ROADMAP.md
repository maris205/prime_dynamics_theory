# RH-388 updated roadmap

## Route A: GO -- exact rank-one retention

RH-388 closes the executable route

    explicit theta envelope
      -> strict Stieltjes transfer only for r>=2
      -> exact factorial Laplace remainder for every r>=2
      -> positive real cube for every 1<=K<=floor(3L)
      -> uniform b_K induction over the complete integer window
      -> RH-383 endpoint l_infinity/l_1 bound
      -> o(P_2) resummation with exact P_1 retained.

The key new interface is rank separation.  RH-387's full smooth
replacement pays a rank-one source error larger than `P_2`; RH-388 keeps
that coordinate exact and gains the required factor `x^-2` on every
transferred rank.  The conclusion is uniform over arbitrary moving
`K_y` inside the window, not merely over fixed truncation orders.

## Route B: GO -- bounded-gap necessity

Maynard's bounded consecutive gaps, exact strict succession, and the
RH-383 endpoint direction give

    limsup p_y^2|P_1-I_2|>=1/2,
    limsup p_y^2 pi^2|GapP-GapI|>=X_infinity,

with the same endpoint bound for `GapJ`.  This is a genuine actual-prime
negative edge for the declared smooth `J/I` hierarchy.  It explains why
rank-one retention is not cosmetic.

## Route C: STOP_SCOPED

Stop rather than extrapolate if a consumer requests:

- convergence of the divergent-looking factorial series;
- an order window larger than `K<=floor(3L)`;
- a universal impossibility theorem for every surrogate;
- `P_3`, cubic, or higher coefficient precision;
- a complex channel or a channel outside the seven frozen integers;
- a growing clock, active phasewise `c11`, or prefix-dependent `K_N`;
- an operator, trace, zero-divisor, or RH conclusion.

The positive theorem requires exact `P_1`; the negative theorem concerns
only the frozen `P/J/I` kernels.  It does not rule out a different
surrogate carrying other exact prime data or correction terms.

## Source route

The analytic theta input remains Johnston--Yang Theorem 1.4, equation
(1.8), printed page 2.  The bounded-gap input is Maynard Theorem 1.3,
printed page 385/PDF page 3.  Both are immutable remote logical locks.
Neither external payload is redistributed.  The Git closure is exactly
77 release blobs and the logical closure is 77+2=79 inputs.

## Artifact route

Release completion requires simultaneous agreement of:

1. 56 independently recomputed rows and 24 genuine semantic mutations;
2. fresh result and recursively closed official Draft 2020-12 schema;
3. 77 release Git blobs and two ordered remote logical locks;
4. default-offline and separate opt-in live verification for both remotes;
5. byte-identical main and semantic PDFs;
6. fixed publication manifest plus external-input replay;
7. theorem, integrity, review, format, remote-source, visual, and replay
   audits.

Every finite row retains the role `reproduction_not_analytic_proof`.
