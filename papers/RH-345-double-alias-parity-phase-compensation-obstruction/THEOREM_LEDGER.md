# RH-345 theorem ledger

| Item | Status | Exact scope |
|---|---|---|
| Complete critical identity | INHERITED EXACTLY | RH-344 gives `p=Y_k+P-S_k`, with `Y_k=T_k^rest-d` and `S_k=A_k+F_k^orb`. |
| Double-alias demand | INHERITED / PROVED | `S_k/A_k->2` and `S_k/H_k->infinity`. |
| Actual parity phase law | INHERITED / PROVED | RH-326 gives `P/A=C_*C_M lambda^(eta_sigma)(1+o(1))` on bounded physical phase. |
| Complete scalar ratio | PROVED | At fixed phase `eta`, `P/S->C_*C_M lambda^eta/2`. |
| Unique double-alias phase | PROVED EXACTLY | `eta_2=log(2/(C_*C_M))/log(lambda)` is the unique symbolic phase with leading ratio one. |
| Conditional physical off-balance obstruction | PROVED | If the actual `Y_k=o(H_k)` and `C_*C_M lambda^eta!=2`, then `|p_(sigma,k,2k)|/(2H_k)->infinity`. |
| Aggregate critical nonclosure | NOT CLAIMED | The required actual hypothesis `Y_k=o(H_k)` is not proved. |
| Balance-phase target precision | PROVED NECESSARY | Scalar closure requires `P=S_k+o(H_k)`, equivalently relative `o((beta R)^(-2k))`. |
| Leading phase law at balance | INSUFFICIENT | Relative `o(1)` does not imply target-scale matching. |
| Exact scalar inverse map | PROVED EXACTLY | Every desired `0<X_k<r_H^(-2k)` has `delta_k=1-(1-r_H^(2k)X_k)^(1/(2k))` and exact parity packet `X_k`. |
| Close scalar sequence | PROVED | `P_k^close=S_k` gives zero critical residual when `Y_k=0`. |
| Far scalar sequence | PROVED | `P_k^far=S_k+A_k/k` gives critical residual `A_k/k`. |
| Common square-root law | PROVED | At `eta_2`, both scalar sequences satisfy `delta_k=C_*sqrt(sigma_k)(1+o(1))`. |
| Far weighted critical divergence | PROVED | `A_k/(2kH_k)=(beta R)^(2k)/(C_M k)(1+o(1))->infinity`. |
| Actual parity eigenvalue realization | NOT CLAIMED | The scalar sequences are not identified with the actual noisy operator's parity eigenvalue. |
| Scalar-only route verdict | STOP_SCOPED | Off balance it fails under target-negligible rest; at balance the proved scalar information remains underdetermined. |
| Actual signed critical compensation | NOT_TESTABLE / OPEN | No moving-order theorem estimates the physical orbit-free remainder. |
| Lower sideband | NEXT ROUTE | RH-346 should complete the physical lower-sideband orbit decomposition before any scalar test there. |
| RH-288 and Gates A--E | OPEN | No determinant-gluing or Gate hypothesis is activated. |
