# Route evaluation

TPC-417 is a genuine finite object upgrade: the claim is about the entire
`N`-by-`N` locally normalized proxy, not one adjacent entry.  The block proof
is exact at four fixed heights.  It supplies no growing uniformity, physical
coefficient map, arithmetic sign identification, arithmetic `L2`, fixed-power
payment, Route-B closure, or twin-prime result.
