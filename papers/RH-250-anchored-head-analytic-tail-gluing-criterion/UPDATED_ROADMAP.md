# Roadmap after RH-250

## Current coordinate

```text
finite_head_anchor_missing_tail_conditional_open_new_anchor_or_route_stop
```

The exact gluing theorem identifies the last two certificates needed before
RH-240 can be invoked: an anchored finite head and a uniform quotient and
target tail.  The current relaxed head misses at every endpoint, while the
tail is only a finite subbatch diagnostic.  This is a genuine scoped route
stop for the frozen cloud class.

RH-251 should review the ten layers, record which statements are exact,
finite, conditional, or blocked, and decide whether any local Gate-A progress
survives without overstating it.
