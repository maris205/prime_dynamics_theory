# TPC-315 claim firewall

    PROVED_EXACT_FINITE = rational physical construction; finite Gram/PSD
                          identities; exhaustive sign traversal; logarithm tail
                          bound; directed interval arithmetic
    NUMERICALLY_CERTIFIED_FINITE = 8 fresh rows, 48 law/target cases, 24/24
                                   below-one minima, 24/24 above-one controls,
                                   disjoint law orders, independent replay
    MODELING_CHOICE = TPC-314 menu, same locked engine, fresh interval,
                      shell anchors, height, weighted diagonal normalizer,
                      Gram-minimum target and all-positive control
    HOLDOUT_LIMIT = fresh source interval but same physical engine; no external
                    data or independent physical generator
    TARGET_DEPENDENCE = each minimum label is selected from the fresh Gram that
                        is subsequently weighted; pre-locking the menu does not
                        make the target predictive
    REFUTED_FINITE = a claim that the declared laws have one invariant amplitude
                     order across the two finite panels
    OPEN = canonical law; external physical holdout; uniform growing weighted
           theorem; literal arithmetic L2; fixed-power credit; Gate B; twin
           prime endpoint

The word “holdout” is used only for the source interval relative to TPC-314;
it is not a claim of external statistical independence.
