# TPC-328 paper plan

## Question

After the finite three-origin triangulation, does the actual V59 arithmetic
residual exhibit source-coordinate `L2` cancellation when it is passed through
the literal coherent prime-shell operator?  Is a contraction visible for the
declared sign laws, or is there a finite off-diagonal obstruction?

## Exact object

Use the same deleted-diagonal centered block `B_(p,Q,s)` as TPC-316 through
TPC-327, on three disjoint origins and four nested source scales.  Form
`C_e=sum_p e_p B_p` and compare the full energy `E_e(v)` with its coordinate
diagonal `D_e(v)` for

```text
v = beta_o^(2) = Lambda(t+2)-b^(2)(t).
```

The source formula is inherited from the locked V59 finite comparison model;
its finite Euler product and logarithms are recorded as a declared modeling
layer, not promoted to an arithmetic asymptotic theorem.

## Release-bearing progress

1. Prove the exact finite Gram identity `E=D+O` and identify `O` as the
   source-coordinate off-diagonal term.
2. Recompute all `96` origin/scale/shell/exponent rows for four predeclared
   sign laws, with a `5e-8` ratio guard.
3. Record a non-vacuous finite cancellation/obstruction split: `81` versus
   `15` all-plus rows, alongside positive component controls on all rows.
4. Bind an exact rational local anchor to the actual prime-indicator-minus-odd
   vector and provide independent replay plus mutation stress checks.
5. State the narrowest consequence: four finite sign laws do not supply a
   uniform contraction, and no growing source-native estimate is obtained.

## Evaluation and stop rules

The result is accepted only if the producer replay, independent checker,
optimized-mode checker, stress suite, PDF audit, and local Bridge-B normal /
optimized equality all pass.  Any unresolved ratio, parent hash mismatch,
source-vector mismatch, duplicate JSON key, or changed physical mask fails
closed.  A finite ratio is never converted into fixed-power credit or a Route-B
gate payment.

## Natural successor

Use the actual census, rather than a preselected positive narrative, to choose
TPC-329.  The first candidates are (i) a held-out origin/scale stress test of
the source-native vector, and (ii) a finite theorem or counterexample for the
signed-Gram cross-term.  A new paper is warranted only when it adds a distinct
object or a sharper obstruction.
