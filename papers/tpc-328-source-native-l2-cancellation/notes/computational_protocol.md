# TPC-328 computational protocol

1. Verify the TPC-327 producer/certificate and TPC-267 V59 source locks.
2. Freeze three origins, four nested scales, shell anchors, exponents, masks,
   the source formula, and the `5e-8` ratio guard.
3. Build the source midpoint vectors and four coherent matrices for every one
   of the 96 Cartesian rows.
4. Record `E`, `D`, `O`, the ratio, an outward display guard, and the label
   determined by that guard.  Require positive energy and diagonal terms.
5. Rebuild all rows in the independent checker without importing the producer;
   use a reverse shell accumulation order and separately implemented source
   arithmetic.
6. Recompute the exact rational anchor and verify its three Fraction digests.
7. Run the stress suite, producer/checker optimized variants, PDF hygiene, and
   local Bridge-B normal/optimized equality.
8. Treat every result as finite.  No row count or ratio is allowed to create
   a source-uniform theorem or fixed-power credit.
