# Roadmap after RH-183

The orthogonal branch cannot be repaired by a phase or scalar wrap. The next
typed alternative is Petrov--Galerkin rather than reducing-subspace geometry:

```text
right source history + left observation history
  -> canonical biorthogonal frames
  -> two directed residuals
```

RH-184 should prove a target-independent balanced cross-Gram realization and
record its unavoidable conditioning before any physical audit.
