# RH-320: Escaping-packet endpoint-energy obstruction

Start with the exact prefix spectrum through order `K-1` and add a signed
`K`-packet whose endpoint coefficient has modulus one and is aligned with the
existing order-`K` error.  The resulting genuine finite spectra have:

- exact target moments at every fixed order for all sufficiently large `K`;
- convergence on every strict radius, including `R=1.4`;
- a squared-mass clock compatible with `M<=sigma^(-1)`;
- endpoint `H2(rho_star)` norm at least one.

Thus the archived coarse axioms are compatible with both endpoint convergence
and endpoint nonconvergence.  The missing actual input is coefficient
convergence plus endpoint energy tightness.  The construction is synthetic
and does not prove actual nonconvergence.  Gates A--E remain false/open.
