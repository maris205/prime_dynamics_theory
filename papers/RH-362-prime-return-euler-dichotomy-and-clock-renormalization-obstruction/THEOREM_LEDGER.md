# RH-362 theorem ledger

| Item | Status | Exact scope |
|---|---|---|
| Four-volume foundation | FROZEN / PRESERVED | RH-1--RH-361 remain the atomic sources, compressed into the disjoint ranges `1--160`, `161--241`, `242--281`, and `282--361`; the outer replay checks `4` volumes, `361` sources, `73` members, `1,548` dependency hashes, `8` result hashes, and `0` failures. |
| Return--divisibility bridge | PROVED UPSTREAM | `M|a_n(P)` iff `r_M(P)|n` for every integral point, modulus, and order. |
| Concrete seed nonperiodicity | PROVED | `P=(0,0)` enters a strictly escaping negative cone after the displayed finite prefix. |
| Low-rank prime identity | PROVED | For nonperiodic `P`, `{p:r_p(P)<K}` is exactly the prime-divisor set of `prod_(n<K)a_n(P)`. |
| Local pointed-cycle determinant | PROVED | On the complete marked `r_p`-cycle, `Tr(U_p^n)=r_p 1_(r_p|n)` and `det(I-zU_p)=1-z^(r_p)`. |
| Formal bouquet zeta | PROVED | The countable disjoint union has finite fixed-point counts in every order and formal product `prod_p(1-z^(r_p))^(-1)`.  No positive analytic radius is claimed. |
| Return Dirichlet Euler product | PROVED | `Z_P(s)=prod_p(1-p^(-s r_p))^(-1)` converges normally, is holomorphic, and is zero-free on `Re(s)>0`. |
| Dirichlet coefficient law | PROVED | `c_P(m)=1` exactly when `r_p(P)|v_p(m)` for every prime; coefficients are multiplicative `0/1` values and both convergence abscissae equal zero. |
| Logarithmic derivative | PROVED | Support is `p^(j r_p)` with weight `r_p log p`; this is not the classical von Mangoldt ledger. |
| Natural block operator | PROVED | `T_s=direct_sum p^(-s)U_p` is compact on `Re(s)>0`; `q Re(s)>3` is sufficient for `S_q`. |
| Ordinary Fredholm identity | PROVED IN DECLARED REGION | `det_F(I-T_s)=Z_P(s)^(-1)` for `Re(s)>3`.  No ordinary Fredholm status is claimed below the proved trace-class region. |
| Regularized determinant identity | PROVED IN DECLARED REGION | If integer `m Re(s)>3`, the scalar product equals `det_m(I-T_s)` times the explicit finite block-trace correction. |
| Boundary at `s=0` | PROVED SCOPED NEGATIVE | Neither `Z_P` nor its reciprocal can be meromorphically continued through `s=0`; no whole-line natural-boundary claim is made. |
| Periodic seed branch | PROVED CONDITIONALLY ON EXISTENCE | Exact integral period `N` gives `zeta(Ns)` times a finite Euler correction.  No integral periodic seed for this map is asserted. |
| Inverse-length clock | PROVED SCOPED NEGATIVE | `p^(-s/r_p)` forces each local factor to `1-p^(-s)` but the direct sum is in no finite Schatten class for nonperiodic `P`. |
| Hasse--Weil or full-map interpretation | FALSE / NOT CLAIMED | Only one marked cycle from each finite-field map is used. |
| Original physical triggers 1--4 | UNTOUCHED | No `D_(4k)`, typed `q/E_off`, complete unnormalised prefix, or RH-241 moving noisy bridge is supplied. |
| Gates A--E | FALSE/OPEN | No canonical physical determinant, scattering completion, self-adjoint generator, von Mangoldt trace, or completed-zeta divisor equality is proved. |

Finite computations certify source hashes and exact formulas only.  The
all-prime theorems are proved analytically in the manuscript.
