# TPC-373 proof and certificate package

## Exact finite statements

1. The inherited origins, count, shell anchors, laws, beta values, and block
   partition are deterministic declarations made before profile inspection.
2. The full-window geometry is a finite sum of nonnegative rational squares
   and is positive on the declared panel.
3. The block-distance masks form an exact finite partition of every matrix.
4. The largest-absolute-eigenvalue rule, including its minimum-mode tie
   break, is deterministic once the full matrix is formed.
5. For the selected unit eigenvector, linearity gives
   `sum_d v^T L_d v = v^T T v = lambda`.

These statements do not identify a mechanism for the parent spectral excess.

## Numerical certificate

The certificate contains all 18 row records, full spectral/Schur/Frobenius
metrics, eigenmode residuals, eight layer terms, signed and absolute
fractions, exact-anchor digests, parent provenance, and a claim firewall.
The independent checker uses a separate prime sieve and reverse shell order.
The adversarial suite mutates protocol, provenance, row census, mode rule,
layer records, error bounds, anchor, firewall, and clue fields.  Local
Bridge-B reruns producer, independent replay, and stress checks in normal and
optimized Python modes with empty stderr and byte-identical stdout required.

## Non-claims

The finite profile is not a cross-block decay theorem, an eigenvector
localization theorem for other windows, a causal attribution, or an
asymptotic statement.  It supplies no arithmetic `L2`, fixed-power credit,
source-uniform estimate, official Route-A/Route-B verdict, or twin-prime
conclusion.  The official evaluator files named by the Session are absent;
the local bridge is repository evidence only.
