# Roadmap after RH-279

The next physical input is an all-level factorization or square-root block
law that supplies the moving `m_sigma`, trace norm, and prefix constants.
The theorem deliberately avoids a nonexistent fixed limiting quotient.
