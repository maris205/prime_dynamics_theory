# Roadmap after Volume IV

The four-volume series ends at the current numbered endpoint RH-361. The
mathematical route coordinate remains

```text
actual_same_clock_unnormalized_head_transport_open
```

The narrowest admissible reopening inputs are:

1. an actual same-clock proof or physical obstruction for `D_(4k)(R)->0`;
2. a typed actual full-trace `q` or complete `E_off` aggregate theorem;
3. an unnormalized complete direct-prefix theorem, including the low orders;
4. the RH-241 moving noisy all-order envelope and a valid coefficient bridge;
5. another independent source-backed theorem edge.

Finite fits, synthetic spectra, abstract coefficient fibers, deterministic
reparameterizations, normalized selected windows, and inactive analytic
criteria do not activate RH-362. Even a positive defect theorem transfers
only the named coefficient budgets; root, rank, spectral-submultiset, and
canonical determinant identifications remain separate.

The cyclic affine-Gaussian direction is testable only after a compact,
row-normalized, signed/folded reference is defined in the RH-334 trace data
type. Its first correction-factor target is

```text
C_(sigma,k) = 1 + o((beta R)^(-2k))
```

on the complete signed `2k` path sum and frozen basepoint observation. The
full-line diagonal identity alone is not an ordinary compact operator trace.
