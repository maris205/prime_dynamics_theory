# TPC-338 computational protocol

1. Verify the TPC-337 producer and certificate hashes.
2. Rebuild the six source windows and the fixed all-plus matrix.
3. Apply nine predeclared bijections to all four masked vectors.
4. Form the nested five-control and nine-control means, centered vectors,
   covariance Gram matrices, spectra, and normalized spectra.
5. Compare all nested fields with a relative `3e-7` independent guard.
6. Use a reverse shell accumulation and independent trial factorisation.
7. Run semantic mutation stress and normal/optimized equality.

The `L1` spectral distances and relative Frobenius changes are finite
descriptive statistics.  No asymptotic interpretation is assigned.
