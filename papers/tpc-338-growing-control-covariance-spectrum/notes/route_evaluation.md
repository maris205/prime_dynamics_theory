# TPC-338 route evaluation

The available local evidence is the parent-locked producer, independent
reverse-shell replay, exact finite proof package, mutation stress, PDF audit,
and local fail-closed Bridge-B wrapper.

The Session-named files `propose.md`, `skills/route-a-evaluator.md`, and
`skills/route-b-evaluator.md` are absent:

```text
Route-A = NOT_EVALUATED_OFFICIAL
Route-B = LOCAL_FAIL_CLOSED_FALLBACK
ARITHMETIC_ADVANCE = NO
FIXED_POWER_CREDIT = 0
FULL_GATE_B = OPEN
```

The positive result is nested energy dominance; the obstruction is the
control-family sign reversal, which rules out using one observed covariance
sign as a canonical arithmetic mechanism.
