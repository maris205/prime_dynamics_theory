# RH-390 updated roadmap

## Route A: GO -- growing-rank exact retention

RH-390 closes the executable route

    strict prime-tail endpoint
      -> all-rank Tonelli source transfer
      -> safe A/B/C denominator ledger
      -> exact finite Laplace remainder
      -> symbolic full-K recurrence
      -> common endpoint cube and 126 Lipschitz bound
      -> RH-386 growing-order transfer
      -> uniform rank frontier S_y.

The theorem is simultaneous in every exact rank `2<=s<=S_y` and every
exact factorial order `1<=K<=floor((2s-1)L)`.  The frontier obeys
`S_y~(1-delta)log L/log 7`; it is not a maximum of separately proved
fixed-rank statements.

## Route B: GO -- fixed-rank sharp retention necessity

For every fixed threshold `s>=2`, the newly proved all-rank coefficient
`gamma_(s-1)>0` combines with Maynard bounded consecutive gaps, the exact
successor, and the common-head Taylor estimate.  Smoothing the missing
rank `s-1` in either the `I` or `J` hierarchy has error unbounded relative
to `P_s`.

This result extends the RH-388 rank-one prototype to every fixed rank.  It
does not establish necessity for a moving `s`, for an arbitrary surrogate,
or after adding different exact prime data.

## STOP_SCOPED boundary

Stop rather than extrapolate if a consumer requests:

- growing-rank necessity;
- a universal obstruction for arbitrary surrogates;
- convergence of the factorial asymptotic series;
- complex channel parameters or channels outside `c=1,...,7`;
- active `c11`, a growing clock, or prefix-dependent `K_N`;
- a cubic or `P_(s+1)` coefficient not paid by the error ledger;
- an operator, trace, zero-divisor, or RH conclusion.

## Source route

The proof-minimal closure deliberately skips unrelated RH-389/TPC-137/Tao
material.  It contains 87 immutable RH-388 release blobs plus the ordered
Johnston--Yang and Maynard logical locks, for 89 logical inputs.  Neither
remote payload is vendored.

## Artifact and release route

Release completion requires simultaneous agreement of:

1. 72 independently checked rows and 24 semantic mutations;
2. fresh result and recursively closed official Draft 2020-12 schema;
3. 87 release Git blobs and two ordered remote logical locks;
4. two default-offline verifiers making zero network requests;
5. byte-identical main and semantic PDFs;
6. a fixed 34-member publication manifest plus outer verification;
7. recursive exclusion of all four remote payload hashes;
8. theorem, integrity, review, format, source, visual, and replay audits.

Every finite row remains exact reproduction, not analytic proof.
