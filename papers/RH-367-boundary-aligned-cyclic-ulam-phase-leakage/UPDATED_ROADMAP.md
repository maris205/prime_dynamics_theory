# Roadmap after RH-367

RH-367 closes the exact finite-Ulam alignment/phase-defect audit at its
declared scope while leaving the physical coordinate

```text
actual_same_clock_unnormalized_head_transport_open
```

unchanged.

The only mathematically meaningful continuations of this branch are:

1. Construct a common strong-space projector and resolvent perturbation
   theorem that genuinely identifies the finite-Ulam family with a continuum
   operator.
2. Prove a kernel- and boundary-independent asymptotic law, if one exists,
   for a declared global leakage observable. The present source explicitly does
   not supply this.
3. Audit the direct/split noisy operators on one fixed function space with
   interval or operator-norm certificates; retain all fitted slopes as
   diagnostics until then.
4. Return to the original physical triggers or to a genuinely new arithmetic
   source. Repackaging RH-3, RH-10, RH-55, or the same cyclic scan is not a
   new theorem edge.

Route A is `GO` for the finite theorem; Route B is `STOP_SCOPED`. Gates A--E
remain false/open.
