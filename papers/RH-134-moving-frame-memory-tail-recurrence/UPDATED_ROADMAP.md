# Roadmap after RH-134

## Immediate metric bridge

Transport the recent action Gramian on the same temporal polar gauge.  Use a
lower Gram comparison to convert the raw affine tail recurrence into a
relative Rayleigh recurrence.

## Sharp obstruction

Quantify how the forcing term is amplified by the weakest target Gram
direction.  Without a lower metric gap, arbitrarily tiny absolute forcing
can yield arbitrarily large `q_t`; state this obstruction sharply if needed.

## Audit

Compute temporal `rho_t` and `q_t` on all 330 transitions, keeping first
births, frame forcing, and boundary forcing separate.
