# Citation verification

TPC-288 is a finite exact-arithmetic continuation of the frozen TPC-268 and
TPC-287 objects.  It introduces no external analytic estimate and makes no
priority claim.  The manuscript cites the upstream project artifacts only as
repository provenance; the PSD, modular-rank, and interval arguments are
proved in the local derivation/proof package.  Standard linear-algebra facts
used in the proof are stated directly, so no unverifiable external quotation
is required.
