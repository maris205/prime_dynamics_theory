# TPC-295 citation verification

No external theorem is needed for the new claims.  The surjectivity and
modular denominator-clearing lemmas are proved directly in
`PROOF_PACKAGE.md`; the parent TPC-294 and frozen TPC-268 artifacts are local
provenance locks.  Earlier route references are local TPC releases rather than
external literature.
