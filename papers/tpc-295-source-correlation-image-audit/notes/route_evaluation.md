# TPC-295 route evaluation

The Session-named `propose.md`, `skills/route-a-evaluator.md`, and
`skills/route-b-evaluator.md` are absent from this checkout.  No official
Route-A/Route-B pass is therefore claimed.  The local fail-closed fallback is
the direct surjectivity proof, denominator-clearing lemma, canonical modular
certificate, independent source-first replay, adversarial linear-algebra
stress test, and Bridge-B checker.

Route position: the broad finite source-correlation image is full on the
declared rows, so “sign pattern cannot be reached by any finite source vector”
is no longer a useful obstruction at this level.  The next gate is the actual
restricted source profile and the norm/budget of the explicit preimage.

```text
ROUTE_A = NOT_EVALUATED_OFFICIAL_FILE_ABSENT
ROUTE_B = LOCAL_FAIL_CLOSED_CERTIFICATE_ONLY
ARITHMETIC_ADVANCE = NO
FIXED_POWER_CREDIT = 0
FULL_GATE_B = OPEN
TWIN_PRIME_RESULT = NONE
```
