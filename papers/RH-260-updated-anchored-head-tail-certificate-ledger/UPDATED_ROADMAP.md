# Roadmap after RH-260

## Current coordinate

```text
legal_heads_obstructed_target_tail_exists_Ms_uncertified_quotient_finite_nonuniform_complete_certificate_zero
```

RH-260 sharpens the RH-250 interface rather than closing it.  The target
analytic tail is now an exact existence statement: for every strict subdisk
inside the RH-252 radius, a finite `M_S` exists and supplies a geometric tail
formula.  That statement is not a numerical certificate because `M_S` has not
been interval- or otherwise rigorously bounded.

The two expanded head routes currently audited remain empty: RH-255's
single-use box has 0/32 passes, and RH-258's unit-cap signed-integer lattice
has 0/32 passes.  RH-259 has 23/23 finite contractive twelfth powers, but the
worst finite root rate is `0.5056418006`, nine archived endpoints are outside
the audit, and no uniform small-noise theorem follows.

The next reopening inputs are therefore independent obligations:

1. a genuinely new legal anchored selector outside both archived head classes;
2. a certified numerical bound for `M_S` on a useful circle;
3. a uniform quotient block-power theorem covering the missing endpoints and
   the relevant small-noise family; and
4. an exact cloud-to-deterministic coefficient bridge.

RH-261 may review the ten-paper batch, but it must preserve these distinctions.
All Gates A--E remain false/open.
