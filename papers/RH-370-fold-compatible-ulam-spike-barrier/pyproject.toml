[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rh370-fold-compatible-ulam"
version = "0.1.0"
description = "Exact quotient and spike-barrier audits for RH-370"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
