# Roadmap after RH-219

```text
fixed quartet as local branch seed                  [useful]
fixed quartet as infinite determinant               [rejected]
repeated fixed factor as locally finite spectrum    [rejected]
gauge-complete finite factor                        [next]
rank-growing physical divisor                       [required, open]
```

RH-220 restores the center and scale data discarded by normalization before
the program grows the root cloud.
