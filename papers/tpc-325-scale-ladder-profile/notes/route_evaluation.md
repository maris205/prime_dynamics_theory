# TPC-325 route evaluation

## Local fail-closed evaluation

The project-level producer, independent reverse/einsum replay, scale stress
suite, PDF checks, provenance locks, and normal/optimized equality are the
available local controls.  The Session-named `propose.md`,
`skills/route-a-evaluator.md`, and `skills/route-b-evaluator.md` are absent
from this checkout, so no official Route-A or Route-B pass is claimed.

## Route state

```text
STRUCTURAL_INTERFACE = PROVED_EXACT_FINITE
FINITE_SCALE_READOUT = NUMERICALLY_CERTIFIED_FINITE
ARITHMETIC_ADVANCE = NO
FIXED_POWER_CREDIT = 0
FULL_GATE_B = OPEN
TWIN_PRIME_RESULT = NONE
```

The natural next test is a disjoint scale replication or a literal
source-native signed arithmetic `L2` estimate.
