# TPC-325 citation verification

The manuscript cites only the local parent-engine provenance and standard
linear-algebra facts used for finite PSD Gram matrices.  No external theorem
is used to support the scale trend or any arithmetic conclusion.  The exact
finite identities are proved in `DERIVATION_PACKAGE.md` and
`PROOF_PACKAGE.md`; all numerical statements are backed by the canonical
certificate and replay scripts.
