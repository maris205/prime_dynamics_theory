# Roadmap after RH-175

```text
literal infinite history shift determinant     [rejected]
finite cyclic history closure                  [next]
  -> remove stationary cycle mode
  -> compare determinant with geometric cloud
  -> retain orientation by marked traces
physical transfer identification               [open]
```

The negative result is branch-specific.  A finite cycle has no infinite
shift tail and has an exact finite determinant, so it is the next route.
