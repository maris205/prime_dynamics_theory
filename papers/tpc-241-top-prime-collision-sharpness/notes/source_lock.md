# Source lock

The theorem uses four inputs already locked in this session:

1. TPC-215: on top-prime denominators, `C_p=-log(p)/p` exactly.
2. TPC-238: the normalized finite-window lower frame for the complete primitive
   frequency vector.
3. TPC-240: the same fixed-profile, top-p, shell-q source row and endpoint-safe
   lattice-depth inequalities.
4. The classical prime number theorem with partial summation, giving
   `sum_(Q<q<=2Q)q=(3/2+o(1))Q^2/log Q` and
   `sum_(U/2<p<=U)(log p)^2/p=(log 2+o(1))log U`.

The new step is Cauchy after q-collapse, not a new external analytic theorem.
No external theorem is claimed for signed four-packet cancellation.
