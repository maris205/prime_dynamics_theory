# Roadmap after RH-309

The strongest direct analytic reopening input is now endpoint `H2`
convergence at any rate compatible with the `1/sqrt(log(1/sigma))` lower
barrier.
The competing typed route reaches a different critical phenomenon: its first
counterloop alias lies exactly at the archived Gaussian localization horizon.
