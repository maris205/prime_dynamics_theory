# Roadmap after RH-154

Finite overlap birth spikes can be removed without changing an eventual
support theorem.  The terminal half of every frozen chain has a common
overlap floor above `0.0666`, inverse upper below `15.01`, and the same
positive correlated-base minimum as the full atlas.

1. **RH-155: native reset Gram--tail assembly.** Compress recent and memory
   components directly onto each spectral reset packet and certify their
   paired generalized ratio.
2. **RH-156: delayed reset support cocycle.** Insert the native pair into the
   terminal-half congruence chain and propagate a support lower.
3. **RH-157: refinement law.** Test whether a fixed fractional suffix keeps
   a scale-independent overlap floor under finer anchors.

The half-horizon choice is a finite optimal retention certificate, not an
all-level theorem.  No Stage A, Hilbert--Polya, zeta-zero, or RH claim is made.
