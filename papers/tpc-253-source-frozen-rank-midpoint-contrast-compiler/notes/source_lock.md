# Source Lock

Observed repository baseline:

```text
d7f1155999eb494bdbb98aefe1041a5d8928578f
```

Observed `TPC_HANDOFF.md` SHA-256:

```text
f2ad77eb42eab18b427b7a8032c68d687209a0d884851ee3c05513c83bf5e91c
```

Frozen GO bridge:

```text
31333053692ca404b6de9a5463cdc803f6b784bbdcc4ca3af36c9ebe16431b16  research/tpc-big-road/bridge_b_source_frozen_rank_midpoint_contrast_compiler.md
```

The complete 249-line bridge was read before drafting. Its status is exactly
`PROVED_STRUCTURAL_L1_SOURCE_FROZEN_RANK_MIDPOINT_CONTRAST_COMPILER`.

Frozen predecessor tree digests:

```text
TPC-247 tree  c984cbb9c51fabea54e02618e96efa587b5c1d266a8ac0768c365ad4fc497bf9
TPC-252 tree  919fb268d805289ab589738926baad73e6e8830db7e4700bdb014c7ac6238c22
```

Each tree digest is SHA-256 of the sorted `sha256sum` lines for every regular
file under the locked predecessor directory, with paths relative to the
repository root.

Frozen predecessor bridge-file SHA-256 values:

```text
54bb956ad55245970a7d5d8852f1472d6a9dae68e940d1f9ced0b4c243271eed  bridge_b_literal_v59_source_operator_attachment.md
2763e2b1b67e1ad0e7dd68f0429eed7ae250691d60fdb1c94fcdff89a579f5f3  bridge_b_declared_partition_refinement_degeneracy.md
```

Imported object and conventions:

```text
I_x=(x/2,x] intersect Z={n_1<...<n_N}, N>=2,
ell=floor(N/2), r=N-ell,
L=first ell ordered coordinates, R=remaining r,
rho^2=ell*r/N,
z=rho(1_L/ell-1_R/r),
g=A_x beta,
C_x=<w,g>,
inner product conjugate-linear first.
```

The rank split is frozen before inspecting coefficients, contrasts, margins,
or signs. It is a source-only modeling choice, not a V59-canonical partition
and not V59's smooth partition. The literal TPC-247 operator retains
output/input orientation, outer prime weight, unit masks, deleted diagonal,
physical kernel, centered residue bracket, and literal `beta`.

No symmetry of `K_H`, self-adjointness of `A_x`, actual V59 numerical replay,
sign/nonzero estimate, asymptotic or arithmetic advance, L2, fixed-atom
credit, Gate-B closure, strict `1/400`, or twin-prime result is imported. No
locked source path, handoff, Git index/history, dynamic research document, or
other paper directory is modified.
