# Roadmap after RH-312

The endpoint obstruction is now localized to a universal logarithm; the
remaining deterministic factor is analytic past the endpoint circle.  The
next step is to split the logarithm into its orthogonal even and odd Hardy
channels.  No noisy spectral realization follows from this decomposition.
