# TPC 并行研究侦察：第一轮整合报告

日期：2026-09-09。任务：在用户明确授权后启动多 agent 研究，寻找可核验的新输入。
位置：主线为②解析消去岛、图像 Bridge A 内的 Gate A / Gate B；
同时检查③/④的前缀回接与⑤的指定种子储备线。

**本轮结论：保留两项经独立审查的增长模型结论，若干条件接口及限定反例；
没有得到真实物理标量所需的固定幂次消去，没有编号论文的 GO。**

实际使用了八个不同任务的只读侦察 agent，随后增加两个独立证明审查 agent。
十份完整报告均已保存，所有 agent 均已收回；没有启动写论文 agent。
[任务目录与原始审查记录](research/tpc-big-road/research-rounds/2026-09-09-multi-agent-recon/README.md)

## 研究地形与本轮判断

当前正式来源 TPC418 给出了有限完整壳层的交替符号、局部能量归一化算子界。
本轮证明其一个共同增长推论，并另外研究了保留同余项的长窗口模型。
前者使用素数定理消去壳层家族依赖；后者使用绝对行和与能量的比较。
二者的数学成立性与真实算术用途必须分开。

真实 Gate B 对象仍是 TPC247/V59 的 $\mathfrak C_x=\langle w,A_x\beta\rangle$。
物理高度是 $H_x=x^{21/32}$、素数尺度是 $Q=x^{1/3}$，固定移位 $h_0=2$
出现在 $w(u)=\Lambda(u+2)-b_x^{(z)}(u)$ 中。
要求的是 $\mathfrak C_x\ll x^{5/3-\delta+o(1)}$，且 $\delta>1/400$，
不是一个可任意选择分母的模型范数趋零。
[原始系数及标量契约](research/tpc-big-road/bridge_b_polarized_local_bdh_scalar_compiler.md)

侦察读了十四篇近期相关原始论文的摘要/引言，并针对实际引用读取定理陈述。
Blomer–Pascadi 的 Kloosterman 固定幂次输入已在仓库 V59 被登记；
在平方根长度处的局部节省不自动带来保持真实系数、单位掩码、对角修正和总归一化的分解。
[原始定理 1.1](https://arxiv.org/html/2607.24311v1)
Guo 的新预印本陈述具有很强的移位一致性，但它讨论对数加权 Liouville 相关；
这里未独立验证其完整证明，也不将其当作自然计数幂次节省。
[原始陈述，v4](https://arxiv.org/html/2608.23500v4)

因此，优先级应由“是否触及真实对象与完整损失”决定，而不是哪个模型最容易再得到小范数。
以下排名是进一步研究的顺序，不是论文发表推荐。

## 推荐保留的方向与精确下一问题

### 1. 真实系数回接与加权输入/输出代价：优先级最高，尚未解决

- **假设/目标：** 保持 $A_x,\beta,w$ 原样，寻找能付清输入、输出与全域重组代价的消去机制。
- **本轮实得：** 在一个合法有限 CRT 子窗口上，得到精确的重加权与 Schur 乘子接口
  $A_J=\lambda R(F\circ M)R$，而不是直接令物理算子等于交替符号模型。
- **最低验证：** 每个候选必须逐项展开真实核，计算重加权矩阵和实际加权向量范数，
  并界定 $\mathfrak C_x-\mathfrak C_J$。只比较指数或某个矩阵的谱不够。
- **关键失败检验：** 本轮接口的 $\|R\|^2\ge C/|A|$，与旧 bulk 项相乘给出
  $(C/|A|)(16|A|/V_-)=16C/V_-$；交替 bulk 增益在这种范数转移中被抵消。
  若 $A=0$，该对角回接本身失效。
- **覆盖问题：** 有限例子 $x=64$ 只覆盖 18/1,352 个允许的三元组，
  这是支持计数而非质量估计。完整物理壳层达到七个素数时，区间内的这类端点 CRT
  子窗口已经不可能存在；无限大 CRT 原点不能充当物理原点。
- **新意/风险：** 精确代价有诊断价值；最接近 TPC247、TPC235、TPC417/418，
  未建立发表级新颖性。真实物理推进风险高，不能宣称本轮已提供全域回接。
- **试验：** 有限系数恒等式的精确有理数检查通过；新有限接口仍按候选等级保存，
  未经第二名 agent 的完整独立证明审查。无需 GPU。

[完整回接报告](research/tpc-big-road/research-rounds/2026-09-09-multi-agent-recon/agent-reports/recon-physical-attachment.md)

一个必须保留的预算恒等式是：若模型 $M=D^{1/2}ZD^{1/2}$，则

$$
|\langle w,M\beta\rangle|
\le\|Z\|\,\|D^{1/2}w\|_2\,\|D^{1/2}\beta\|_2.
$$

即使未来把长窗口界 $\|Z\|\ll Q^{-1}$ 接到真实对象上，
通过这个不等式得到目标仍需要加权向量范数乘积达到 $x^{2-\delta+o(1)}$，
$\delta>1/400$。此预算在本轮未付清；这也不是所有可能消去方法的必要条件。

### 2. 自适应好块覆盖：条件前缀引理，不是已证的算术选择器

- **假设：** 实际阈值定义的同一个好尺度集 $G_X$ 满足前驱覆盖
  $[(1-\epsilon)v,v]\cap G_X\ne\varnothing$，对所需全部 $v$ 一致成立。
- **推导：** 对字面周期 determinant-two Möbius 系数、间距 $q=as$，可将累计前缀递归拆成
  好块与短缺口。报告给出了终端归一化 $T/q$ 下的最大前缀界，代价依次为
  好块误差、$2\epsilon$、小前缀以及 $O(q\log(T/Y)/T)$ 格点项。
- **未证部分：** 上述覆盖假设。全局对数密度小并不排除一整段前驱区间坏掉。
- **最低验证：** 必须控制真实共同好尺度集的局部孔洞；有限计算只能测试该谓词，不能给出渐近假设。
- **新意/风险：** 与 TPC159 的精确二进祖先避让不同，但主要是初等自适应重组。
  新颖性未建立，条件输入风险高；即使覆盖成立，所得仍首先是对数增益而非物理固定幂次。
- **试验：** 两条真实小纤维的精确事件区间测试中，阈值 1/8 无孔洞，改为 1/16 出现四个孔洞。
  这验证了失败检验的可执行性，不证明渐近覆盖。无需 GPU。

[条件引理、完整公式与反例](research/tpc-big-road/research-rounds/2026-09-09-multi-agent-recon/agent-reports/recon-mobius-prefix.md)

### 3. 两项增长模型结论：作为已审查工具保留，不据此单独推进编号

**短窗口推论。** 对 TPC418 的完整整数壳层、原有全局交替符号与 CRT 掩码，
$N=4H<p_{\min}$。每次在坐标前缀上重新计算能量，对绝对充分大的 $H$ 有

$$
\sup_{k,\,R_0\le R\le4H}\|Z_{k,R}\|_2
\le\frac2{\sqrt{\min(H,\lfloor R_0/2\rfloor)}}
+\frac{192\log(4H)}H.
$$

它随 $H,R_0\to\infty$ 一致趋零，常数不依赖壳层数或最大尺度。
但短前缀不能免费包含：$R=2$ 时范数严格大于 $1/4$；一般有界 $R_0$ 也有正下界。
独立审查补上了每个壳层前缀至少两个素数的绝对阈值。
[整合证明](research/tpc-big-road/research-rounds/2026-09-09-multi-agent-recon/SHORT_WINDOW_COROLLARY.md)

**长窗口界。** 对完整 C1 核、任意原点、任意实数系数 $|\varepsilon_p|\le1$，
$H\ge p_{\max}$、$N=4H$，令 $D$ 为全部成分的行平方能量、$\mu=\min_p(a_pp)$，则

$$
\|D^{-1/2}MD^{-1/2}\|_2\le144\pi/\mu<576/\mu.
$$

壳层版本给出 $576/Q_{\min}$。它保留长窗口的同余邻居，允许任意原点和符号；
增益来自能量分母与绝对行和比较，不是算术符号消去。
[证明](PROOF_PACKAGE.md)；[推导及诊断](DERIVATION_PACKAGE.md)。

- **验证：** 两项均通过独立只读证明审查；数值探针只是辅助诊断。
- **最接近工作：** 短窗口是 TPC417/418 加经典素数定理的推论；
  长窗口使用经典加权 Schur 型机制，不是新发明的范数原理。
  近期文献亦把加权 Schur 检验作为既有基线。[原始论文引言](https://arxiv.org/html/2510.05585v1)
- **价值/风险：** 修正“只能有限验证”的过宽概括，明确增长与前缀边界；
  发表级新意低或未定，真实算术用途未定。无需 GPU pilot。
- **否决线：** 不允许将两个不相交的窗口范围混用，也不能把 $H$ 叫作物理 $h_0=2$。

## 被排除的具体方法格：不代表全局不可能

| 方法格 | 本轮证据 | 不可扩大为 |
|---|---|---|
| 保留真实 Möbius 折叠对角项的非负 majorant 支付固定幂次 | 实际半素数子族给出 $D_B\gg x^{5/3}/(\log x)^2$，并有明确正的渐近常数 | 真实有符号能量或 Gate A 标量必然大 |
| 短窗口中把交替符号界推广到任意符号 | 某些合法符号族的归一化算子范数大于 $1/512$；原点可取 1 的版本也成立 | TPC418 的既定交替符号定理错误 |
| 用 CRT 原点存在性代替物理区间内存在性 | 素数乘积必须整除 $o(o+N)$，造成明确原点/壳层数量障碍 | 所有算术或非 CRT 回接均不可能 |
| 从全局好尺度密度和对数估计推出每个指定前缀 | 一个真正由稀疏块序列产生的相关函数同时满足这些估计，仍保留坏尺度簇 | Möbius 自身存在该反例 |
| 从几乎处处稀有事件结论推出指定种子命中 | 同一 odometer 上等质量、可和 $L^2$ 微扰保留度量型前提，却抹掉指定种子命中 | 原始筛函数的指定轨道已被反驳 |
| 直接导入更强的一般文献定理 | 未找到同时匹配系数、域、归一化并支付完整物理损失的输入 | 文献方法完全无用或不存在未来组合 |

上述反例/障碍保留各自的来源、推导与探针等级；除两项专门证明审查外，
不宣称所有侦察结论都已经再次独立复核。
[完整报告索引](research/tpc-big-road/research-rounds/2026-09-09-multi-agent-recon/README.md)

## 回归、复现与工作区保护

当前启动回归的八种命令/模式均通过：实际由两次顶层 wrapper（普通与 `-O`）各调用六个子检查，
合计十二次子执行，不是八条单独的顶层命令。
QA 同时指出：大四壳层行部分重用 TPC417 证书；独立性不是全量重算。
独立 stress 验证器接受某些重新绑定摘要后的类型/高度/状态改动，
但没有证明它们能绕过完整的摘要锁定 wrapper。PDF PASS 只代表存在与哈希一致，不代表语义验证。
这些问题已记录，未擅自修订旧科学证书。
[回归与边界报告](research/tpc-big-road/research-rounds/2026-09-09-multi-agent-recon/agent-reports/recon418-regression.md)

起始 HEAD 与抓取后的 `origin/main` 均为 `ab23455ba941e5a14ded27d49de0e874aee811ac`。
起始工作区有十条 tracked 修改和 615 个 untracked 文件；因已有改动不安全，未做 rebase。
侦察与审查 agent 全部只读。本轮只有主线程写入无编号研究材料，并在交接头部追加本轮结论；
保留旧交接全文、正式论文、候选账本、路线图与用户维护改动。
没有 commit、push、PDF 发布、新论文目录或自动新编号。

## 最终路由决定

`STOP_SCOPED_PHYSICAL_TRANSFER_AND_NOVELTY_UNPAID`：停止把本轮模型结果推进为编号论文。
图像 Bridge A 内 Gate A / Gate B 仍开放；图像 Bridge B 的指定算术种子问题也未解决。
下一项有意义的新输入必须是实际算子/权重的低损失回接、真实前缀的新增局部控制，
或能够支付有符号总和的源定理。更多同类有限证书不是重开条件。

工作流说明：`idea-creator` 用于生成、筛选和保存正负结果；`formula-derivation` 与
`proof-writer` 将恒等式、模型命题、缺失假设和独立审查分开；`novelty-check` 用于有界查重。
指定的外部 GPT-5.4 MCP 不可用，采用当前可用的独立子 agent 审查，未冒称跨模型认证。
本任务为纯数学研究，未启动 GPU 试验；也未声称完成全面新颖性检索或所有论文的完整证明复核。
