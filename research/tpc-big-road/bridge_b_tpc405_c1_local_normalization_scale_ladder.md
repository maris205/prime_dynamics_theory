# Bridge B: TPC-405 local-normalization scale ladder

TPC-405 upgrades the TPC-404 selected-prime CRT proxy identity to an exact
finite parameter-uniform bound for one adjacent normalized entry.  For
integers `H>=1`, `N>=H+2`, `Q>N`, distinct shell primes `Q<p_i<=2Q`, and the
explicit CRT residues `0` on even indices and `-N` on odd indices, the exact
certificate proves

```text
0 <= z <= t_1/(a_min sqrt(S_0 S_1)) <= 4/(a_min H) <= 4/H.
```

The producer and literal independent CRT row-energy replay cover the declared
five heights `H=16,32,66,128,256` and four multiplicities `m=1,2,3,4`, for
20 exact cases.  The stored decimal values are observations; the inequalities,
CRT data, and local energies are exact rational data.

This is a theorem for one synthetic selected-prime proxy entry.  It is not a
full operator estimate, a physical `h_0` theorem, an arithmetic `L2` result,
an arithmetic sign theorem, a fixed-power saving, Route-B closure, or a
twin-prime conclusion.  The normalized growing theorem and full operator norm
remain open.
