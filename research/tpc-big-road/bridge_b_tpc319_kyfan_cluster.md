# Bridge B — TPC-319 Ky Fan cluster and normalization firewall

This bridge carries the next finite diagnostic on the same literal
deleted-diagonal centered prime-shell operator.  TPC-319 replaces the single
top eigenvalue by a variational Ky Fan cluster mass and audits normalized and
unnormalized scale laws side by side.  It is a scoped diagnostic bridge, not an
official Route-A or Route-B evaluator; the Session-named evaluator files are
absent from this checkout.

`text
TPC319_MAXIMUM_CLAIM = NUMERICALLY_CERTIFIED_FINITE_KY_FAN_CLUSTER_NORMALIZATION_AUDIT
TPC319_ROUTE_ADVANCE = YES_SCOPED_KY_FAN_CLUSTER_AND_NORMALIZATION_FIREWALL
TPC319_KY_FAN_AUDIT = NUMERICALLY_CERTIFIED_FINITE_24_ROWS_5_K
TPC319_NORMALIZED_DECREASES = NUMERICALLY_CERTIFIED_FINITE_80_OF_80
TPC319_UNNORMALIZED_INCREASES = NUMERICALLY_CERTIFIED_FINITE_80_OF_80
TPC319_NORMALIZATION_FLIP = PROVED_EXACT_FINITE_IDENTITY_PLUS_NUMERICALLY_CERTIFIED_FINITE_80
TPC319_CLUSTER_GAP_CENSUS = NUMERICAL_OBSERVATION_FINITE
TPC319_EFFECTIVE_RANK = NUMERICAL_OBSERVATION_FINITE
TPC319_ARITHMETIC_ADVANCE = NO
TPC319_FIXED_POWER_CREDIT = 0
TPC319_FULL_GATE_B = OPEN
TPC319_TWIN_PRIME_RESULT = NONE
TPC319_ROUND2_CLUE = AUDIT_A_SCALE_INVARIANT_SPECTRAL_MEASURE_OR_PROVE_A_SOURCE_NORMALIZATION_LAW_BEFORE_ANY_POWER_CLAIM
`

## Scope and result

The protocol fixes H=66, X=640,1280,2560, Q={24,36,54,80}, s={1,2}, and
k={1,2,4,8,16}.  The 24 rows use forward and reverse shell accumulation,
SciPy top-17 and NumPy full symmetric spectra, and a finite Weyl guard based on
the safe literal bound |K|<=160.

For every adjacent scale and every k, the unnormalized Ky Fan mass grows
strictly while the source-normalized mass decreases strictly.  The exact
identity

`text
(F_k(2N)/(2N))/(F_k(N)/N) = (1/2) F_k(2N)/F_k(N)
`

shows why this is a firewall against reading normalized finite compression as
arithmetic power saving.  The edge-gap census also shows that a canonical
one-dimensional eigendirection is not available uniformly on the panel.

The result is same-engine finite evidence.  It does not prove a source-scale
normalization law, a stable limiting spectral projector, signed prime-shell
reassembly, or a twin-prime endpoint.
