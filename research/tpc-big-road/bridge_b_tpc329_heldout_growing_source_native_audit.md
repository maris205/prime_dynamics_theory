# Bridge B — TPC-329 held-out growing source-native audit

TPC-329 is the held-out continuation of TPC-328.  It evaluates the same
literal deleted-diagonal centered prime-shell operator on two new origins
(`28001`, `36001`) and two larger scales (`4096`, `8192`).  The finite V59
source-native residual is tested under four predeclared sign laws and paired
across scales.  A fixed affine coordinate permutation
`pi(i)=(5*i+17) mod source_count` is applied as a hostile null; it is bijective
for both source counts and preserves the source multiset and `L2` norm.

The canonical certificate has `32` actual rows, `64` scale-paired law records,
and `128` actual/permuted comparisons.  The actual all-plus census is
`31` negative / `1` positive; the permuted all-plus census is `0` negative /
`32` positive, with `31/32` classifications changed.  The other actual /
permuted law censuses are `25/7` versus `30/2` (alternating), `32/0` versus
`32/0` (mod-4), and `32/0` versus `28/4` (half split), all in
negative/positive order and with no unresolved row.  Component controls are
positive on `32/32` rows.

The exact anchor `[28001,28016]`, `Q=4`, `s=1` stores all three rational Gram
digests.  The producer, independent checker, stress suite, PDF audit, and
normal/optimized equality are required by the local fail-closed checker.

    TPC329_MAXIMUM_CLAIM = NUMERICALLY_CERTIFIED_FINITE_HELDOUT_GROWING_SOURCE_NATIVE_AUDIT
    TPC329_EXACT_GRAM_DECOMPOSITION = PROVED_EXACT_FINITE
    TPC329_SOURCE_NATIVE_VECTOR = PROVED_EXACT_FINITE_DECLARED_MODEL
    TPC329_COMPONENT_CONTROLS = NUMERICALLY_CERTIFIED_FINITE_32_OF_32
    TPC329_PLACEMENT_NULL = NUMERICALLY_CERTIFIED_FINITE_MULTISET_PRESERVING_CONTROL
    TPC329_ALL_PLUS_ACTUAL = NUMERICALLY_CERTIFIED_FINITE_31_NEGATIVE_1_POSITIVE
    TPC329_ALL_PLUS_PERMUTED = NUMERICALLY_CERTIFIED_FINITE_0_NEGATIVE_32_POSITIVE
    TPC329_PLACEMENT_CHANGES = NUMERICALLY_CERTIFIED_FINITE_31_OF_32
    TPC329_ARITHMETIC_ADVANCE = NO
    TPC329_FIXED_POWER_CREDIT = 0
    TPC329_GROWING_SOURCE_NATIVE_L2 = OPEN
    TPC329_FULL_GATE_B = OPEN
    TPC329_TWIN_PRIME_RESULT = NONE
    TPC329_STATUS = NUMERICALLY_CERTIFIED_FINITE_HELDOUT_GROWING_SOURCE_NATIVE_AUDIT
    TPC329_ROUND2_CLUE = SEPARATE_SOURCE_NORM_FROM_ARITHMETIC_PLACEMENT_WITH_MULTIPLE_PREDECLARED_CONTROLS

The two-scale slopes and placement contrast are finite observations.  They do
not provide a source-uniform arithmetic estimate, fixed-power saving, strict
`1/400` payment, official Route-A/Route-B evaluator pass, or twin-prime
conclusion.  The Session-named evaluator files are absent from this checkout;
the local checker is explicitly a fallback control.
